# ZHP PMS — Claude Code Context

> Read this file before starting any work. It defines naming conventions, architecture decisions, and current project state. **Keep it updated** — see "Working Across Multiple Machines" at the bottom. This file is the single source of truth that travels with the repo between the laptop and the PC; if it's stale, the next session starts blind.

---

## What this is

**ZHP PMS** (working platform name — originally built as "Zahill PMS" for a single property, now a **multi-tenant Property Management System platform** intended to be sold/hosted for multiple hotel/glamping clients from one shared codebase and database). Zahill is the first, reference property running on the platform; "Birdnest" is a second property used to build and verify multi-tenancy end-to-end. Both run on the same infrastructure, fully data-isolated from each other.

Built as a monorepo with React/Vite frontend(s) and a Node/Express backend shared by every property.

**Owner:** Juppy (juppyjp@gmail.com)

**Product name is now locked: HALF.** Domain/tenancy scheme decided (2026-08-14): a single app subdomain (`half.kdai.cloud`), not one subdomain per client. Every property logs into the same domain; the JWT (already carries `propertyId`) is what resolves tenant, exactly like GitHub/Stripe Dashboard/Google Workspace rather than Shopify-style `client.myapp.com`. Rejected per-client subdomains because it would require wildcard DNS/cert + a Host-header-based login rewrite for no real benefit at current scale (one browser origin is fine since no one needs two properties open in parallel tabs today — superadmin already has its own separate, non-property-scoped auth layer for cross-property work). Module-specific subdomains keep the existing pattern, just moved off `zahill.kdai.cloud` onto `half.kdai.cloud` (e.g. `kitchen.half.kdai.cloud`, `display.half.kdai.cloud`, `tv.half.kdai.cloud`) — small fixed set, no wildcard needed. If a client later wants their own branded custom domain, that's a per-client CNAME added on top of this, not a redesign.

**Not yet done:** this is a naming/architecture decision, not an executed migration — `zahill.kdai.cloud`/`d-zahill.kdai.cloud` are still the live domains, and "Zahill PMS"/"ZHP PMS" still appear throughout code (package names, page titles, MQTT topic namespace, Nginx configs). Renaming those and standing up `half.kdai.cloud` is future work, not done in this pass.

---

## Monorepo Structure

```
ZHP PMS/
├── client/              ← PMS frontend (React/Vite PWA) — property staff + superadmin
│   └── src/pages/admin/ ← Superadmin-only pages (Properties list, PropertyDetail)
├── server/              ← Shared backend (Node/Express), serves ALL properties
├── room-display/        ← Room Display PWA (per-room tablet)
├── tv-display/           ← TV Welcome Display (React/Vite page loaded by the screensaver APK)
├── kitchen-display/      ← Kitchen Display PWA (kitchen ticket board kiosk)
├── resto-display/        ← Resto Ordering app (guest QR self-order + floor staff order-taking/confirm-queue/table PWA)
├── tv-screensaver/       ← Android TV DreamService APK (Kotlin) that wraps tv-display in a WebView
├── room-display-kiosk/   ← Android tablet kiosk APK (Kotlin) that wraps room-display in a WebView + posts device telemetry (Phase 1; no lock-down yet — see ROOM_DISPLAY_KIOSK_PLAN.md)
├── PLANNING.md          ← original single-property feature/schema spec (historical reference)
├── ROADMAP.md           ← current feature roadmap, phase status, module registry
├── MULTI_TENANCY.md     ← how/why multi-tenancy was built, migration-by-migration detail
├── ROOM_DISPLAY_KIOSK_PLAN.md ← native Android kiosk track (tablet APK + TV launcher), phased
└── CLAUDE.md            ← this file
```

---

## Naming Conventions — CRITICAL

These names are final. Do not use alternatives.

| Correct Name | Do NOT call it |
|---|---|
| **Property** | client, tenant, account (when referring to a hotel/glamping site on the platform) |
| **Superadmin** | platform admin, root user |
| **Room Controller** | ESP32 controller, IoT device, smart device |
| **Room Display** | IoT panel, tablet app, room tablet, control panel |
| **relay** | light, device, switch (in code/API) |
| **module** | feature flag, plugin (when referring to `property_modules` entries) |

Note: "Zahill PMS" as a product name is being superseded by the platform now serving multiple properties — `ROADMAP.md` already refers to it as "ZHP PMS." Not yet fully renamed across the codebase (package names, page titles); treat as in-progress.

---

## Multi-Tenancy & Platform Architecture

**This is the core architectural fact about this codebase: it is not single-tenant.** Every property's data lives in the same database, isolated by `property_id`. This was built and verified in migrations **019–026** against a live second property (Birdnest) — full detail in `MULTI_TENANCY.md`.

### How isolation works
- Every business table has a `property_id` column (added in `019_multi_tenancy.sql`).
- Every authenticated route reads `req.propertyId` (set by `middleware/auth.js` from the JWT — see below) and every query is scoped `WHERE property_id = $n`.
- The JWT issued at login (`routes/auth.js`) embeds `propertyId` alongside `userId`/`role`. `middleware/auth.js` decodes it and sets `req.propertyId` on every request.
- Formerly-singleton tables (`property_settings`, `ai_market_summary` — used to be `WHERE id = 1`) are now one row per property, scoped by `property_id` with a unique constraint instead of a hardcoded PK.
- Tables with human-readable string IDs that used to be globally unique (`booking_sources`, `payment_methods`, `roles`) are now namespaced per property (`<id>-<property_uuid>`) so two properties can both have a role called `manager` without colliding. `users.role`, `bookings.source`, `payments.method` were widened to `varchar(50)` in migration `026` to fit these namespaced values.

### Module system (per-property feature flags)
Not every property needs every feature (e.g. a property with no ESP32 hardware shouldn't see Room Controller). `server/modules.js` maps each module to the route files it gates:

| Module | Routes gated | Default |
|---|---|---|
| `reservations` | bookings, complimentary, checkin, allotments, pricing, ratePlans | ✅ on |
| `front_desk` | checkin | ✅ on |
| `guest_crm` | guests, loyalty | ✅ on |
| `financial` | payments, reports, nightAudit, folio | ✅ on |
| `operations` | tasks | ✅ on |
| `sales` | products, sales, tables | ✅ on |
| `in_room_media` | board, display | ✅ on |
| `room_controller` | iot | ❌ off by default (hardware-dependent) |
| `insights` | insights | ✅ on |
| `activities` | activities | ✅ on |
| `calling` | calls | ✅ on |
| `resto_ordering` | resto, restoGuest | ❌ off by default (paid add-on tier, same philosophy as `back_office`) |
| `back_office` | purchasing, expenses | ❌ off by default (paid add-on tier, Slices A+B + Inventory Value — Suppliers + Purchasing + PO receiving + Expenses + Inventory Value) |
| `yield_management` | yield | ❌ off by default (paid add-on) — per-room-type automatic pricing (occupancy tiers by rooms-left/%, day-of-week, holiday/event uplifts, floor/ceiling), tabs inside `/pricing` for owners; nightly job 02:00 WITA. Migration 061. |
| `smart_reports` | — (checked per report inside `routes/smartReports.js`, not a route gate) | ❌ off by default (paid add-on) — the SCHEDULED reports (Morning Brief so far) in Settings → Reports & Alerts. The page itself and the instant Telegram alerts are free for every property. Migration 068. See the Reports & Alerts write-up below. |
| `channel_manager` | channelManager | ❌ off by default — spike/validation only (migration 060), enabled only on Zahill for testing, not production automation. See `ROADMAP.md`'s "Channel Manager Integration + Dynamic Pricing" for the full step-by-step plan. |
| `pos_integration` | posIntegration | ❌ off by default — an external POS (Separuh) looks up in-house guests and charges bills to their room. Own per-property API key (`authPos`), guard applied per-route. Migration 070. See the POS Integration write-up in the Frontend section. |

`resto_ordering` assumes `sales` is also on for the property (reads `products`/`restaurant_tables`, writes `sales`) — not enforced in code, just a real dependency to be aware of when onboarding a property.

Always-on, ungated: `auth`, `dashboard`, `settings`, `units`, `users`, `communications`.

Enforcement: `server/index.js` wires `moduleGuard('<module>')` into each route mount (after `auth`, so `req.propertyId` is already set). A disabled module returns `403 {"error": "Module not enabled for this property"}`. `property_modules` (property_id, module, is_enabled) is the backing table. `GET /api/settings/modules` (owner-only) is what the frontend nav should call to hide/show sections — confirm the client actually wires this before assuming nav visibility is automatic.

### Superadmin & property onboarding
A superadmin is a platform-level operator, not tied to any one property (`users.is_superadmin = true`, `property_id` nullable for them). Distinct auth layer: `middleware/authSuperAdmin.js`, mounted at `/api/admin` in `server/index.js`, ahead of and separate from the per-property `auth` + `moduleGuard` chain.

`routes/admin.js` endpoints:
- `GET /api/admin/properties` — list all properties + module counts
- `POST /api/admin/properties` — create a property (`name`, `slug`, `plan`, `display_token`), then calls `seedPropertyDefaults(property.id)` to clone default settings/modules/booking-sources/payment-methods/roles in one shot
- `GET /api/admin/properties/:id` — property detail + its module states + branding fields (`logo_url`, `brand_color`, `property_name`, `property_address`, `property_phone`, `property_email`, joined from `property_settings`)
- `PATCH /api/admin/properties/:id` — update name/slug/plan/is_active
- `PATCH /api/admin/properties/:id/modules` — toggle a module on/off
- `PATCH /api/admin/properties/:id/branding` — update brand color + contact/invoice fields on `property_settings` (same columns the property owner can also edit in their own Settings page — both write the same row, last save wins, by design)
- `POST /api/admin/properties/:id/logo` — multipart logo upload (`multer` + `sharp`, resized to 512×512 PNG, saved to `server/uploads/property-logos/`, served unauthenticated at `/property-logos/*` since the login screen, Room/TV Display, and email clients all need it without a JWT)
- `GET /api/admin/properties/:id/users` / `POST /api/admin/properties/:id/users` — manage a property's staff accounts

**Frontend — traced button-by-button against the API above (verified, not assumed):**

`client/src/pages/admin/` (`AdminLayout.jsx`, `Properties.jsx`, `PropertyDetail.jsx`), routed in `App.jsx` at `/admin` and `/admin/properties/:id`, gated by `RequireSuperAdmin` (checks `user.is_superadmin`, redirects to `/login` or `/` otherwise). `Login.jsx` sends a superadmin straight to `/admin` after auth; `AuthContext.jsx` skips the per-property `fetchModules()` call for superadmins since they have no `property_id`.

| UI action | Wired to |
|---|---|
| `Properties.jsx` page load | `GET /api/admin/properties` — list + module count |
| "+ New Property" → modal → "Create Property" | `POST /api/admin/properties` (auto-generates `display_token` client-side via `crypto.randomUUID`) |
| "Manage" button on a property row | navigates to `/admin/properties/:id` |
| `PropertyDetail.jsx` page load | `GET /api/admin/properties/:id` — property info + modules |
| "Save Changes" (name/slug/plan/active) | `PATCH /api/admin/properties/:id` |
| Module On/Off toggle buttons | `PATCH /api/admin/properties/:id/modules` (optimistic UI update, rolls back on error) |
| `PropertyDetail.jsx` "Staff Users" card | `GET /api/admin/properties/:id/users` (list), `GET /api/admin/properties/:id/roles` (populates the role dropdown) |
| "+ Add User" → modal → "Create User" | `POST /api/admin/properties/:id/users` (validates the role belongs to that property server-side) |
| `PropertyDetail.jsx` "Branding" card — logo upload | `POST /api/admin/properties/:id/logo` (multipart, preview updates from the returned `logo_url`) |
| "Branding" card — color picker + name/address/phone/email fields → "Save Branding" | `PATCH /api/admin/properties/:id/branding` |

**Closed gap (was open until a previous session):** the superadmin UI previously had no way to create a property's first staff login — `routes/admin.js` had the `users` endpoints but nothing called them. Added a "Staff Users" card to `PropertyDetail.jsx` plus a new `GET /api/admin/properties/:id/roles` endpoint (list-only, so the create-user modal can populate a role dropdown). Onboarding a new property is now fully self-serve from the superadmin UI: create property → toggle modules → add first staff user, no API tooling required.

**Closed gap (this session): per-property branding.** Every property used to render with Zahill's hardcoded logo and name everywhere — a single static `client/public/logo.png` and hardcoded strings, since the client/room-display/tv-display apps are each one shared deployment for every property. Migration `032` added `property_settings.logo_url` / `brand_color`. The new "Branding" card above lets superadmin set logo, color, and contact/invoice fields (`property_name`/`address`/`phone`/`email` — the property owner keeps their own edit access to the same fields in their own Settings page; both write the same `property_settings` row, last save wins).

Logo threading, end to end:
- **Client nav** (`App.jsx` `TopNav`) — `SettingsContext.jsx` fetches `GET /api/settings/branding` (plain `auth`, not owner-gated — every staff role needs the nav logo) alongside its existing sources/payment-methods calls.
- **Login screen** (`Login.jsx`) — runs pre-JWT, so it can't resolve a property the normal way. Added a new no-auth route file `routes/public.js` (`GET /api/public/properties/:slug/branding`) and an optional property-slug field on the login form (also reachable via `/login/:slug`), remembered in `localStorage` (`lastPropertySlug`) so returning staff see the right branding without retyping. **This is cosmetic only** — it does not change how `POST /api/auth/login` resolves the user (still by email alone, see Known gaps below).
- **Invoice PDF** (`routes/folio.js`) — draws the logo top-right via `pdfkit`'s `doc.image()` if the file exists on disk; wrapped in try/catch so a missing/corrupt logo falls back to the pre-existing text-only header rather than breaking invoice generation.
- **Room Display / TV Display** — both already call the same `GET /api/display/room/:roomId/state` (via `authDisplay`/`display_token`); that response now carries a `property: { name, logo_url, brand_color }` field, so both apps picked up branding by threading one new prop through their screen components (`SetupScreen.jsx` is the one exception — it runs before a room/token is configured at all, so it has no property to resolve and still shows the platform default).
- **Guest emails** (`services/mailer.js`) — `wrapEmailBody()` prepends a logo `<img>` at send time (not baked into the stored templates) if `logo_url` is set. Needs an **absolute** URL since email clients don't resolve relative paths — new env var `SERVER_PUBLIC_URL` (see Environment Variables below); if unset, emails just skip the logo rather than breaking.

**Known limitation:** the Staff Users card is list + create only — no edit or deactivate/delete from the UI yet (matches what `routes/admin.js` exposes; there's no `PATCH`/`DELETE` on `/properties/:id/users/:userId`). Add those endpoints + UI controls if you need to reset a password or remove a departing staff member without going to the DB directly.

**Credentials to know about (seeded placeholders — change before production):**
- Superadmin: `admin@platform.com` / `superadmin123`
- Demo second property "Birdnest" (slug `birdnest`): owner login `owner@birdnest.com` / `testpass123` — kept as a live reference for multi-tenancy verification; harmless to keep, fully isolated from Zahill's data, or delete if no longer needed.

### Known gaps (from MULTI_TENANCY.md, still open as of last check)
- **Login still doesn't resolve the *user* by subdomain/tenant** — `POST /api/auth/login` looks up a user by email alone (globally unique since migration `023`). The login screen can now show the right property's *branding* pre-auth via an optional slug field (`GET /api/public/properties/:slug/branding`, see Superadmin section above), but that's cosmetic only — actual auth is still email-only, and there's still no real subdomain/DNS-based routing. Fine while staff don't overlap across properties; revisit if you want `{slug}.pms...` login pages or true per-property subdomains.
- `booking_sources` / `payment_methods` / `roles` use namespaced raw strings as their identity rather than a surrogate key — works, but awkward if another table ever needs a clean FK to them.
- No environment-level docs yet for a full "spin up client #3" runbook beyond `POST /api/admin/properties` — worth writing once you actually onboard a paying second/third client.

---

## System Architecture

```
┌─────────────────────────────────────────────────────────┐
│                      Cloud VM                            │
│  ┌─────────────┐   ┌──────────────┐   ┌──────────────┐  │
│  │  Mosquitto  │◄──│   Backend    │──►│  PostgreSQL  │  │
│  │MQTT Broker  │   │ Node/Express │   │  (all        │  │
│  │port 1883    │   │  port 4000   │   │  properties) │  │
│  └──────┬──────┘   └──────┬───────┘   └──────────────┘  │
└─────────┼─────────────────┼────────────────────────────-─┘
          │ MQTT             │ REST
   ┌──────▼──────┐   ┌──────▼────────┐   ┌──────────────┐
   │    ESP32    │   │  PMS client/  │   │ Room Display │
   │Room Control-│   │ (per-property │   │ + TV Display │
   │    ler      │   │  login, JWT   │   │ (per-property│
   │  per room   │   │  scoped data) │   │ display_token)│
   └─────────────┘   └───────────────┘   └──────────────┘
```

One backend, one database, many properties. The client app, Room Display, and TV Display are the same codebase deployed once — property identity comes from the logged-in user's JWT (staff apps) or the per-property `display_token` (guest-facing displays), not from separate deployments.

---

## MQTT Setup

- Broker: `mqtt.d-zahill.kdai.cloud` port `1883` (Zahill's own broker — each property should get its own broker/credentials; do not share across properties, to avoid cross-property command collisions)
- WebSockets: port `9001`
- Auth: username `zahill`, password configured per `server/.env`
- DNS: `mqtt.d-zahill.kdai.cloud` A record → VM IP
- **Connection is app-wide, not per-property** (`server/mqtt/index.js` — one module-level client for the whole Node process). Only opened at startup if at least one **active** property has `room_controller` **enabled** (`server/index.js`, checked against `property_modules`/`properties` before calling `mqttClient.connect()`) — otherwise it's skipped entirely rather than endlessly retrying a broker nothing needs. `connect()` is idempotent; `PATCH /api/admin/properties/:id/modules` also calls it when `room_controller` is turned on, so enabling the module takes effect immediately without a server restart. (There's currently no matching "disconnect if the last property turns it off" — harmless, since an idle connected client produces no reconnect noise.)

### Topic Structure

**Subscribe (backend → ESP32):**
- `zahill/room/{id}/relay/{n}/set` — payload: `on` or `off`
- `zahill/room/{id}/rgb/set` — payload: `{"r":255,"g":0,"b":0}`
- `zahill/room/{id}/ir/send` — payload: slot index
- `zahill/room/{id}/request/status` — triggers full status publish

**Publish (ESP32 → backend):**
- `zahill/room/{id}/relay/{n}/state` — `on` or `off` (retained)
- `zahill/room/{id}/rgb/state` — JSON (retained)
- `zahill/room/{id}/status` — full JSON snapshot
- `zahill/room/{id}/connected` — `true`/`false` LWT (retained)

<!-- TODO: topic namespace is currently "zahill/..." hardcoded — when a second property with real Room Controller hardware comes online, this needs a per-property namespace (e.g. "{property_slug}/room/{id}/...") so MQTT topics don't collide across properties on a shared broker. -->

---

## Backend — Current State

**Stack:** Node.js + Express (CommonJS), PostgreSQL (raw SQL), JWT auth, PM2 + Nginx
**Port:** 4000 behind Nginx
**Entry:** `server/index.js` — also where module gating is wired (see Multi-Tenancy section above)

**Route files** (`server/routes/`):
- `auth` — login/logout (always on)
- `public` — unauthenticated endpoints callable pre-login/pre-JWT (e.g. property branding lookup by slug for the login screen)
- `admin` — superadmin: property CRUD, module toggles, per-property user management (separate `authSuperAdmin` layer)
- `units`, `users`, `settings`, `dashboard`, `display` — core, always on (`display` also carries `POST /room/:roomId/telemetry` — the `room-display-kiosk/` Android APK's device-health push, `authDisplay`, no moduleGuard, upserts `room_display_devices`)
- `bookings`, `checkin`, `allotments`, `pricing`, `ratePlans` — reservations + front desk (`ratePlans` = the "arrangement" per booking: RO/BB/HB/FB + a flat net per-pax-per-night meal price; owner CRUD at `/settings/rate-plans`)
- `guests`, `loyalty` — guest CRM
- `payments`, `reports`, `nightAudit`, `folio`, `agents` — financial (`agents` = Agent Accounts / Direct Billing #13: per-agent statement, AR aging, agent payments + allocations, consolidated invoice PDF; owner-only per handler. `reports` = monthly revenue summary — one endpoint, `GET /api/reports/revenue`, night-based room/F&B revenue + daily chart + by-source breakdown; had no client page until `Reports.jsx` closed that gap, see Frontend section)
- `tasks` — operations kanban
- `products`, `sales`, `tables` — ancillary sales, table management (ride on the `sales` module via `modules.js`)
- `kitchen` — Kitchen Display kiosk app's API. `authDisplay` (per-property `display_token`, same as `display`) on every route, not staff JWT — the Kitchen Display is its own kiosk app (`kitchen-display/`), not a page inside the PMS client. `moduleGuard('sales')` applied per-route instead of via `modules.js`.
- `resto`, `restoGuest` — Resto Ordering (`resto-display/`), own `resto_ordering` module. `resto` is the staff surface (`auth`, staff JWT, plus `authQueryToken` for its SSE stream); `restoGuest` is the public guest QR self-order surface, gated by a new per-table credential (`authTableQR`) — deliberately **not** the shared `display_token` (see the write-up below for why). `moduleGuard('resto_ordering')` applied per-route in both files instead of via `modules.js`.
- `board` — Guest Board CMS (In-Room Media)
- `iot` — Room Controller (MQTT-backed device state, ESP32 relay/RGB/IR — hardware-dependent, off by default)
- `calls` — Calling (two-way room↔desk voice, own `calling` module, no hardware dependency — see the Room Display section below for the full write-up)
- `insights` — Market Insights (competitors, trends, AI briefing)
- `communications` — guest email (owner-only)
- `purchasing` — Back Office Slice A (Suppliers, Raw Materials, Purchase Orders + receiving), own `back_office` module, owner-only per handler. See the Back Office write-up below for the full design (why ingredients get their own `raw_materials` table instead of reusing `products`, the PO status workflow, partial receiving).
- `expenses` — Back Office Slice B (categorized cost log — laundry, electricity, maintenance, etc. — plus CSV export), same `back_office` module, owner-only. See the Back Office write-up below.
- `roomTypes` — Room types (`/api/room-types`, always on, not module-gated like `units`): GET any staff, POST/PUT/DELETE owner. PUT pushes name/base_rate/max_guests to every room via trigger; a rename also updates `yield_settings`, `yield_rate_log` and the `Auto: <name>` pricing periods in one transaction; DELETE is blocked while rooms use the type. `POST/PUT /api/units` take `room_type_id` (or a type name that must already exist) and ignore direct rate/capacity edits. `UnitSettings.jsx` is grouped by room type (rate/capacity edited once per type, collapsible groups, + Add Room inside a group).
- `yield` — Yield/dynamic pricing (`/api/yield/*`: overview, settings/:roomType, run {dry_run}, rates?month=YYYY-MM, log, report/dow, events, holiday-uplift). Per-room-type settings include tier mode (rooms-left vs % occupied), day-of-week factors, floor/ceiling, look-ahead, and `discount_window_days` (default 14, empty = no limit: occupancy-tier *discounts* only apply to nights within N days of today; increases and day-of-week/holiday effects are never limited), own `yield_management` module mounted with `moduleGuard` + `requireRole('owner')`. Logic in `server/services/yieldService.js`, nightly `server/jobs/yieldPricing.js`. UI = extra tabs in `client/src/pages/Pricing.jsx` (components in `pages/pricing/YieldTabs.jsx`), shown only for owner + module on. The `pricing` route itself is still gated by `reservations`.
- `smartReports` — Settings → Reports & Alerts (`/api/smart-reports/*`: catalog, recipients CRUD, `recipients/:id/link`, `recipients/:id/test`, `preview/:report`), mounted with `auth` + `requireRole('owner')` only — **no moduleGuard**, since the instant Telegram alerts are free; paid scheduled reports are refused per report unless `smart_reports` is on. Logic in `server/services/smartReports.js` + `telegramLink.js`, cron in `server/jobs/index.js`. (Replaces the old `/api/settings/telegram-chats` endpoints, removed.)
- `channelManager` — Channex channel-manager spike/validation, own `channel_manager` module (default off, only enabled on Zahill for testing), owner-only, all endpoints under `/test/*` to signal spike-only scope — not production automation. See `ROADMAP.md`'s "Channel Manager Integration + Dynamic Pricing" for the full plan and what's actually built vs. not.
- `posIntegration` — External POS API (`/api/pos/*`: `GET /rooms`, `GET /rooms/:room`, `GET /branding`, `GET /breakfast`, `POST /transactions`), own `pos_integration` module. Auth is `authPos` (Bearer = `properties.pos_api_key`), not staff JWT; `moduleGuard` per-route. Logic in `server/services/posIntegrationService.js`. See the POS Integration write-up.

**IoT database tables (migration 006):**
- `units.controller_id` — links a PMS unit to its ESP32 Room ID (e.g. `"1"`)
- `unit_relays` — relay labels and current state per unit
- `room_controller_status` — online/offline, IP, RGB state per controller

**MQTT client:** `server/mqtt/index.js` — connects to Mosquitto, subscribes to all room topics, updates DB on incoming messages.

**Migrations** (`server/db/migrations/`, run via `npm run migrate` → `server/db/migrate.js`):

| # | What |
|---|---|
| 001 | Initial schema |
| 002 | Pricing periods |
| 003–004 | Deposit amount / paid status |
| 005 | Dynamic sources & methods |
| 006 | IoT devices |
| 007 | Relay config |
| 008–009 | Roles |
| 010 | Guest board |
| 011–012 | Night audit |
| 013 | Booking discount |
| 014 | Market insights |
| 015 | Property location |
| 016 | Competitor extras |
| 017 | AI market summary |
| 018 | Calls |
| **019–021** | **Core multi-tenancy** — property_id everywhere, route scoping, per-property display_token |
| **022** | **Property module system** |
| **023–026** | **Superadmin + onboarding** — auth, admin routes, seedPropertyDefaults, singleton-table fixes, column widening |
| 027 | Fix namespaced role/source/method IDs |
| 028 | Folio charges |
| 029 | Tax config |
| 030 | Guest communication (email templates) |
| 031 | SMTP config (per-property) |
| 032 | Property branding (logo_url, brand_color on property_settings) |
| 033 | Group Bookings — `reservation_groups`, `bookings.reservation_group_id`, `group_booking_confirmed` email trigger |
| 034 | Kitchen Display System — `sales.order_type`/`table_number`/`kitchen_status` |
| 035 | Table Management — `restaurant_tables`, `sales.table_id` |
| 036 | Stock Tracking — `products.track_stock`/`stock_quantity`/`low_stock_threshold`, `stock_movements` |
| 037 | Concierge / Activities — `activities`, `activity_bookings`, new `activities` module |
| 038 | Guest Board ↔ Activities linking — nullable `guest_board_cards.activity_id` FK |
| 039 | Two-way calls — `calls.direction` (`room_to_staff`/`staff_to_room`), `calls.initiated_by` |
| 040 | Split `calling` module out of `room_controller` — seeds `calling=true` for every property |
| 041 | Agent Accounts / Direct Billing (Phase D #13, Slice A) — `booking_sources` generalized with `source_type` (walkin/direct/booking_engine/ota/travel_agent/company/wholesaler), `payment_status` (normal/city_ledger/city_ledger_payment/commission/commission_and_city_ledger), and agent billing fields (contact, NPWP `tax_id`, `billing_address`, `credit_terms_days`, `credit_limit`, `commission_type`/`commission_value`). Additive only — `bookings.source` unchanged, `is_ota` kept. |
| 042 | Agent Accounts / Direct Billing (Phase D #13, Slice B) — nullable `bookings.folio_status` (`pending_agent_invoice`), new `agent_commissions` ledger (`booking_id` unique). City-ledger checkout + commission posting via `server/services/agentBillingService.js`; `loadFolio` extracted to `server/services/folioService.js`. Additive only. |
| 043 | Agent Accounts / Direct Billing (Phase D #13, Slice C) — `agent_invoices`, `agent_payments`, `agent_payment_allocations` tables + `bookings.agent_invoice_id`. `folio_status` now also uses `'invoiced'`/`'paid'`. Statement/aging/payments/consolidated-invoice via `server/services/agentStatementService.js` + `server/routes/agents.js` + `client/src/pages/Agents.jsx`. Additive only. |
| 044 | Rate plans ("arrangements") + bed config + room/F&B net revenue split. New `rate_plans` (RO/BB/HB/FB per property, flat `meal_price` net per pax/night, one `is_default`). `units.bed_config`, `bookings.bed_preference` (double/twin/twin_or_double/other). `bookings.rate_plan_id` + `room_revenue`/`fnb_revenue` (NET, post-discount; `computeFolioTotals(room_revenue+fnb_revenue).total ≈ total_amount−discount`). `folio_charges.service_date` + partial unique index `uq_folio_charges_night`. `night_audit_runs.fnb_revenue`. Backfill: `room_revenue = (total_amount−discount)/F`. **Reports/dashboard/night-audit/loyalty room-revenue now NET** (visible drop vs the old gross `total_amount` sum). **Slice 2 (per-night auto folio posting, `server/services/roomChargeService.js`) also ships on this migration** — night audit posts one `room` + one `fnb` `folio_charges` row per in-house booking per night (idempotent); checkout catches up / voids early-departure nights; `PUT /:id/dates` + rate-plan change repost; cancel/no-show void. Fixes the pre-existing gap where a normal stay's folio never contained the room charge. |

| 045 | Room ID (`units.controller_id`) — scope + default. The `UNIQUE (controller_id)` index was **global**, not per-property (two properties couldn't both have a room "101") — now `UNIQUE (controller_id, property_id) WHERE NOT NULL`. Widened `varchar(10)→(32)`. Backfilled `controller_id = name` for every unit that had none (per property; skips names >32 chars or that collide with an existing custom id). `POST /api/units` now defaults `controller_id` to the room name (overridable; explicit `''` opts out) — `routes/units.js` + `routes/iot.js` slice bumped to 32, 23505 messages say "Room ID". Fixes the friction where a display-only property had to hand-type a Room ID for every room. |
| 046 | Guest Messages — new `guest_messages` table (front desk → room tablet, manual send only; see the Room Display section above for the full write-up). `unit_id`, `booking_id` nullable, `body`, `sent_by` nullable, `read_at`. Additive only, kept separate from the email/SMTP Guest Communication tables. |
| 047 | Guest ID/passport number — `guests.id_number`, for the daily guest report Bali properties must submit to the local police (STPM/lapor tamu). |
| 048 | Resto Ordering — new `resto_ordering` module (default off, backfilled to every property). `restaurant_tables.qr_token` (per-table QR credential, resolves via a new `authTableQR` middleware — deliberately not the shared `display_token`). New `table_sessions` table (one dining visit; partial unique index enforces one open session per table) + `sales.table_session_id`. `sales.confirmation_status`/`confirmed_by`/`confirmed_at`/`rejection_reason` — a pre-kitchen "pending" bucket for Room Display's room-service orders only (QR self-order and staff-entered orders still fire straight to the kitchen). `sales.order_source`. Also drops `sales.payment_method`'s hardcoded `cash`/`qris`/`room_charge` CHECK and widens it to `VARCHAR(50)` — validation moved into `salesService.createSale` against the property's real `payment_methods` table (see the Resto Ordering write-up below). |
| 049 | Sales post to the guest folio — `folio_charges.sale_id` (nullable FK to `sales`). Closes a real gap that predates Resto Ordering: `folio_charges.type='sale'` has been a valid value since migration 028, but `salesService.createSale` never actually wrote one, so ANY room-charge sale (staff POS "charge to room", Room Display's guest self-order, and now the resto app) showed up in the sales list/reports but never on the guest's own Folio tab. `createSale` now posts a `type='sale'` folio charge whenever `payment_method='room_charge'`; `routes/resto.js`'s reject handler voids it via `sale_id` if a room-service order is declined after the fact. |
| 050 | Resto Ordering — open tabs (order now, settle at table close). `sales.settled_at`/`settled_by`. New `'unpaid'` sentinel value for `sales.payment_method` (like `'room_charge'`, not a real `payment_methods` row): a placed-but-not-paid order. Guest QR orders and staff-entered dine-in orders both land `'unpaid'` and fire to the kitchen as before; `restoSettleService.settleAndClose` flips every unpaid non-rejected sale in a table session to one real method in a single txn, posting folio charges when it's `room_charge`. `POST /api/resto/tables/:id/settle` + `GET /api/resto/rooms` (in-house list for the room-charge picker); `/tables/:id/close` now 409s if the session still has unpaid orders. Partial index `idx_sales_unpaid`. Additive only. |
| 051 | Housekeeping room-condition status — `units.housekeeping_status` (`clean`/`dirty`/`inspected`, default `clean`; `inspected` reserved, UI only uses clean/dirty) + `units.housekeeping_updated_at`. A **second dimension orthogonal to `units.status`** (which stays purely occupancy/availability). Checkout now also sets `housekeeping_status='dirty'` (`routes/checkin.js`) alongside the existing auto housekeeping task. Housekeeping clears it from the room tablet's IdleScreen ("Mark Room Clean", two-step confirm) via a new `type:'mark_clean'`/`'mark_dirty'` on `POST /api/display/room/:roomId/housekeeping` (`opsGate`), or front desk from the Dashboard tile via new `PATCH /api/units/:id/housekeeping` (plain `auth`, **not** owner-gated like `PUT /:id`). Both close any open `type='housekeeping'` task for the unit and `sse.notify` the room channel. `GET /display/room/:roomId/state` returns `unit.housekeeping_status`; `dashboard/summary` + `bookings/today/arrivals` carry it too. Deploy-time backfill: units with an open housekeeping task → `dirty`. v1 is checkout-cleaning only (no stayover/daily-service tracking); dirty check-in warns but doesn't block. Additive only, no new module (gated by existing `operations`). |

| 052 | Room Display Devices — new `room_display_devices` table (tablet telemetry from the `room-display-kiosk/` Android APK). `property_id`-scoped, keyed `(property_id, controller_id)`, ~21 nullable columns (battery/power/network/wifi/storage/uptime/versions) + `last_seen_at`. Written only by `POST /api/display/room/:roomId/telemetry` (`authDisplay`, no moduleGuard). LEFT JOINed into `dashboard/summary`'s occupancy query as `tablet_*` columns. Not a gated module, no backfill. Additive only. |
| 053 | Back Office Slice A — new `back_office` module (default off), `suppliers`, `raw_materials` + `raw_material_movements` (a deliberately separate ledger from the sellable-`products` world — see the Back Office write-up below), `purchase_orders` + `purchase_order_items` (each line restocks either a `product` or a `raw_material`, never both), `stock_movements.purchase_order_id`. Additive only. |
| 054 | Purchase Order activity log — new `purchase_order_events` (append-only timeline: created / submitted / approved / received, including partial-receipt detail), written by `purchasingService.js` at each transition, surfaced on the PO detail modal. Additive only. |
| 055 | Back Office Slice B (Expenses) — new `expenses` table: fixed-`CHECK` `category`, `payment_method` (validated in-route against `payment_methods`, not FK'd — same convention as `sales.payment_method`), optional `supplier_id`, void-not-delete (`is_voided`/`voided_by`/`voided_at`, mirrors `folio_charges`). No new module — rides `back_office`. Additive only. |
| 056 | Products cost tracking (Inventory Value, part 1) — `products.cost_per_unit` (weighted-average, updated on PO receipt only, mirrors `raw_materials.cost_per_unit`). Additive only. |
| 057 | Telegram notifications — new `telegram_chats` table (property_id, chat_id, label, is_active). No new module. Additive only. |
| 058 | `telegram_chats` uniqueness — `UNIQUE (property_id, chat_id)`, caught live while testing (the same chat could be linked twice, e.g. once via the UI and once via a separate API call, causing a genuinely duplicated alert). `POST /api/settings/telegram-chats` now returns a clean 409 on a duplicate instead of a raw 500. Additive only. |
| 059 | Room "Out of Order" status — `units.status`'s `'maintenance'`/`'blocked'` pair (undocumented difference between the two, never had a reason/date, and `'maintenance'` already meant something else — a `tasks.type` and an `expenses.category`) replaced with a single, industry-standard `'out_of_order'` status. New `units.status_reason`/`status_expected_back`/`status_updated_at`/`status_updated_by`. Existing `maintenance`/`blocked` rows (none on any known property) backfilled to `out_of_order`. |
| 062 | Room types as a real entity — new `room_types` (name, base_rate, max_guests, description, sort_order; unique per property case-insensitively) + `units.room_type_id`. **`units.type`/`base_rate`/`max_guests` are kept as trigger-maintained read-only mirrors** (`units_sync_from_room_type` on units, `room_types_sync_units` on room_types), so every existing reader is unchanged and the mirror cannot drift; a legacy insert that sets only `units.type` (seed scripts) find-or-creates the room type. Backfilled from distinct existing types. **The migration has a safeguard: it STOPS (rolls back, changes nothing) if a property that has bookings has any room type whose rooms disagree on base rate or max guests, or type names differing only by case** — because from now on a room silently takes its type's rate on its next update. A property with no bookings (e.g. the demo rooms 001 seeds on a fresh install) only gets a NOTICE and the most common value wins (`migrate.js` now prints notices). Run the pre-check SQL in the deploy notes / fix the rooms, then re-run. Rooms can no longer carry their own rate: edit it on the room type. Channex mapping is still per unit (moves to per room type in the production channel-manager step). |
| 061 | Yield / dynamic pricing v1 — new `yield_management` module (default off, paid), `pricing_periods.source` (`manual`/`auto`), per-room-type `yield_settings`, `yield_holiday_uplifts`, `yield_events`, append-only `yield_rate_log`; `holidays` gains `is_joint_leave`/`source` + seeded with official SKB 3 Menteri 2026–2027 national holidays & cuti bersama. Engine writes `source='auto'` multiplier periods (`sort_order -1000`) so any manual period wins and no read path changed. See `ROADMAP.md` step 4. Additive only. |
| 060 | Channel Manager (Channex) — spike/validation, not production. New `channel_manager` module (default off), `properties.channex_property_id`, vendor-agnostic `channel_manager_mappings` table (`provider`/`external_room_type_id`/`external_rate_plan_id` — generic naming on purpose so a future Beds24/STAAH adapter doesn't need a schema change). Validated end-to-end against Channex's real staging sandbox — see `ROADMAP.md`'s "Channel Manager Integration + Dynamic Pricing" for the full write-up, including two real API gotchas found live (room-type occupancy fields, and rates needing to be sent as a decimal string or landing 100x too low). Additive only. |
| 063 | Guest Registration Card — `guests.address`, `bookings.purpose_of_stay` (both nullable/optional, filled in at print time rather than at guest/booking creation), `property_settings.registration_notice` (per-property house-rules text, defaulted to Zahill's current wording for every property). See the Registration Card write-up below. Additive only. |
| 064 | `activity_bookings.payment_method` drops its hardcoded `cash`/`qris`/`room_charge` CHECK and widens to `VARCHAR(50)` — same fix migration 048 already made for `sales.payment_method`, so a property's real, configurable payment methods (e.g. Bank Transfer, Wise) can be selected for an activity booking too, not just the three original literals. See the Activities write-up below. Additive/widening only. |
| 065 | `booking_sources.publish_rate` — new per-source toggle (default `true`) controlling whether the Registration Card prints real Room Rate/Deposit numbers or a neutral "Arranged by \<source\>" line. Backfills existing OTA and `travel_agent` sources to `false`. See the Registration Card write-up below. Additive only. |
| 066 | `booking_events` — append-only audit log for `PUT /api/bookings/:id` changes (same shape as `purchase_order_events`, migration 054, generalized to bookings). See the Edit Details write-up below. Additive only. |
| 067 | Sales → hotel extras till. `products.category` CHECK gains `room_addon`/`transport`/`laundry`/`service`; `drinks`/`food` kept (resto app, Room Display Dining, Kitchen Display, old sales); `tour` retired (existing rows relabelled `service` — tours are Activities). `sales.service_charge_amount`/`tax_amount` (nullable — tax charged at sale time on directly-paid extras). `payments.type` gains `'incidental'` + `payments.sale_id` (an extra paid at the desk, posted to the folio alongside its charge). No rows deleted. See "Ancillary Sales" in the Frontend section. || 068 | Reports & Alerts — new `smart_reports` module (default off, paid) + `notification_recipients` (property_id, name, channel `email`/`telegram`, address — NULL for a Telegram recipient until they connect, role, `reports TEXT[]`, is_active, last_sent_at/last_error, `link_token`/`link_expires_at`/`telegram_name` for the Connect link; unique per property+channel+address). Adds `property_settings.telegram_bot_token` (the property's own bot; NULL = platform bot; unique across properties). Copies every existing `telegram_chats` row onto it with both instant alerts ticked, so linked chats keep getting what they got; `telegram_chats` is kept but no longer read. Additive only. |

| 069 | Market Insights per property — `property_settings.market_area` / `market_keywords TEXT[]` / `market_description` (Zahill backfilled with its previous hardcoded values: "Kintamani, Bali", `kintamani glamping` + `bali glamping`, "glamping resort"). Additive only. |
| 070 | External POS integration — new `pos_integration` module (default off, backfilled), `properties.pos_api_key` (unique, nullable, owner can regenerate), `sales.order_source` CHECK gains `'external_pos'`, `sales.external_ref` (+ partial unique index per property — retry-safe) and `sales.description` (the POS's own text; POS sales have no `sale_items`). Additive only. |

| 071 | Birthday offer — `property_settings.birthday_offer` (optional line added to the birthday WhatsApp sent from Guests; set in Email & Communication → WhatsApp Messages, returned by `GET /api/settings/branding`). Zahill backfilled with its previous "complimentary upgrade" wording; every other property starts empty (plain "We'd love to welcome you back soon!"). Additive only. |

| 072 | Complimentary stays — `bookings.complimentary_scope` (`room`/`room_meals`/`all`) + reason / by / approved_by / at / `complimentary_night_value` (net value given per night, for reports) / `complimentary_prev_total`/`_prev_discount`/`_prev_nights` (restored on removal). New `complimentary_requests` + `complimentary_request_codes` (one hashed code per Telegram approver). See the Complimentary stays write-up. Additive only. |
| 073 | Complimentary approval by Telegram button — `complimentary_request_codes.chat_id`/`message_id` (edit the message afterwards; a press only counts from that chat), `complimentary_requests.declined_by_name`/`approved_via` (`code`/`button`), status gains `declined`. Additive only. |

**Next migration number: 074** (keep `ROADMAP.md` in sync when you add one — this line was found stale at 047 when 047 already existed on disk; double-check against the actual highest-numbered file in `server/db/migrations/` if in doubt, don't trust this line blindly).

---

## Frontend — Current State

**Stack:** React 18 + Vite 5 + PWA (vite-plugin-pwa)
**Location:** `client/`

Key pages (`client/src/pages/`): `Dashboard`, `Reservations`, `NewBooking`, `BookingDetail`, `CheckIn`, `QuickCheckIn`, `Guests`, `GuestProfile`, `Loyalty`, `Allotment`, `Pricing`, `Sales`, `Operations`, `NightAudit`, `Agents`, `Reports`, `BackOffice`, `Users`, `Settings` (Booking Sources & Payment Methods only — see below), `SettingsProperty`, `SettingsCommunications`, `SettingsRoles`, `SettingsBoardCards`, `SettingsRoomControllers`, `SettingsRatePlans`, `UnitSettings`, `Activities`, `GroupDetail`, `Login`.

**Back Office — Slice A (`BackOffice.jsx`, `/back-office`) — implemented 2026-09-13, migration 053.** First slice of the module ROADMAP.md scoped back in 2026-08-17 and never started. New `back_office` module (default off, paid add-on tier). One page, one nav entry, internal tabs (Suppliers / Raw Materials / Purchase Orders) — matching `Sales.jsx`'s tab-switching shape rather than separate routes, so future slices (Expenses, Cash & Bank, Recipes) just add more tabs to the same file. **Real design decision made with the owner mid-plan: raw ingredients (flour, rice, chicken breast — never sold directly) get their own `raw_materials` table, not a repurposed `products` row.** Reusing `products` was considered and rejected — it would have risked an ingredient leaking into a guest menu / staff POS grid / Kitchen Display if a filter were ever missed at any of the several places that already query `products`, and would have mixed future ingredient-cost reporting into the general Sales/Reports domain. `raw_materials` has its own `unit_of_measure` (kg/g/l/ml/pcs), NUMERIC (fractional) `stock_quantity` — unlike `products`' INT stock, since ingredients are bought/consumed in kg/g/l/ml — and its own `raw_material_movements` ledger, fully separate from `stock_movements`. `purchase_order_items` restocks *either* an existing sellable `product` (e.g. bulk-buying bottled water for resale) *or* a `raw_material` (e.g. buying rice for the kitchen) per line, enforced by a `CHECK` that exactly one is set. PO status workflow (`draft → pending_approval → approved → received`, or `cancelled` from any pre-received status) is a whitelist transition map in `server/services/purchasingService.js`; receiving (`POST /purchase-orders/:id/receive`) supports partial receipt across multiple calls, clamped so a line can never be over-received, and only flips the PO to `received` once every line is fully received. Verified against the live dev DB end-to-end — service layer *and* real HTTP round-trips against a running server (create → submit → approve → partial receive → full receive; confirmed `moduleGuard('back_office')` actually 403s while off and succeeds once toggled on). **Slice B — Expenses — implemented 2026-09-13, migration 055.** A lightweight, categorized cost log (laundry, electricity, maintenance, etc.) — explicitly **not** a step toward a full accounting system (GL/chart-of-accounts/journal entries stay out of scope, confirmed with the owner). The design goal instead: a real Net Income view inside the PMS, and data clean enough to hand to an outside accountant. Fixed `CHECK`-constrained `category` (free text was rejected — can't be reliably summed for Net Income or mapped to a chart of accounts by a bookkeeper), `payment_method` validated in-route against `payment_methods` (same convention as `sales.payment_method`, not a DB-level FK), optional `supplier_id` link to the Slice A Suppliers directory, **void-not-delete** (mirrors `folio_charges`, not `agent_payments`' hard-delete — an accountant needs an unbroken audit trail). New `server/routes/expenses.js` (`GET /`, `POST /`, `DELETE /:id` = void, `GET /export` = CSV) — no new module, rides `back_office`. **CSV export is new to this codebase** (every prior download was PDF) — also added to Purchase Orders (`GET /purchase-orders/export`) since an accountant needs both operating costs and inventory purchases; client-side download reuses `Agents.jsx`'s PDF-blob-download shape, just `text/csv`. Caught and fixed a real bug building the PO export: `created_at`/`updated_at` are `TIMESTAMPTZ` (real JS `Date` objects from `pg`, unlike `DATE` columns which `db/index.js` normalizes to plain strings) — `String(date).slice(0,10)` silently grabbed garbage from `Date.prototype.toString()` instead of an ISO date; fixed with `.toISOString().slice(0,10)`. `GET /api/reports/revenue` gained `expenses_total`/`net_income`, surfaced as a new "Net Income" card on `Reports.jsx`; safe for properties that don't use Expenses (an empty table just returns 0, degrading Net Income to "= Total Revenue" rather than erroring). New Expenses tab on `BackOffice.jsx` (fourth tab). Verified end-to-end against the live dev DB and real HTTP round-trips. **Inventory Value — implemented 2026-09-13, migration 056.** Scoped as the deliberate first step before any COGS/Net-Income-from-purchases work — raised by the owner directly: *"before [subtracting purchases], should we also have the value of our inventory stated in the app?"* Correct call — buying inventory is an asset swap, not an expense, until it's consumed (sold), and you can't compute "value consumed" without first knowing "value on hand." `products` gains `cost_per_unit` (weighted-average, same treatment `raw_materials.cost_per_unit` already had from Slice A — `purchasingService.js`'s `receivePurchaseOrder` product branch now blends every PO receipt into the running average the same way the raw-material branch already did, verified with the identical 100@5000+50@6000→5333.33 case proven for raw materials). New `GET /api/purchasing/inventory-value` (raw materials + products, each at `stock_quantity × cost_per_unit`, both subtotals + grand total) and a new "📊 Inventory Value" tab on `BackOffice.jsx` (fifth tab). An item with stock but no `cost_per_unit` yet (never received through a PO) is listed but **excluded from the total**, flagged with a visible warning rather than silently valued at 0. Deliberately **no COGS, no Net Income change, no Recipes** in this pass — those stay future work; this only makes "what's on hand worth" visible. Verified against the live dev DB and real HTTP round-trips (including against real data the owner had already entered while testing — a raw material with 210kg on hand blending to a real weighted-average cost). **Not yet built:** Cash & Bank, AP Bills, Recipes/COGS (future slices, full spec in `ROADMAP.md`), any Dashboard/low-stock alerting integration, browser click-through (DB + HTTP + client-build verified instead, per this session's bar).

**Pricing page tabs + Unit Settings (2026-09-20/21).** `Pricing.jsx` is the classic "pricing periods" page for everyone; when the property has `yield_management` AND the user is an owner it gains tabs (Rate calendar / My rules / Automatic / Events / Reports, components in `pages/pricing/YieldTabs.jsx`). Rate calendar has three views — Month calendar (one room type, Monday-first grid), Compare room types (types × days, sticky column), List — remembered per browser. The "Automatic" tab starts with a "sources in use" panel (In use / Waiting for data / Off / Not connected) and has Preview (dry run) + Apply now; a manual pricing period ALWAYS overrides the automatic rate for its dates (whole override, not stacked). The new-period modal picks units by room type (tick a type = all its rooms; empty = all units incl. future rooms). `UnitSettings.jsx` is grouped by room type: collapsed-by-default accordion (one open at a time), summary strip, availability bar, room preview line, room tiles; rate and max guests are edited ONCE per type ("Edit Type"), a room only keeps bed setup/Room ID/status. **Known gaps:** api.co.id national-holiday sync not built (holidays come from the migration-061 SKB 2026–2027 seed; Balinese Galungan/Kuningan/Nyepi only seeded for 2027); AI local-events finder (v1.1) not built; agent room blocks (allotments) are not counted in yield occupancy; out-of-order is current-status only, not date-based; Channex mapping still per unit, not per room type.

**Ancillary Sales (`Sales.jsx`, `/sales`) — reworked 2026-09-23, migration 067.** Builds on (and for `Sales.jsx`, supersedes) the 2026-09-22 interim catalog split below: F&B is now hidden from Sales **always**, not only when `resto_ordering` is on. F&B is leaving the PMS (external POS integration, see `POS_INTEGRATION_PLAN.md`; resto app where a property still uses it), so the Sales page is now a front-desk till for **hotel extras only** — tabs Sell / History / Items. Categories `room_addon` (extra bed, early/late check-out), `transport`, `laundry`, `service`, `merchandise`, `other`. F&B products (`food`/`drinks`) are hidden from this page, not deleted. Removed: the Tables tab and dine-in/takeaway/room-service order type (table management lives in `resto-display/`). Payment picker uses the property's real `payment_methods` (minus `ota_managed`) instead of hardcoded cash/QRIS; "Charge to Room" searches in-house guests by name **or room** and posts to the folio via the unchanged `salesService.createSale`. Quantity is a typed number — **extra bed is a one-off charge, front desk enters nights as the quantity** (per-night auto-posting via night audit deliberately not built). `POST /api/sales` now 400s a non-whole/<1 quantity; `POST/PUT /api/products` 400s an unknown category. `GET /api/sales` returns `items_summary`/`guest_name`/`unit_name` for History. **Stock History** (Items tab → "History" on tracked items) shows the latest 50 `stock_movements` rows via the existing `GET /api/products/:id/stock/movements` (now also joins `purchase_orders.po_number`), with a running "stock after" balance computed client-side backwards from the current count; `POST /api/products` now logs the opening quantity as a `restock` movement noted "Starting stock" so history never starts from an unexplained number. **Pay now vs Charge to room + tax on extras:** item prices are **before tax**, like rooms. Picking an in-house guest shows a Charge to room / Pay now toggle. *Charge to room* posts the net charge to the folio (tax added at checkout, as before). *Paid directly* (Pay now, or a walk-in) — `POST /api/sales` passes `taxDirectPay` to `salesService.createSale`, which computes service charge + tax at the property's rates (`folioService.computeFolioTotals`, same formula as the folio) and stores them on the sale (`sales.service_charge_amount`/`tax_amount`, migration 067; NULL = not taxed at sale time — room charges and all older sales); `sales.total_amount` stays the NET item sum so Reports' ancillary revenue stays net. For Pay now the sale is **also posted to the folio**: the NET `folio_charges` row (folio adds its own tax) **plus** a received `payments` row of the gross with new `type='incidental'` (+ `payments.sale_id`), so the folio is a complete record while its balance is unchanged. `incidental` exists because deposit/balance are the ROOM's payments — `routes/payments.js`'s status recompute and the bookings list look for "the balance payment"; the Registration Card's payment-method lookup (`routes/checkin.js`) is now restricted to deposit/balance for the same reason. The till previews Subtotal / Service / Tax / Total to collect from `GET /api/settings/branding` (now also returns `tax_rate`/`service_charge_rate`). **Folio grouping fix:** `folioService` charge queries return `is_fnb` (the sale contains food/drinks) — invoice PDF + Folio tab now group only food/drink sales under Food & Beverage and front-desk extras under Other (previously every `sale` charge read as F&B). Invoice "Payments Received" labels `incidental` as "extras" and formats `received_at` properly (was `String(Date).slice` → "Thu Sep 24"). **Pay-now extras are never agent-billable** (review fix, same day): `folioService.PAID_AT_DESK_SQL` (a folio charge whose sale's `payment_method` isn't `room_charge`/`unpaid`) is excluded from the agent statement, consolidated agent invoice, the invoiced→paid check and — via `loadFolio`'s new `agent_billable_total` — the commission base; charge-to-room extras stay billable to the agent. The group rollup's `paid_amount` counts only deposit/balance payments. `salesService.createSale` now always prices items from the `products` row (the staff till used to forward the browser's `unit_price`). **Known gap:** voiding the folio charge of a Pay-now sale doesn't reverse its incidental payment (leaves a credit on the folio); there's no sale void/refund flow yet. **Sales receipt:** `GET /api/sales/:id/receipt` (pdfkit, `services/saleReceiptPdf.js`, shared `pdfHeader.drawDocumentHeader`, same layout family as the Activities receipt) for directly-paid sales only — 400 for `room_charge` (it's on the room Invoice) and `unpaid`; no tax/service lines since a direct sale's total is the item sum. Opened from the post-sale success panel ("🖨 Print receipt") or a History row, in a new tab for printing (falls back to download if the popup is blocked). Items tab edit controls only show for owner or `resto_menu` (same rule as the server's `requireOwnerOrMenu`). **Guest-facing menus are F&B-only server-side** (`salesService.FNB_CATEGORIES`, used by `display.js` menu/order, `resto.js` menu/order/config, `restoGuest.js` menu/order) so an extra bed can never appear in Room Display's Dining tab or a table QR menu, and Room Display's `orderingEnabled` is now `sales` on **and** at least one available F&B product. Tours/yoga/trekking stay in Activities (bookable from the tablet). **Known gap:** a property without `resto_ordering` has no UI left to manage F&B products — intended, since F&B is moving to the external POS; Zahill's existing F&B items still feed Room Display Dining until they're marked unavailable or the POS integration replaces it.

**POS Integration — PMS side built 2026-09-25, migration 070.** The property's own POS (Separuh, repo `zahillpos`, branch `pms-integration`) replaces the PMS's F&B stack and only tells the PMS about bills a guest charges to their room. New `pos_integration` module (default off). The owner makes the key in Settings → Property Details → **POS Integration** card (Create key / Copy / Make a new key — `GET /api/settings/pos-api-key`, `POST /api/settings/pos-api-key/regenerate`, owner-only; regenerating breaks the POS until the new key is pasted in). The POS backend calls `/api/pos/*` with `Authorization: Bearer <pos_api_key>` (`middleware/authPos.js`): `GET /rooms` (every checked-in stay: booking_id, room, room_id, guest_name, dates, pax), `GET /rooms/:room` (match by room name **or** Room ID, any case; 404 if nobody is checked in), `GET /branding` (name, address, phone, email, `brand_color`, logo as inline base64 — the POS copies them into its own Setup → Branding, added 2026-09-26), `GET /breakfast?date=` (today WITA by default: the Kitchen tab's breakfast rows — `bookings.loadKitchen`, now exported — as booking_id / room / guest / pax / rate plan / checking_out / special requests, plus totals; the POS waiter tablet's Breakfast list, added 2026-09-26), `POST /transactions` `{ room | booking_id, amount, description, external_ref }` → 201 `{ sale_id, folio_charge_id, booking_id, amount, balance_due, replayed:false }`. **`amount` is NET** — after the POS discount, before service/tax — because the folio adds the property's service charge + tax on every charge (a gross amount would be taxed twice). `external_ref` = the POS transaction id: a retry (or concurrent retries — the unique index loser re-reads) returns the original sale with 200 + `replayed:true`, never a second charge. `balance_due` is the whole-stay estimate (`computeProforma`, same as the Balance Due page), not the posted ledger. Each charge = one `sales` row (`payment_method='room_charge'`, `order_source='external_pos'`, `description`, no `sale_items`, `kitchen_status` NULL so it never reaches Kitchen Display) + one `folio_charges` `type='sale'` row. Folio/invoice group it under **Food & Beverage** (`is_fnb` also true for `external_pos`); Sales History shows its description (`items_summary` falls back to `sales.description`); Reports count it in ancillary revenue like any sale; agent-billable like other room charges. **Not built:** void/refund of a POS charge from either side (void the folio charge by hand for now — the `sales` row stays), multiple keys per property (one key, like `display_token`), **POS side built the same day** on `zahillpos` branch `pms-integration` (not yet deployed): Settings → Hotel PMS, Charge to Room at the till with a room picker, net amount + POS transaction id as `external_ref`, posted inside the POS DB transaction so a PMS failure saves nothing — see that repo's README. Verified via HTTP against the dev DB (403 module off, 401 bad/rotated key, lookup, charge, replay, 3 concurrent retries → one sale, folio F&B grouping, Sales list), test rows removed; Settings card not clicked through in a browser.

**Reports (`Reports.jsx`, `/reports`) — closed 2026-09-13.** `server/routes/reports.js`'s `GET /api/reports/revenue` had existed since Phase A with real data behind it, but no client page or nav entry was ever built on it — literally unreachable. Building the page surfaced two real backend bugs, both fixed in the same pass: (1) the monthly KPI query attributed a booking's *entire* revenue to its check-in month while the daily chart spread it across actual nights stayed, so the two disagreed for any booking crossing a month boundary — fixed with a shared `NIGHTS_CTE` (one row per booking-night, clipped to the requested month) that every query in the file now builds on, so everything is night-based and self-consistent; (2) the by-source breakdown grouped on the raw `bookings.source` slug (e.g. `booking_com`) instead of joining `booking_sources` for its `label` (`Booking.com`) the way `Agents.jsx` already does — fixed with a `LEFT JOIN`. Also: the chart's day-vs-day comparison was originally against a made-up "revenue ÷ calendar days" figure — checked against GuestPro (the Indonesian PMS this codebase is scoped against) and general industry sources, confirmed ADR (`revenue ÷ room-nights sold`) is the real standard, so the tooltip now compares each day's own implied rate against ADR instead. Gated `RequireOwner` + `RequireModule moduleName="financial"`, nav entry in both `Sidebar.jsx` and `App.jsx`'s mobile drawer (Revenue & Billing group) — not added to `SettingsRoles.jsx` `MENU_DEFS`, same as Rate Plans/Night Audit/Agent Billing. Verified against the live dev DB, not just code review. Not yet clicked through in a browser. **Gained a "Net Income" card 2026-09-13** (Back Office Slice B) — `expenses_total`/`net_income` fields on the same endpoint, see the Back Office write-up below. **Gained a "Export CSV" button the same day** — `GET /api/reports/revenue/export`, a two-block CSV (a Metric/Value summary — Room/F&B/Ancillary/Total Revenue, Expenses, Net Income, Bookings, Room Nights — then the Revenue by Source breakdown) for the same month shown on-screen. Refactored the query logic out of the route handler into a shared `getMonthlyReport(propertyId, month, year)` so the JSON endpoint and the CSV export can never drift apart. Completes the "hand it to an accountant" trio started by Back Office's Expenses/Purchase Orders exports — Revenue, Expenses, and Purchases are now all independently downloadable as CSV, each from its own page's Export button. **Period filter — 2026-09-24:** `GET /api/reports/revenue` and `/revenue/export` take `?from=&to=` (inclusive YYYY-MM-DD, max 366 days; the old `?month=&year=` still works and resolves to the whole month — verified identical output). `getMonthlyReport` → `getReport(propertyId, from, to)`; room/F&B stay night-based (a night counts on the date it starts), sales are bucketed by WITA day (`created_at AT TIME ZONE 'Asia/Makassar'`), expenses by `incurred_on`. `Reports.jsx` has a period picker — Today / Yesterday / This week (Mon–Sun) / Last 7 days / This month (default) / Last month / Pick a month / Custom range — with the daily chart hidden for a single day. The CSV export follows the selected period and, for multi-day periods, adds a third **Daily breakdown** block (Date, Room/F&B/Ancillary/Total Revenue, Expenses, Net Income, Room Nights) whose rows sum exactly to the summary block. Edit Price corrections flow straight into these figures (they read `bookings.room_revenue`/`fnb_revenue`).

**`/settings` used to be one page mixing four unrelated concerns** (property name/address/tax config, SMTP + email templates + comm log, booking sources, payment methods) under a misleading "Sources & Methods" label. Split into three owner-only routes/pages: `/settings` (`Settings.jsx`, trimmed to just Booking Sources + Payment Methods, label now matches contents), `/settings/property` (`SettingsProperty.jsx`), `/settings/communications` (`SettingsCommunications.jsx`). All three share the same `GET/PATCH /api/settings/property` endpoint — the PATCH is `COALESCE`-based per-field server-side, so two pages independently patching different field subsets don't clobber each other (verified). All three are wrapped in `RequireOwner` at the route level now, not the old `RequireMenu menuKey="settings"` — the page component already hard-blocked non-owners regardless of `allowed_menus`, so this just moves the same real restriction to the route boundary (consistent with the `RequireOwner` pattern below).

Superadmin pages (`client/src/pages/admin/`): `AdminLayout`, `Properties`, `PropertyDetail`.

**Navigation** — `client/src/components/Sidebar.jsx` is the desktop nav (persistent left sidebar, replaced the old `TopNav` top bar + flyout-dropdown pattern in `App.jsx`). Mobile keeps the existing `BottomNav` (unchanged layout, in `App.jsx`) below the 768px breakpoint — same `.sidebar { display:none }` / `.bottom-nav { display:flex }` media-query swap mechanism as before, just swapping which desktop element hides. Superadmin's `AdminLayout.jsx` intentionally still uses the old top-bar pattern (`.nav-bar`/`.nav-logo`/`.nav-end` classes, kept in `index.css` for it) — it's a 2-page, non-module-driven shell, not part of this. `client/src/styles/global.css` is dead/unimported CSS (only `index.css` is actually loaded, via `main.jsx`) with overlapping class names — don't edit it, don't trust it when grepping for current styles.

Sidebar layout: `.sidebar-nav` (flex:1, `min-height:0`, its own `overflow-y:auto`) holds the daily-use items and is the one region that scrolls; `.sidebar-settings` sits below it as a separate, visually distinct, non-flex block (divider + dark tint) pinned above the footer — this mirrors Cloudbeds/Mews keeping config structurally apart from daily nav, not mixed into one list. **If `.sidebar`'s children ever look wrong again (one region eating another's space), check for competing `overflow`/`max-height` rules between `.sidebar` and its children first** — that exact bug happened once already.

**Nav grouping (regrouped 2026-09-09 after an audit — the flat 11-item daily list, three-way inconsistency between sidebar / bottom-nav / roles editor, and 4 items unreachable on mobile were all fixed in one pass):**
- **Daily nav** (`.sidebar-nav`): `📊 Dashboard` stands alone at the top, then non-collapsible labelled groups via the `NavGroup` helper — **Front Desk** (Reservations · Quick Check-in · Check-in/out · Guests · Loyalty · Sales), **Guest Experience** (Activities · Guest Board), **Operations** (Operations · Night Audit — yes, Night Audit is *operations*, it's a daily close ritual, not accounting) — then **Revenue & Billing** as a collapsible `SidebarSection` defaultOpen (Pricing · Channels · Rate Plans · Agent Billing). "Agent AR" was renamed **Agent Billing**. Scrollbars in `.sidebar-nav`/`.sidebar-settings` (and app-wide) are styled thin + track-less + faint (`index.css`, `*` scrollbar rules + webkit pseudo-elements).
- **Settings** (`.sidebar-settings`, pinned, collapsed): **Property** `SidebarSection` (Property Details · Units · Sources & Methods · Email & Communication · Room Controllers) and **Access** `SidebarSection` (Users · Roles & Permissions).
- **`📞 Call a Room`** is no longer a loose button in the nav list — it's a floating action button (`components/CallRoomFab.jsx`, `.call-fab`), rendered in `App.jsx`'s `Layout`, `hasModule('calling')`-gated, hidden during an incoming/active call.
- **Mobile `BottomNav`** (`App.jsx`): the "More" drawer now renders `moreGroups` — the same group labels as the sidebar — and includes the four items that were previously **missing entirely on mobile** (Night Audit, Agent Billing, Room Controllers, Guest Board). Bottom bar stays Dashboard · Reservations · Quick CI · Guests.
- **`SettingsRoles.jsx` `MENU_DEFS`** groups realigned to match (Front Desk / Guest Experience / Operations / Revenue / Settings / Resto). New **`guest_board`** menu key — Guest Board is no longer hard `RequireOwner`; staff can be granted it (server `routes/board.js` was already only `auth`-gated). The Free/Paid toggle inside `SettingsBoardCards.jsx` stays owner-only (creating/unlinking a priced activity is an owner call). `AuthContext.jsx` `ORDERED_PATHS` gained `guest_board`/`pricing`/`units`/`users` so a single-menu staffer doesn't hit a redirect loop.
- Icons de-duped: Operations `🔧→🧰`, Room Controllers `⚡→🎛️` (Quick Check-in keeps `⚡`, Sources & Methods now `💳`).

**Route guards** — every module-gated client route is wrapped in `RequireModule` (in `App.jsx`; accepts a single module name or an array when a page depends on more than one, e.g. `/checkin` needs both `reservations` and `front_desk`), and every owner-only route in `RequireOwner`. Both the sidebar/bottom-nav *and* the routes read from the same `hasModule()`/`can()` in `AuthContext.jsx`, so toggling a module off in superadmin now both hides the link and blocks direct URL access — before this pass, only the nav-hiding half existed for most routes (worth checking `App.jsx`'s route list, not just the nav component, whenever a new module-gated page is added).

**Reservations calendar** (`Reservations.jsx`) shows each available night's effective rate (base rate, overridden by the highest-priority active pricing period) directly in the calendar cells, via `GET /api/pricing/calendar?month=&year=`.

**Reservations List view sort — fixed 2026-09-22.** Raised directly: the List view rendered `GET /api/bookings`'s results in the API's raw order (`ORDER BY check_in_date` ascending), so day 1 of whichever month was selected always sat at the top regardless of whether that date was already weeks in the past — burying the arrivals staff actually needed to see under old stays, and out of step with the Timeline/calendar view right next to it (which already treats "today" as the reference point, auto-scrolling to it instead of starting at day 1). Fixed client-side only (the shared `GET /api/bookings` endpoint's own order is unchanged, since `Dashboard.jsx`/`Sales.jsx` also consume it): the table now renders in two buckets — today-and-future first (soonest check-in first), then past stays (most recent first) below a small "Past" divider row. Applies regardless of whether the list is scoped to a calendar month or a search/date-range filter.

**`GET /api/pricing/suggest` per-night pricing fix + nightly/receipt breakdown — implemented 2026-09-18.** Real bug found while adding a per-night breakdown to `NewBooking.jsx`'s price suggestion: `/suggest` resolved the pricing period from the **check-in date only** and applied that one flat rate to every night of the stay — so a stay crossing into a different active period (e.g. a weekday check-in running into a weekend) silently mispriced the crossed-into nights, even though `/api/pricing/calendar` right above it already does correct per-night period resolution for the Reservations calendar. `/suggest` now resolves the rate **per night** the same way, and returns a `night_breakdown: [{ date, room_rate, period }]` array plus a `varies_by_night` flag; `rate_per_night`/`period` (singular) are now `null` when nights disagree, since callers should read `night_breakdown` instead. Also fixed: the response computed `subtotal`/`tax_amount`/`service_charge_amount` internally but only ever returned `grand_total`, so the client had no way to show the tax breakdown at all — now all of it is returned. `NewBooking.jsx`'s price suggestion is now a receipt-style breakdown (Room → meal plan if any → Subtotal → Service Charge → Tax → bold Grand Total, matching the same visual language as the Folio/Invoice screens) instead of one cramped line, plus a "Nightly breakdown" `<details>` disclosure — collapsed by default, **auto-expanded** whenever `varies_by_night` is true so front desk can't miss a real rate difference across nights.

**Amend stay dates / manual no-show** — two front-desk gaps closed after a reservation-flow audit against Cloudbeds/Mews-style PMS behavior. `BookingDetail.jsx`'s "Amend Dates" action (`PUT /api/bookings/:id/dates`) lets staff change check-in/check-out on an existing booking without going through Transfer Room — re-checks conflicts on the same unit, **prices the change** (since 2026-09-24 — see "Amend Dates pricing" below), and is blocked for group-member bookings (dates are shared across a group's rooms; amending one room's dates independently isn't supported yet). "Mark No-Show" (`PUT /api/bookings/:id/no-show`, also surfaced as a quick action on `CheckIn.jsx`'s arrivals list) lets staff flag a no-show manually instead of waiting for the nightly `runNightAudit` job, which still auto-flags any `confirmed` booking whose check-in date has passed. Neither endpoint sends a guest-facing email (the `email_templates` `TRIGGERS` list wasn't extended) or touches payments/refunds — both remain open gaps.

**Edit Details — implemented 2026-09-22.** Raised directly: staff mis-selected the source when creating a booking and had no way to correct it. `routes/bookings.js`'s `PUT /api/bookings/:id` already accepted `source` (and `num_guests`/`special_requests`/`internal_notes`/`bed_preference`/`purpose_of_stay`) in its COALESCE update with zero UI ever exposing most of them — real gap, same shape as the earlier Activities-reservation-linking one this session. First pass was a single-field "Edit Booking Source" modal; reconsidered immediately (raised directly: "should it just be an edit the whole reservation? not one field per button") and consolidated into one **"Edit Details"** action in `BookingDetail.jsx`'s action menu covering every plain field that endpoint already supports with no availability/conflict checking involved — Source, Number of Guests, Bed Preference, Purpose of Stay, Special Requests, Internal Notes. Deliberately **excludes** `total_amount` (money stays under Payment Tracking, same convention Amend Dates already notes) and `status` (has its own dedicated Check In/Confirm/Cancel/No-Show flows that shouldn't be bypassed by a generic field edit) — and deliberately does **not** fold in dates or unit, since Amend Dates/Transfer Room genuinely need availability checking and folio reposting that a plain field edit doesn't, so those stay their own separate flows rather than being absorbed into this one. Source select is filtered to active sources plus the booking's current one even if since deactivated; shows a soft warning (not a hard block) if `booking.folio_status` indicates agent-billing activity already happened against the *current* source and it's being changed. No backend changes — reused the existing endpoint as-is, sending every field in one request. Verified against the live dev DB with real HTTP round-trips: all six fields updated together in one call, `bed_preference: ''` correctly clears to `NULL`, then fully reverted (including a direct DB fix for `purpose_of_stay`/`internal_notes`, since the COALESCE pattern can only set text fields to empty string, not true `NULL`, via this endpoint — a pre-existing limitation, not new).
- **Edit History — implemented the same pass, migration 066.** Raised right after: an edit made through this (or any future caller of the same endpoint) left no record of what changed, who changed it, or when. New `booking_events` table (same append-only "note" shape as `purchase_order_events`, migration 054, generalized to bookings) + `logBookingChanges()` in `routes/bookings.js` — a single choke point called from `PUT /api/bookings/:id` right before commit, comparing the before/after row for every field the endpoint can touch (`num_guests`, `source`, `total_amount`, `special_requests`, `internal_notes`, `status`, `rate_plan_id`, `bed_preference`, `purpose_of_stay`) and writing one human-readable summary row per save (e.g. `"Guests: 2 → 3; Source: Direct → Walk-in"`) — every future caller of this endpoint gets an audit trail for free without remembering to log it themselves. Free-text fields (`special_requests`/`internal_notes`) log as `"...updated"` rather than dumping the full old/new text; `source`/`rate_plan_id` resolve to their real label/code via a small targeted lookup, not raw IDs. A no-op save (identical values) correctly writes nothing. `GET /api/bookings/:id` now returns `events` (newest first, author name joined); `BookingDetail.jsx` gained an "Edit History" card, positioned right below Staff Notes — the last section on the Details tab. Verified against the live dev DB with real HTTP round-trips: a real multi-field edit produced one correctly-summarized event, an identical no-op PUT added nothing, a second edit sorted above the first. Test events and the booking's fields fully reverted afterward.

**Small fixes — 2026-09-24.** New Booking's deposit now defaults to **50%** (was 30%; the server's fallback when no deposit is sent matches — staff can still pick 0/30/50/100% or type any %). A global wheel listener (`client/src/main.jsx`, `resto-display/src/main.jsx`) blurs a focused `<input type="number">` on mouse-wheel so scrolling the page can't silently change a price/quantity/tax rate (e.g. 11 → 11.01).

**Guest Lists — implemented 2026-09-24 (no migration).** Client request: a morning view of the guest situation. New page `client/src/pages/GuestLists.jsx` (`/guest-lists`, Front Desk nav group right after Check-in/out, icon 🗂, desktop sidebar + mobile More drawer) behind a **new `guest_lists` menu key** (Roles & Permissions → Front Desk; owners always see it) so housekeeping/kitchen roles can be given it without Reservations/Check-in access — rows only link to the booking when the user also has `reservations`. Backed by `GET /api/bookings/guest-lists?date=` (default today WITA via `roomChargeService.todayWITA`, plain `auth`, rides the `reservations` gate): every non-cancelled/no-show booking lands in exactly one list — **arrivals** (check-in = date; Expected/Arrived), **in-house** = stay-overs (check-in < date < check-out; flags "Not checked in"), **departures** (check-out = date; Due out/Checked out, and for today also guests still checked in past their check-out date as **Overdue**). Rows show room + type, guest + nationality/source/group, pax, stay, rate plan, pending balance (deposit/balance lines), bed preference + special requests (internal notes deliberately left off — the printout goes to other teams), and for today's arrivals the room's live housekeeping status. Summary strip: rooms/pax per list, **breakfast pax this morning** (in-house + departures on a plan with `includes_breakfast`) and **dinner pax tonight** (arrivals + in-house with `includes_dinner`). Date picker with ←/→ and **⬇ Download PDF** — `GET /api/bookings/guest-lists/pdf?date=` (pdfkit, `services/guestListsPdf.js`, A4 portrait, shared `pdfHeader.drawDocumentHeader`, same blob-download as the Reservations Guest Report PDF); the JSON and PDF share one `loadGuestLists()` so they never drift. Verified against the dev DB via HTTP for three dates (JSON + PDF, PDF layout checked); not yet clicked through in a browser.

**New Booking: nights field + room availability in the dropdown — 2026-09-24 (no migration).** `NewBooking.jsx` dates are now Check-in · **Nights** · Check-out, kept in sync (typing nights sets check-out, picking check-out sets nights, moving check-in keeps the nights; local-date math via `addDaysYmd`/`nightsBetween`, not UTC); arriving from a calendar cell pre-fills 1 night. Once dates are set, the **Unit dropdown** (single and every group room) calls the existing `GET /api/bookings/transfer-availability` and greys out rooms **booked** for those dates ("— booked (guest)") and rooms **already picked in another row of the same group**; `out_of_order` rooms are labelled "— out of order now" but stay selectable (the status isn't date-based). The per-room "Unit is available / Not available" check below the dropdown is unchanged, and the server still re-checks on save — its group conflict message now names the room ("Room 107 is not available…") instead of the unit UUID. Overdue guests block their room too (see "Overdue check-outs").

**Finding group bookings — implemented 2026-09-24 (no migration).** Before this, a group page was only reachable right after creating it or via a room's "Part of a group booking" banner — nothing listed or marked groups. (A) A **👥 Group** tag (`GroupTag` in `Reservations.jsx`) on every group room in the Reservations list and "Upcoming Arrivals" (click → group page, row click still opens the room), plus a 👥 prefix on the calendar check-in cell and "· Group booking" in its tooltip — all from the `reservation_group_id` the booking rows already carried. (C) A third **👥 Groups** view on Reservations (next to Calendar / List; month arrows hidden there): one row per group — booked by, dates, rooms (+ names), guests, status (Upcoming / In house x/y / Checked out / Cancelled), **"N to assign"** (rooms still under the booker's name → Assign Guests) and balance due; search matches the booker or any room guest; Show = Upcoming & in house (default) / Past / All. Backed by `GET /api/bookings/groups?when=current|past|all&q=` (declared before `/:id`); its balance uses the same formula as the group page's rollup (rooms' `total_amount` − group discount − received deposit/balance payments), verified equal in the HTTP test. On `GroupDetail.jsx`'s Rooms list, each room with unpaid deposit/balance lines shows "Rp X unpaid" + a **Pay →** shortcut to `/reservations/:id#payment` — `BookingDetail.jsx` scrolls to the Payment Tracking card (`id="payment-tracking"`) when opened with `#payment`; (a per-room shortcut). **Record Group Payment** (2026-09-24): one payment from the group — typically the booker's single transfer — marks several rooms' pending deposit/balance lines received at once. Button in the group's Payment Summary while any line is unpaid → modal listing every unpaid line room by room (deposits pre-ticked; "All deposits" / "Everything unpaid" / "Clear"), method (property's payment methods minus `ota_managed`), date, notes, running total. `POST /api/bookings/group/:groupId/payments` `{ payment_ids, method, received_at?, notes? }`: all-or-nothing, 400 if a line isn't in this group, 409 if one is already received; marks lines exactly like Mark Received and runs `recomputeBookingStatus` per room (pending → deposit_paid → confirmed). Partial amounts aren't split — adjust the room's pending line on its booking first. Also fixed Payment Tracking showing a received date one day early (it sliced the UTC timestamp; now shows the local date).

**Add Room to a group + cancelled rooms out of group totals — 2026-09-25 (no migration).** Groups change before arrival (more rooms / fewer rooms). **More:** "+ Add Room" on `GroupDetail.jsx`'s Rooms card → `POST /api/bookings/group/:groupId/rooms` `{ unit_id, num_guests, rate_plan_id, bed_preference, total_amount?, deposit_amount?, reason? }`: same dates as the group (from today if it's already in house), under the booker's name (Assign Guests later), source/special requests copied from the group's rooms, status `pending`; `total_amount` = whole stay tax included like New Booking, **empty = the room's normal rate** (`nightlyRoomRates` + rate plan meals, same as `/pricing/suggest`); deposit default 50%; **no group discount on an added room**; availability checked with `occupiedUntilSql`; logged to the new room's Edit History; 409 for a cancelled or checked-out group. **Fewer:** cancel that room's booking as before — but the group totals used to keep counting it: `GET /group/:groupId`'s rollup and the Groups list now sum only active rooms (not cancelled/no-show), taking each room's own prorated `discount_amount` instead of the group's original discount, and payments received on a cancelled room are reported separately as `paid_on_cancelled` (red note on the page: refund or move by hand). The group Pro Forma PDF also skips cancelled/no-show rooms. Not built: undo a cancellation, and moving a received deposit between rooms. Verified via HTTP against the dev DB (3-room 10%-discount group → cancel one → totals drop by that room incl. its discount share; add at normal rate and at a custom price; room already taken → 409; re-adding the cancelled room's unit works; Groups list = group page; cancelled group → 409), test rows removed; not clicked through in a browser.

**Check Availability — 2026-09-25 (no migration).** FO answers "any rooms for these dates?" without scrolling the calendar, and carries on into the booking. New page `client/src/pages/Availability.jsx` (`/availability`, 🔎 Availability — first item of the Front Desk nav, above Reservations — + mobile More drawer, and a "🔎 Check Availability" button on Reservations; `reservations` menu key + module): check-in · nights · check-out, guests, rooms, quick Tonight / Tomorrow / This weekend. Results per room type: free count ("5 of 8 free"), the stay price (normal rate, room only, incl. service/tax, "from" when rooms differ; per-night rates shown when a pricing period changes mid-stay), "too small for N per room" (guests ÷ rooms vs `max_guests`), and room tiles — taken rooms greyed with why (booked by … until …, overdue, out of order). "+ Add room" / tapping tiles picks rooms; a sticky bar shows the picks + total → **Continue to booking** opens New Booking with `?check_in=&check_out=&units=a,b&guests=2,2` (NewBooking now reads these; 2+ units open as a group; the party is spread over the rooms, capped at each type's max; price still confirmed with "Use this" since the rate plan can change it). Backend: `GET /api/bookings/availability-search?check_in=&check_out=&guests=&rooms=` (max 90 nights) → `services/availabilityService.searchAvailability(propertyId, { checkIn, checkOut, guestsPerRoom, includeGuests })` — **written to be reused by a public booking-engine widget later** (call it with `includeGuests: false`; guest names and out-of-order notes are left out). Availability = the same `occupiedUntilSql` rule as every booking check; out of order counts as unavailable unless expected back by check-in. The occupancy SQL helpers (`TODAY_WITA_SQL`, `checkoutDaySql`, `occupiedUntilSql`, `overdueSql`) moved from `routes/bookings.js` to **`services/occupancySql.js`** (bookings.js + dashboard.js import them from there). Verified via HTTP (a booking blocks its room with guest name + until-date and the counts drop by one, room free again from its check-out date, too-small flag, bad range / >90 nights → 400) and **clicked through in headless Chrome** (desktop + phone width: search, pick two room types, sticky bar, Continue → New Booking pre-filled as a 2-room group); test rows removed.

**UI tidy-up, batch 1 — 2026-09-25** (from a desktop + phone screenshot pass over Dashboard, Reservations, Check-in/out, Guest Lists, Guests, New Booking, Sales, Operations, Night Audit, Agents, Activities): **tabs** — `.tab-bar`'s base line is now an inset shadow instead of a border + `-1.5px` item margin, because the bar's `overflow` clipping cut most of the active tab's underline off (invisible on some pages, e.g. Activities); **phone tables** — Agents, Allotment, Night Audit and Reports wrapped in `.table-wrap` so they scroll sideways instead of being cut off; **Check-in/out on phones** — `.guest-row` wraps, the action column drops under the guest (was running off-screen); late arrivals now say "LATE ARRIVAL · was due 24 Sept" (was "OVERDUE", the word used for guests not checked out) and "today" uses the local date (was UTC — wrong before 08:00 WITA); **New Booking** — check-in · nights · check-out on one row (`.form-row-dates`); **Activities** — standard underline tabs and summary cards (`stat-grid`; its `className="grid"` had no CSS, so the boxes stacked full-width). Not done yet: Guests list actions (Edit/Delete on every row), other settings pages.

**Zahill-only text removed from shared screens — 2026-09-25 (multi-tenancy fix).** Every property was showing/sending Zahill's details: the booking-confirmation WhatsApp (BookingDetail), the welcome WhatsApp (Check-in), the birthday WhatsApp (Guests — also promised Zahill's "complimentary upgrade" to every property; now each property's own optional offer line, migration 071), the Dashboard subtitle ("Kintamani, Bali"), the Check-in checklist's WiFi hint (Zahill's WiFi name + password), the Users page subtitle, the SMTP "From" placeholder, and the TV welcome screen ("Kintamani · Bali"). They now use the property's own name (`branding.name`) and area: `GET /api/settings/branding` also returns `area` (= `property_settings.market_area`, set in Property Details → Market Insights); the display state's `property.location` is now the area, falling back to the street address (Room Display + TV). No area set → the location is simply left out. Still named Zahill on purpose: the TV setup hint ("Zahill TV Settings" is the APK's real name).

**Page headers — 2026-09-25.** Shared `.page-header` (index.css) now wraps: the title block keeps ≥ 280px and the action buttons drop underneath (left-aligned on phones) instead of squeezing the title into several lines — applies to all ~35 pages using it. New `components/PageHeader.jsx` for detail pages: eyebrow (← back link · what this is), title (the name people know it by), one line of key facts + a status badge, actions on the right. Used by `GroupDetail.jsx` (title = booker; dates · nights · rooms · guests; Upcoming / In house x/y / Checked out / Cancelled; actions = Check In Group, Assign Guests with a count while rooms are under the booker, 💬 WhatsApp booker + ⬇ Pro Forma icon group, ⋮ Amend Dates / Add Room / (Assign Guests) / Cancel Group — same layout as the booking page) and `BookingDetail.jsx` (title = guest, eyebrow = Booking #…, room · dates · nights · guests + status). `.header-divider` replaces the inline divider. `.page-wrap` got extra bottom padding so the last row can scroll clear of the floating call button. Known: `.alert` is `display:flex`, so an alert mixing text with `<b>`/links splits into columns on narrow screens — wrap the text in one `<div>` (fixed on the group page; others not audited).

**Quick Check-in out of the menu — 2026-09-25.** The sidebar's ⚡ Quick Check-in item and the phone bottom bar's ⚡ Quick CI tab now show only to staff with `quick_checkin` but NOT `checkin_full` (they'd have no other way in); everyone else (incl. owners) reaches it from Check-in / out's ⚡ Quick Mode button, which now shows only with `quick_checkin`. For `checkin_full` users the bottom bar's third tab is ✅ Check-in (`/checkin`), and Check-in / out left the mobile More drawer (no duplicate). The `/quick-checkin` route and the Roles permission are unchanged.

**Group Amend Dates + Remove Room — 2026-09-25 (no migration).** Follows Add Room (above): a group's dates used to be changeable only room by room — and not at all, since single Amend Dates is blocked for group rooms (now 409 `GROUP_BOOKING` pointing to the group page). **📅 Amend Dates** on `GroupDetail.jsx` (check-in · nights · check-out, per-room price preview) → `GET /api/bookings/group/:groupId/dates/quote?check_in=&check_out=` + `PUT /api/bookings/group/:groupId/dates` `{ check_in_date, check_out_date, charge: 'difference'|'complimentary'|'custom', amount?, reason }`. Changes every room not cancelled / no-show / checked out, **all-or-nothing** (any unavailable room → 409 listing the rooms; a checked-in room can only change its check-out date), each room priced and moved exactly like a single booking: `datesQuote()` + the new shared **`applyNewDates()`** (extracted from `PUT /:id/dates`, which now calls it too — same note, same `applyBookingPrice`/`repostStay`). `custom` = the extra for the WHOLE group (negative = credit), split over the rooms by their share of the new normal price. Also updates `reservation_groups` dates, recomputes pending agent commissions, one owner control alert for the group when extra nights are given below the normal price. **Remove** on each room row (not checked in, not the last active room) → `POST /api/bookings/group/:groupId/rooms/:bookingId/cancel` `{ reason }`: cancels that room, voids its folio charges, logs to its Edit History (incl. money already received on it). Verified via HTTP against the dev DB (extend + charge, shift no-charge, custom credit split, conflict leaves everything unchanged, checked-in guard, remove + last-room guard, payment lines = prices after every step, single-booking Amend Dates regression), test rows removed; not clicked through in a browser.

**Check in now, pay later — implemented 2026-09-24 (no migration).** Direct/walk-in bookings still need full payment to check in (`checkinOneBooking` in `routes/checkin.js`, codes `DEPOSIT_UNPAID`/`BALANCE_UNPAID`), but front desk can now override it when the guest can't pay yet (EDC down, etc.): `POST /api/checkin/:bookingId/start` (and the group `/group/:groupId/start`) accept `pay_later_reason` — any FO staff (owner's choice), reason required (blank = refused), logged to Edit History as "Checked in without full payment — Rp X unpaid (pay later). Reason: …". The unpaid lines simply stay pending, so the amount shows in Balance Due, the checkout warning and the folio, and is settled later with Record Payment / Mark Received. UI: shared `components/PayLaterOption.jsx` under the payment-block message on Check-in/out (English — continues into the normal steps with a ⚠ banner) and Quick Check-in (Indonesian: "Check-in dulu, bayar nanti"). Record Payment's form also gained quick amounts: **1 night** (booking price ÷ nights) and **Full balance** — for a guest paying night by night mid-stay.

**Record Payment on the folio — implemented 2026-09-24 (no migration).** Found on production: a checked-out group room (29 extra beds added via the folio's Add Charge for the whole group) showed Rp 7.25M owed with no way to mark it paid — Payment Tracking only handles the room's deposit/balance lines, the Folio tab could only add/void charges, and checkout doesn't collect folio extras. New `POST /api/folio/:bookingId/payment` `{ amount, method, received_at?, notes? }` (method validated against `payment_methods`; any status except cancelled/no-show, incl. checked out): applies the money to the room's **pending deposit/balance lines first** (oldest first; a partial payment splits a line into a received part + pending remainder, so Payment Tracking and `recomputeBookingStatus` stay right), and records **the rest as an `incidental` payment** (extras paid at the desk, migration 067 — never counted as the room's payment). Logged to Edit History. UI: **💳 Record Payment** under "Estimated Balance Due" on `BookingDetail.jsx`'s Folio tab (pre-filled with the balance, partial allowed), and a **Record payment →** link on each Balance Due row (`/reservations/:id#record-payment` opens the Folio tab + form). **Known gap:** the group page's Payment Summary is room-only (rooms' totals − room payments), so extras charged to a group room don't appear in the group's Balance Due — check the Master Folio / Balance Due tab for those. Verified via HTTP (the production case, partial payments splitting a room line, guards).

**Amend Dates pricing — implemented 2026-09-24, reworked 2026-09-25 (no migration).** ⚠️ 2026-09-25: the quote no longer uses the normal rate on both sides — the current dates count at the booking's own price (`total_amount − discount_amount`, as FO entered it, so a later tax-rate change can't shift it) and the new dates are SUGGESTED at that booked price per night × new nights (meals included — an extension also charges breakfast for the added night, which used to be taken out of the room price instead), with the room's normal rate (+ rate-plan meals) returned as `new.normal_total` for reference. A plain shift is now free. The Amend Dates window (and the group one) shows the new price as an editable field (pre-filled with the suggestion; typed price → `charge:'custom'`, `amount = typed − current`; options Charge the difference / Keep current price). Change Room's booked-price side likewise reads `total − discount` × the room's share of the net split. Original 2024-09-24 write-up follows. Amend Dates used to keep the same total and just re-spread it over the new nights, so an extension was effectively free. Now `GET /api/bookings/:id/dates/quote?check_in=&check_out=` (`datesQuote()`) prices old vs new dates at the room's **normal rate** (`pricingService.nightlyRoomRates`, same as New Booking / Change Room) incl. service/tax, and `PUT /api/bookings/:id/dates` takes `{ check_in_date, check_out_date, charge: 'difference'|'complimentary'|'custom', amount?, reason }` — reason required. A non-zero charge goes onto the booking price via `applyBookingPrice` (keepDiscount, credit if overpaid — same as Change Room); zero keeps the old behaviour (re-spread + repost). Extending charges the added nights, shortening credits the removed ones, shifting only differs across pricing periods. Logged: "Dates changed: 23–25 Sep (2 nights) → 23–27 Sep (4 nights). Charged +Rp …". `BookingDetail.jsx`'s Amend Dates modal gained a Nights field and the same price box / charge options / reason as Change Room. This is also the fix path for an overdue guest who actually stayed on. Verified via HTTP (extend, shorten → credit, complimentary, overdue extended to tomorrow, guards).

**Overdue check-outs — implemented 2026-09-24 (no migration).** A guest still `checked_in` after their `check_out_date` (usually staff forgot to check them out — new PMS) used to be invisible outside Quick Check-in / Check-in-out, and their room looked free. Now: (1) **the room stays blocked** — `occupiedUntilSql()` in `routes/bookings.js` (an overdue stay occupies its room through tonight, WITA; guests due out *today* aren't affected) is used by every availability check: `GET /availability` + `/transfer-availability` (now also return `overdue`), single + group booking creation, Transfer, Amend Dates, Change Room. New Booking's dropdown/message and Change Room's list say "still checked in (overdue)". (2) **Dashboard**: red banner first on the page ("N guests past check-out and still checked in" — room, guest, due date, days ago, unpaid balance from the Pro Forma, Open booking →) from `dashboard/summary`'s new `overdue_checkouts`, plus a ⏰ badge on the room tile. (3) **Night audit owner email**: a red "Still checked in past check-out" section (anyone with `check_out_date <= business date` still checked in at audit time) + the count in the audit summary. (4) **Reservations calendar**: now loads every stay *overlapping* the month (`date_from`/`date_to` from the day before the 1st) instead of only stays *checking in* that month — fixes stays crossing a month boundary missing from the later month — plus `include_overdue=1`; an overdue stay is drawn on to today in red ("⏰ overdue", tooltip with the original due date). No auto-checkout (payment/key unknown) — the fix is FO checking them out or using Amend Dates.

**Change Room (upgrade / downgrade / move) — implemented 2026-09-24 (no migration).** Client request: upgrading a reservation incl. the extra price. Replaces the price-less "Transfer Room" in `BookingDetail.jsx`'s ⋮ menu (the old `PUT /:id/transfer` endpoint still exists but the UI no longer calls it). FO can do it (no owner gate, by the client's choice); a reason is always required and logged to Edit History. `GET /api/bookings/:id/change-room/quote?unit_id=` prices the move as the NEW room's **normal rate** (base rate + pricing periods, via the shared `services/pricingService.nightlyRoomRates()`) minus **what the booking actually charges for the current room** (`room_revenue` — the price FO typed, after discount — spread per night; fixed 2026-09-25, it used the current room's normal rate too, so a specially-priced booking got a difference that didn't match what the guest paid; the current room's normal rate is still shown as a note). The special price does not carry over to the new room — moving back later uses the normal rate. The new room's price is an editable field in the Change Room window, pre-filled with its normal rate (FO can type a special rate; the difference follows; options are now Charge the difference / Complimentary — "Other amount" is gone). An edited price is sent as `charge:'custom'` with `amount = typed price − booked price`, so Edit History shows both the charge and the normal difference and the owner discount alert still fires — also now used by `GET /api/pricing/suggest` (verified identical output before/after) — for the nights still to come (all nights for an upcoming stay, from today for an in-house guest), grossed up with service/tax; negative = downgrade. `PUT /api/bookings/:id/change-room` `{ unit_id, charge: 'difference'|'complimentary'|'custom', amount?, reason }` moves the booking and adds the charge to the booking price as room revenue via the new shared **`applyBookingPrice()`** (Edit Price's recompute logic, extracted; `keepDiscount: true` so the charge isn't discounted and group rooms keep their prorated discount share; `receivedWasTypo: false`, so a downgrade below what's paid becomes a credit). In house: old room → available + `housekeeping_status='dirty'`, new room → occupied, both tablets pushed via SSE. Blocked for statuses other than pending/deposit_paid/confirmed/checked_in, the same room, a room booked for those dates, and a charge on an agent-invoiced stay. **Known limitations:** the charge covers only the remaining nights, but the folio re-spreads the stay's room total evenly across all nights (per-night room revenue isn't stored). Verified via HTTP (upcoming upgrade charging the difference, in-house complimentary move, downgrade credit after full payment, guards) and Edit Price regression tests re-run after the extraction.

**Early departure at checkout — implemented 2026-09-24 (no migration).** A guest leaving before the booked check-out date (e.g. checked in today, leaves tonight) used to be handled badly: checkout voided every room night from today on, so a same-day departure was charged **Rp 0**, while the booking kept its full price/dates (reports counted the full stay) and the room stayed blocked until the old date. Now `PUT /api/checkin/checkout/:id/complete` detects it (still `checked_in`, today < check-out date) and returns **409 `EARLY_DEPARTURE`** (booked/stayed nights) unless the body has `early_departure: { charge: 'stayed'|'full'|'custom', amount?, reason }`: **stayed** = nights used at the booking's own rate (same-day = 1 night; discount scaled by the same ratio), **full** = keep the whole price (no refund), **custom** = `amount` is the total for the stay. The booking's check-out date is shortened to the nights charged and the price goes through `applyBookingPrice` (credit if overpaid); logged to Edit History with the reason. The folio catch-up now posts the whole (possibly shortened) stay up to its check-out date instead of up to today — fixing the Rp 0. `occupiedUntilSql` frees a `checked_out` booking's room from the day the guest actually left (`checkin_records.checkout_time`, WITA) — so a same-day departure keeps its 1 charged night but the room is sellable that evening; Guest Lists / Balance Due count "checked out today" by that real day too. UI: shared `components/EarlyDepartureOption.jsx` (English + Indonesian) in the checkout forms of BookingDetail, Check-in/out and Quick Check-in, shown only when leaving early; confirm stays disabled until a choice + reason. `grossFactor`/`splitRevenue`/`applyBookingPrice` moved from `routes/bookings.js` to **`services/bookingPriceService.js`** (shared with checkout; behaviour unchanged — Edit Price / Amend Dates / Change Room tests re-run). Verified via HTTP (same-day "nights used" → 1 night posted + room free tonight + on today's Departing; full kept; custom with credit; normal checkout unaffected).

**Dashboard "Tonight's Occupancy" fixed — 2026-09-25 (no migration).** The card counted rooms occupied *right now* (`units.status='occupied'`), so on a turnover day it still counted every guest leaving today and missed today's arrivals (seen live: 15/33 while 22 rooms were arriving with 0 stay-overs — really 22). `dashboard/summary`'s occupancy now also returns `tonight` (distinct rooms with a pending/deposit_paid/confirmed/checked_in booking, check-in ≤ today WITA and `occupiedUntilSql` > today — the same rule every availability check uses, so overdue guests count), `tonight_breakdown` `{staying, arriving, overdue}` and `out_of_order`; `occupied` (right now) is kept. The card shows tonight / rooms not out of order, % and free on the same basis, plus a "N staying over + N arriving (+ N overdue)" line (and "N rooms · N out of order" when any). `occupiedUntilSql`/`TODAY_WITA_SQL` are now exported from `routes/bookings.js`.

**One status rule across pages — 2026-09-24.** Guest Lists sorted guests by booked dates only, so a guest checked in today stayed under Arriving ("Arrived") and a guest who never arrived showed as In-House ("Not checked in"), disagreeing with the Check-in/out page (status-based). Now, **for today**, `loadGuestLists` sorts by **status**, exactly like the Check-in/out page: Arriving = not checked in yet (`pending`/`deposit_paid`/`confirmed`, arrival date ≤ today — late ones flagged `late_arrival`, "Late · was due …"), In-House = `checked_in` leaving after today, Departing = `checked_in` due out today or overdue, or `checked_out` today. Other dates keep the date-based view (future In-House rows say "Booked"). Breakfast/dinner counts (Guest Lists summary **and** Kitchen tab) share `MEAL_FLAGS_SQL`: for today, breakfast counts only guests actually checked in who slept here last night; dinner = checked in staying tonight + not-yet-arrived guests due tonight. The Dashboard's arrivals banner, room tiles and next-booking subqueries used `status IN ('confirmed','pending')` and silently skipped **deposit_paid** arrivals — now include them.

**Kitchen tab — implemented 2026-09-24 (no migration).** Client request: how many breakfasts to prepare for tomorrow. Third tab **🍳 Kitchen** on the Guest Lists page (visible to anyone with `guest_lists`, same date picker; press → for tomorrow), with its own Download PDF (`services/kitchenPdf.js`, room-by-room with a "Served" tick box). `GET /api/bookings/kitchen?date=` + `/kitchen/pdf` (shared `loadKitchen()`): **breakfast** that morning = guests who slept here the night before (staying over + checking out that day) whose rate plan `includes_breakfast`; **dinner** that night = guests sleeping here that night (arriving + staying over) with `includes_dinner`. Counted in guests (`num_guests`), shows special requests (dietary notes), "Not checked in yet" flags, and how many in-house guests *don't* have the meal (room only). Totals equal the Guest Lists summary's breakfast/dinner counts (verified for three dates). Deferred ideas discussed with the owner: a Dashboard "Breakfast tomorrow: N pax" line and an evening Telegram message to a kitchen chat (would need per-chat alert selection, since every linked chat currently gets every alert).

**Assign guests to rooms — implemented 2026-09-24 (no migration).** Client request: groups are booked under one person's name (every room's `bookings.guest_id` = the booker) and the real guest list arrives later; Room/TV Display should show the actual occupant. New `assignGuest()` in `routes/bookings.js` re-points a room's booking at the real guest (existing `guest_id`, or `new_guest { name, nationality, id_number, whatsapp, email }` — only name required, created on the spot) and logs `Guest: A → B` to Edit History; blocked for cancelled/no-show/checked-out. Endpoints: `PUT /api/bookings/:id/guest` (single booking — "👤 Change Guest" in `BookingDetail.jsx`'s ⋮ menu, e.g. agent/company bookings) and `PUT /api/bookings/group/:groupId/guests` (`{ assignments: [...] }`, all-or-nothing — "👥 Assign Guests" on `GroupDetail.jsx`, one picker per room). Shared `client/src/components/GuestPicker.jsx` (debounced `GET /api/guests?search=`, or "+ Add as a new guest" with name/nationality/passport/phone). The booker stays the group's contact (`reservation_groups.primary_guest_id`, card relabelled "Booked by"); billing unchanged (charges stay per room). `GET /api/bookings/group/:groupId` now returns each room's `guest_name`/nationality/id_number; the Rooms card shows it with a "(booker)" tag and a reminder while rooms are still under the booker's name. Everything reading `bookings.guest_id` follows automatically — Room Display (pushed via `sse.notify(controllerId, {type:'guest_changed'})`, refetches instantly), TV Display (15s poll), Registration Card, police Guest Report, Guest Lists, Balance Due. Verified via HTTP against the dev DB (bulk assign existing + new guest, display state shows the new names, no-op / not-in-group / blank-name / checked-out guards); not yet clicked through in a browser.

**Balance Due — implemented 2026-09-24 (no migration).** Client request: a printable list of unpaid balances, especially departing guests, so FO doesn't open each booking. Second tab **💰 Balance Due** on the Guest Lists page (same date picker; Download PDF saves whichever tab is open). Tab shown only to owners + staff with `checkin_full`, and **enforced server-side** (`requireOwnerOrMenu('checkin_full')`) since it's money. `GET /api/bookings/balance-due?date=` + `/balance-due/pdf` (`services/balanceDuePdf.js`, shared `loadBalanceDue()`): sections **Departing** (check-out = date, incl. already checked out but unpaid), **Overdue** (today only: still checked in past check-out), **Staying** (in the hotel that night); only rows with balance ≥ Rp 1. Each row's balance is the **whole-stay Pro Forma figure** (`folioService.computeProforma`: all nights + extras charged to the room + service/tax − payments received), broken into Room & meals / Extras / Service-tax / Paid. Agent-billed stays (city-ledger source or `folio_status` set) are listed but greyed and left out of "to collect"; OTA bookings get a "check if prepaid" note. Headline: to collect from departing (+overdue) and total outstanding. **Guest Lists' Balance column now uses the same Pro Forma figure** (it used to count pending deposit/balance lines only, so extras charged to the room showed as "Paid"). Verified against the dev DB (real rows + a test booking: 1,000,000 room + 250,000 extra − 300,000 deposit = 950,000; staff without `checkin_full` → 403; PDF layout checked); not yet clicked through in a browser.

**Complimentary stays — implemented 2026-09-26, migration 072.** 🎁 Make Complimentary (booking ⋮ menu, or "Complimentary stay" on New Booking for a single room → created at its normal price, then made free on the booking page via `location.state`). Scope: **Room only** (meals + extras paid) / **Room + meals** (extras paid) / **Everything** (extras charged to the room free too — `folioService.markComplimentary` flags them `complimentary` and leaves them out of `loadFolio`/`computeProforma` totals and `agent_billable_total`). Logic in `server/services/complimentaryService.js`, routes in `server/routes/complimentary.js` (mounted at `/api/bookings` before `bookings.js`, `reservations` module): `GET /:id/complimentary` (price per scope, `can_grant`, pending request, approver names), `POST /:id/complimentary` (owner or the new **`grant_complimentary`** permission — Roles & Permissions → Front Desk, `requireOwnerOrMenu`), `POST /:id/complimentary/request` + `/approve` `{request_id, code}` + `DELETE /:id/complimentary/request` (any staff), `DELETE /:id/complimentary` (remove; owner/permission). **Approval code (free):** each approver — a Reports & Alerts Telegram recipient with the new `approve_complimentary` alert ticked (catalog type `alert`, `paid:false`), **personal chats only** (address not starting `-`) — gets their own 6-digit code (sha256-hashed); the code typed says who approved (`complimentary_approved_by`). Locked to the request's scope + reason and to a booking fingerprint (room, dates, price, discount, plan, guests); 10 min, 5 tries, single use; a new request cancels the old. No bot / no approvers → clear 409 (`NO_TELEGRAM` / `NO_APPROVERS`). **Price:** `applyBookingPrice` gained a `split: {roomNet, mealNet}` option (exact net amounts, discount 0); `receivedWasTypo:false` so money already paid stays as a credit. Discount type/value are cleared; removal restores the previous total/discount per night. Edit Price is blocked on a comp stay, Change Room only without a charge. **Reports:** `getReport` returns `paid_nights`, `comp_nights`, `comp_value` (net per-night value + comped extras; those sales leave `ancillary_revenue`); ADR = room revenue ÷ paid nights on Reports / CSV / Daily Close / Weekly / Monthly, each with a "🎁 Complimentary: N nights · value" line; occupancy still counts comp nights. Invoice/pro forma print "Complimentary stay — …" and comped extras as "Free"; Reg Card rate "Complimentary". Owner control alert (paid) on every grant. Verified via HTTP/service tests (Telegram stubbed) + headless Chrome click-through; not tried with the real bot.
- **Approve / Decline buttons (migration 073, same day).** The approver's Telegram message carries ✅ Approve / ❌ Decline inline buttons (`callback_data` `cmp:a|d:<request id>`) plus their code as the fallback. `services/telegramLink.js` now also long-polls while any request is open (`tokensWithPendingLinks` includes pending unexpired `complimentary_requests`; `allowed_updates` adds `callback_query`) and hands presses to `complimentaryService.handleButton`: accepted only from the chat the message went to (`from.id` = chat id = the stored `chat_id`) and via the property's own bot; same checks as the code (pending, not expired, booking fingerprint unchanged) through the shared `finalizeApproval()` — the stay is recorded as made by the person who asked, approved by the approver. Decline sets status `declined`. After approve (button or code) / decline / withdraw / replace, `closeMessages()` removes the buttons from every approver's message and replies under it ("✅ Approved by …", "❌ Declined by …", "↩️ …"); a late press gets a popup ("Already approved by …", "expired"). `sendToChat` takes `replyMarkup` and returns `message_id`. The front desk window polls `GET /:id/complimentary` (`latest_request`) every 3 s while waiting and switches to "approved by X on Telegram" / "Declined by X" by itself. Owner alert moved into `complimentaryService.notifyOwner`. Fail-over order: button → code in the same message → owner / `grant_complimentary` applies directly. Verified with Telegram stubbed (23 checks: wrong chat, stranger, decline, approve, double press, replaced, expired, code fallback, booking changed) + headless Chrome (screen updates after a press); **tried live 2026-09-26 on the dev bot** (Approve and Decline from a real phone, messages updated, screen saw both).

**Edit Price — implemented 2026-09-24 (no migration).** Raised by the client on site: FO mis-typed a booking's price and there was no way to correct it. New owner-only `PUT /api/bookings/:id/price` (`{ total_amount, reason }` — `total_amount` is the same figure New Booking takes: whole stay, tax included, before discount; reason required) + an owner-only "💰 Edit Price" item in `BookingDetail.jsx`'s ⋮ menu (preview of old vs new, discount re-applied, overpayment warning). Allowed at any status except cancelled/no-show — including after checkout, by the client's choice. Recomputes everything derived from the price the same way booking creation does: `discount_amount`, the net `room_revenue`/`fnb_revenue` split (so Reports/Dashboard/Night Audit/Reg Card/yield history follow), the folio's posted room/F&B nights (`roomChargeService.repostStay`), the pending deposit/balance payment lines, the booking status (`services/paymentStatusService.recomputeBookingStatus`, now shared with `routes/payments.js`), and an unpaid agent commission (`agentBillingService.recomputeCommission`). Received payments are never modified: a price above everything already received adds a **second pending `balance` line** for the difference (so `BALANCE_PAID_SQL`/`BALANCE_AMOUNT_SQL` in `routes/bookings.js`, `recomputeBookingStatus`, and `BookingDetail.jsx`'s Payment Tracking/checkout warning now handle more than one balance line); a price below what's recorded as received makes the owner choose (`received_was_typo`, required — the endpoint 400s `RECEIVED_EXCEEDS_PRICE` without it): **true** = the received amount was typed from the same wrong price, so received lines are trimmed newest-first down to the new price (the common FO-typo case); **false** = the guest really paid more, so the difference stays as a credit on the folio (no refund flow — returned by hand). `BookingDetail.jsx`'s Payment Tracking card now also shows Paid and Balance due / Overpaid under the Total. Blocked for group rooms and stays already on an agent invoice (`folio_status` invoiced/paid). Logged to Edit History as `Price corrected: old → new. Reason: …`. The generic `PUT /api/bookings/:id` now **400s if `total_amount` is sent**, since changing only the stored total left everything else on the old price (no UI ever sent it). **Planned follow-up:** a separate permission so trusted FO staff can edit prices too (today owner-only via `requireRole('owner')`). Verified with HTTP round-trips against the dev DB (price up/down, fully-paid top-up line, in-house folio re-post, 403/400 guards); not yet clicked through in a browser.

**Guest Registration Card — implemented 2026-09-22, migration 063.** Front desk previously had no in-system version of the paper registration card guests sign at check-in — it was a hand-maintained Word doc, hardcoded to Zahill's branding and house rules, every field typed in by hand. Now a printable PDF generated from real booking/guest data, branded per property (reuses `routes/folio.js`'s invoice header — extracted into shared `server/services/pdfHeader.js`'s `drawDocumentHeader()` so both can use it). New `server/services/registrationCardPdf.js` renders the card; `GET /api/checkin/:bookingId/registration-card` serves it (plain `auth`, rides the existing `front_desk`+`reservations` gates on `checkin.js`, no new module). Works for any booking status — `confirmed` (not yet arrived) as well as `checked_in` — since front desk prepares this ahead of arrival as often as at/after check-in.
- **Two new optional fields**, filled in at print time rather than required at guest/booking creation: `guests.address` (home address — persists on the guest for future stays) and `bookings.purpose_of_stay` (per-stay, can differ trip to trip). Both editable inline in the new `client/src/components/RegistrationCardModal.jsx` (shared by all three entry points below) via the existing `PUT /api/guests/:id` / `PUT /api/bookings/:id` endpoints (one field added to each's existing COALESCE update, no new routes).
- **House-rules text is per-property editable** — `property_settings.registration_notice`, defaulted to Zahill's current wording for every property (including new ones onboarded later), editable in `SettingsProperty.jsx` alongside Tax Rate/Address (rides the existing generic `GET/PATCH /api/settings/property`).
- **Three entry points**, all opening the same modal: `CheckIn.jsx`'s post-check-in success panel (dropped the old 2s auto-dismiss `setTimeout` so staff has time to act — now an explicit "🖨 Print Registration Card" / "Done" choice), `BookingDetail.jsx`'s Download menu (reprint anytime), and — added after direct feedback that front desk prepares this paperwork *before* the guest physically arrives — a "📝 Reg. Card" shortcut in the Dashboard's per-room popup (`UnitCard`), gated on `unit.booking_id || unit.arriving_booking_id` so it works for both the current in-house stay and a not-yet-checked-in arrival.
- **Deliberate simplifications vs. the old paper form:** no First/Last name split (system only ever stores one `guests.name`); no ETA/ETD (arrival/departure *time* isn't tracked anywhere, only the date); Room Type shown as a real name via `units.type` (migration 062's mirror column) instead of a hand-typed abbreviation; Room Rate uses `bookings.room_revenue / nights` (the platform's existing NET per-night source of truth, migration 044) instead of a raw figure; "Checked In By" is whoever is generating the card right now (`req.user.name`), not necessarily who ran the original check-in, since this is reprintable anytime.
- Verified end-to-end against the live dev DB and real HTTP round-trips (migration apply, PDF generation, both field-save paths, property-level house-rules override), including rasterizing the generated PDF to confirm the actual visual layout (not just `pdftotext`, whose `-layout` heuristic mis-merges tightly-spaced rows in a way the real PDF doesn't exhibit). Not yet clicked through in a browser.
- **Layout tightened — 2026-09-24.** The shared `pdfHeader.drawDocumentHeader` gained a compact layout (one band, three columns: property name/address/phone/email left, logo centred on the page, title block right — both text blocks vertically centred on the logo; ~90pt shorter than the old stacked header). **Since 2026-09-25 it is the only header layout, used by every printed document** (invoice, pro forma, group pro forma, registration card, sale/activity receipts, Guest Lists/Kitchen/Balance Due, and — previously with their own copy-pasted header — the agent invoice and police Guest Report). Options: `refLine`, `extraLines` (e.g. the agent invoice's Issued/Period), `dateLine` (defaults to today, `null` to omit). Compact-header logo is 72pt. The "GUEST SIGNATURE :" label + line stay as on the original form (no box, no date — owner's choice), now **centred on the page**, with the line placed at the bottom of all the space left above the room & booking table (which sits at the foot of the page; min 70pt, else a new page). Also fixed stray "Ð" glyphs at the end of each house-rules line: the notice is saved from a textarea with Windows `
` line endings and pdfkit's standard font prints `
` as "Ð" — `registrationCardPdf.js` now normalises line endings before rendering.
- **Printing ahead + ID documents — 2026-09-24.** Client's check-in order: print the card → guest fills it in and hands over ID → FO scans the ID → finish check-in. So the card is now also printable from: every row of Check-in/out → Arriving Today ("🖨 Reg. Card"), step 1 of the Check-in/out steps, the top of Quick Check-in's check-in form ("🖨 Cetak Kartu Registrasi"), and Guest Lists → Arriving (per-row 🖨 + **"🖨 Print all arrival cards"** — `GET /api/checkin/registration-cards?date=`, every arrival of the date, not cancelled/no-show, **one card per page** in room order, for printing the evening before). `routes/checkin.js`'s card code is now `loadRegCardData()` / `loadRegCardProperty()` / `drawRegCard()` shared by the single and bulk routes. **"Checked In By"** now prints whoever actually checked the guest in (`checkin_records.processed_by`), blank on a card printed before arrival — it used to print whoever generated the PDF. **ID documents:** uploads worked (resized JPG in `server/uploads/`, `guests.id_document_url`) but viewing didn't — `/uploads` was a static mount behind `auth`, which needs the Authorization header, so the Guest Profile's plain "View ID" link got "No token provided"; and the mount checked no property. The static mount is **removed**; new `services/idDocument.js` (`saveIdDocument` — photos → max-1200px JPG with EXIF rotation, **PDF scans kept as-is**, anything else 400; `idDocumentPath`), `GET /api/guests/:id/id-document` (property-checked, `no-store`) and `POST /api/guests/:id/id-document` (add/replace — FO often scans after the guest hands the ID over, separate from check-in); the check-in steps' `/complete` uses the same saver (so PDFs work there too). New `components/GuestIdDocument.jsx` (thumbnail or "Open ID (PDF)", Upload/Replace, loaded as a blob with the token) on BookingDetail's Guest card (`guest_has_id_document` on `GET /api/bookings/:id`) and the Guest Profile. Verified via HTTP (photo + PDF upload/view, other property → 404, text file → 400, check-in upload with a PDF, bulk cards: 4 arrivals → 4 pages).
- **Logos trimmed to their artwork — 2026-09-24.** Zahill's logo PNG was 512×341 with the artwork only 220×217 of it (43% of the width) — the transparent padding got scaled along with it, so the logo looked tiny in every PDF header, the nav and Room/TV Display. New `services/logoImage.processLogo()` trims the empty border (sharp `.trim()`, background = top-left pixel, so transparent or plain white) before the usual fit-inside-512×512; used by the Superadmin logo upload (`routes/admin.js`). One-off `node maintenance/trimLogos.js` (from `server/`) trims every property's *current* logo in place (same `logo_url`, original kept as `<name>-untrimmed.png`; idempotent). `server/uploads/` is gitignored, so **run the script on each server** (done on the dev machine).
- **"Publish Rate" per booking source — implemented 2026-09-22, migration 065.** Raised directly: an OTA or travel-agent booking's real Room Rate/Deposit shouldn't print on the guest-facing Registration Card, since what the hotel nets differs from what the guest paid the channel. New `booking_sources.publish_rate` (default `true` — nothing changes for existing direct/walk-in sources), toggled per source in `Settings.jsx`'s Booking Sources card (checkbox + inline hint + a "Rate hidden on Reg. Card" list badge when off). Deliberately independent of `is_ota`/`source_type` — the owner controls it explicitly per source rather than the system assuming every OTA/agent wants this — though migration 065 backfills existing OTA and `travel_agent` sources to `false` as a sensible starting point. When off, `registrationCardPdf.js` prints **"Arranged by \<source label\>"** in both the Room Rate and Deposit fields instead of numbers (dynamic per source, not a generic placeholder — reuses the same `source_label` already shown on the card). Verified against the live dev DB with real HTTP round-trips: a real Airbnb-sourced booking's card correctly showed "Arranged by Airbnb" while a Direct-sourced booking still showed real numbers; toggling `publish_rate` via the Settings API on the Direct source and regenerating confirmed the text updates dynamically for any source, not just the pre-seeded OTAs. Test toggle reverted afterward.

**`BookingDetail.jsx` action menu** — the page header had grown to up to 8 buttons in a row (Amend Dates, Transfer Room, WhatsApp, Check In/Out, Confirm, Mark No-Show, Cancel) once Call Room was added on top; consolidated into a new reusable `ActionMenu.jsx` (`client/src/components/`) — a generic "⋮" dropdown taking `items: [{ label, icon, onClick, danger }]`, falsy entries filtered so conditional items read naturally (`condition && {...}`). Only the single status-driven primary CTA (Check In / Check Out / Confirm Booking — whichever applies) stays as a visible button; everything else, including the new **Call Room** action (`hasModule('room_controller')`-gated, calls `useCall()`'s `callRoom({ id: booking.unit_id, name: booking.unit_name })` directly — no room picker needed since the booking already names its own unit), moved into the menu.

**`BookingDetail.jsx` / `GroupDetail.jsx` header + tabs revamp — implemented 2026-09-18.** The single flat "⋮" from above got reorganized by frequency/consequence rather than just dumped together, after live feedback that the page felt sparse and the tabs looked like plain buttons:
- Real tabs — the `Details`/`Folio`/`Activities` (and `GroupDetail`'s `Details`/`Master Folio`) switcher now uses the existing `.tab-bar`/`.tab-bar-item` component (already used by `BackOffice.jsx`) instead of a row of `btn-sm` buttons. Caught and fixed a real latent bug in that shared class while wiring it up: `.tab-bar` set `overflow-x: auto` but never `overflow-y` — per the CSS spec, that silently computes the unset axis to `auto` too, so any sub-pixel height mismatch on the flex row could pop a vertical scrollbar clipped to the tab-bar's own height. Fixed with an explicit `overflow-y: hidden`, benefiting every page that uses `.tab-bar`.
- Header actions, right to left: the one status-driven primary CTA → a new `.icon-group` (`index.css`) segmented control joining Call Room/Send Message/WhatsApp Guest/Download into one visually-grouped pill instead of scattered separately-boxed buttons → the `⋮` menu, now internally sectioned (Modify Booking, then a divider, then a red Danger Zone) instead of one flat list. Download was deliberately pulled out of `⋮` and merged into the icon group as a plain icon (no label/triangle) rather than getting its own labeled button, after live back-and-forth: a labeled "Download ▾" button read as visually louder than the icon buttons next to it (transparent `.btn-secondary` vs. filled `.btn-icon`) and, separately, briefly stopped opening at all because `.icon-group`'s `overflow: hidden` (used to clip the group's rounded corners) was clipping the dropdown panel it opens — fixed by moving the rounding onto the first/last child directly instead of clipping the whole group.
- `ActionMenu.jsx` gained `bare` (render as a plain icon with no button chrome, for dropping into `.icon-group`), `icon`/`label` (a labeled trigger, e.g. a standalone "Download ▾"), and `{ divider: true }` items (for sectioning, e.g. Modify Booking vs. Danger Zone). Its dropdown items also gained `whiteSpace: nowrap` + `width: 100%` + a `hint` (native tooltip) field — a multi-word label like "Pro Forma (Estimate)" was wrapping word-by-word because neither was set; fixed by both fixing the CSS and shortening the label, moving the nuance into the tooltip.
- Both pages widened from `maxWidth: 700` to `880` (the narrow two-column Guest/Stay cards read as mostly empty space at 700).

**Folio** — running charge ledger per booking (room charges, F&B, sales, activities), settled at checkout. Tab on `BookingDetail.jsx`. Backed by `routes/folio.js` / migration `028`. The folio money math (`loadFolio` — subtotal → service charge → tax → total → balance_due) lives in `server/services/folioService.js` (extracted in migration 042's pass so `agentBillingService` can reuse it); `routes/folio.js` keeps only the HTTP handlers + the PDF renderer. **0% rates (2026-09-24):** a property that publishes all-in rates sets Tax Rate / Service Charge to 0 in Property Details; the invoice/pro forma PDFs and the Folio / Master Folio tabs then omit the Service Charge and Tax lines (and the Subtotal when both are 0) instead of printing "(0%) Rp 0". `GET /api/folio/group/:groupId` now also returns `service_charge_rate`/`tax_rate`.

**Pro Forma Invoice + Estimated Balance Due — implemented 2026-09-18.** Closes a real gap: `loadFolio`'s "Invoice" PDF and the Folio tab's "Balance Due" only ever reflect what's *actually posted* to `folio_charges` — but room/F&B charges only post via nightly audit or the checkout catch-up (`roomChargeService.postStay`), never at check-in. So a guest asking for an invoice (or staff just glancing at the Folio tab) right after check-in saw close to nothing owed, and a guest who'd only paid a deposit could see a **negative** Balance Due (0 posted charges − a real received payment) — confirmed live against a real booking (`793e206f`, `deposit_paid`, `total_amount` 1,110,000, deposit 333,000 received, folio empty ⇒ Balance Due showed **-333,000** instead of the real 777,000 owed).
- New `folioService.computeProforma(bookingId, propertyId)` — projects every stay night's room/fnb charge using the exact same per-night split real posting uses (`stayNights`/`nightlyAmount`, moved from `roomChargeService` into `folioService` so both share one implementation — `roomChargeService` now imports them back), so an already-posted night and a not-yet-posted night project to the identical amount. Folds in any real non-room/fnb charges (extra sales/activities/misc) and nets out real payments. Never writes to `folio_charges` — pure projection.
- `GET /api/folio/:bookingId/proforma` (PDF, line-item-per-night) and `GET /api/folio/:bookingId/estimate` (same thing as JSON) sit alongside the existing `GET /api/folio/:bookingId/invoice`. `GET /api/folio/group/:groupId/proforma` (PDF) does the same per room + a grand total, mirroring the existing `GET /api/folio/group/:groupId` JSON rollup. `routes/folio.js`'s PDF drawing was refactored into shared `drawDocumentHeader`/`drawChargeTable` helpers so the invoice, single pro forma, and group pro forma PDFs don't triplicate the pdfkit layout code.
- `BookingDetail.jsx`'s Folio tab now shows an **"Estimated Balance Due"** summary (from `/estimate`) above the itemized ledger, and the ledger's own totals are relabeled **"Posted Balance"** with a note that it's ledger-only — so the two numbers can never be confused for each other again.
- **Unposted-nights line (2026-09-24):** under Posted Balance, the Folio tab now says which room/meal nights haven't posted yet and how much (e.g. "🌙 Tonight's room charge — Rp 1.050.000 — posts at night audit … Estimated Balance Due above already includes it"), computed client-side as the estimate's projected room/fnb nights whose `service_date` has no posted charge (grossed up with the estimate's tax/service rates). Raised after a checked-in, fully-paid guest showed a green "Posted Balance -Rp 800.000" before the night's charge had posted — not a real credit.
- Deliberately not built: a standalone "receipt" document — the pro forma/invoice PDFs already list "Payments Received," which covers proving a deposit was paid. Group-level deposit proration already works for free (each child booking's `deposit_amount`/`payments` rows are its own, so its own pro forma/invoice is already correct).

**Guest Communication** — email templates editor in Settings, per-property SMTP config (falls back to platform-default SMTP env vars if a property hasn't configured its own). Backed by `routes/communications.js` / migrations `030`–`031`. **SMTP fail-over hardened 2026-09-21**: the property-vs-platform decision now lives in one shared `services/mailer.js` `resolveSmtp(ps)` helper (used by `sendBookingEmail`, `sendGroupBookingEmail`, and Night Audit's owner email below) instead of being duplicated inline. A property only counts as having "its own SMTP" if `smtp_host`/`smtp_user`/`smtp_password` are **all** present — the Settings form doesn't require the 4 SMTP fields together, so a partially-filled config (e.g. host set, password left blank) used to be treated as "configured," attempted, and fail outright instead of falling back to `PLATFORM_SMTP_*`. Now any incomplete property config correctly falls through to the platform default, and `from` always matches whichever account is actually authenticating (a mismatched custom `smtp_from` sent through the platform's server would look like spoofing to most providers). **Two more real bugs caught live testing against Zahill's actual Titan Email account (2026-09-21):** (1) `secure` was hardcoded `false` regardless of port — fine for STARTTLS (587/25), but port 465 is implicit-TLS (SMTPS) and needs `secure:true`, otherwise the connection hangs until timeout with "Greeting never received"; `resolveSmtp()` now sets `secure: port === 465`. (2) `PATCH /api/settings/property` accepted any string for `smtp_from` with no format check — Zahill's was saved as a bare display name with no email address (`"Zahill Resort Kintamani"`, no `<...>`), which is invalid RFC 5322 and every real mail server rejects it at send time (caught live: Titan's `550 5.7.1 Invalid From address`); the route now 400s if `smtp_from` doesn't contain an actual email address.

**Reports & Alerts (Smart Reports) — started 2026-09-25, migration 068.** One page, Settings → Reports & Alerts (`client/src/pages/SettingsReports.jsx`, owner-only, every property, nav under Settings · Property), for everything sent to staff outside the app. `REPORTS` in `services/smartReports.js` has two kinds: **instant alerts** (`type:'alert'`, Telegram only, **free**) — `alert_new_booking` (new booking) and `alert_guest_requests` (Clean Room, Do Not Disturb, room calling the desk), fired by `telegramService.sendAlert(propertyId, alertKey, message)` from the same four call sites as before, now sent only to Telegram recipients with that alert ticked — and **scheduled reports** (`type:'scheduled'`, Telegram or email, **paid**: the `smart_reports` module, default off) — the page shows them locked with 🔒 when off, Preview still works (sales pitch), ticking / Send test are refused server-side. The old Telegram Notifications card on Email & Communication is replaced by a pointer here; migration 068 copies old `telegram_chats` onto the list. `reportAllowed(key, {channel, moduleEnabled})` is the one check (channel + add-on); only NEWLY ticked reports are checked on update, so a recipient can still be paused/edited after the add-on is switched off; changing a recipient's channel drops reports the new channel can't carry. **Which bot:** each property can use its **own Telegram bot** (so messages come from e.g. @ZahillResortBot) — the owner pastes the @BotFather token in the page's "Telegram bot" card (`GET/PUT/DELETE /api/smart-reports/bot`; PUT checks the token with getMe and refuses a bot that has a webhook or is already used by another property; the token is never sent back to the browser, only the @username and last 4 characters). No own bot → the platform bot (`TELEGRAM_BOT_TOKEN` env) as before. `telegramService.botTokenFor(propertyId)` is the one rule; `sendToChat(token, chatId, text)` now takes the token. Changing or removing a property's bot gives every Telegram recipient of that property a fresh Connect link (a new bot can't message people who pressed Start on the old one). `telegramLink.js` runs one getUpdates loop per bot that has a pending link. Recipients are the property's **own list** — any email or Telegram chat, with or without a PMS login (owner, manager, accountant, a front-desk/kitchen group), each with a role and a tick list of reports; a new recipient gets the reports whose `defaultRoles` include their role. Deliberately separate from `telegram_chats` (operational alerts) and the night-audit owner email — both unchanged. `server/services/smartReports.js`: `REPORTS` catalog, each report built ONCE per property then rendered per channel (Telegram HTML parse mode, short; email HTML with KPI tiles + tables), delivery via `telegramService.sendToChat` (new, per-chat, returns `{ok,error}`; `sendAlert` now uses it) or nodemailer + `resolveSmtp`, result stored on the recipient (`last_sent_at`/`last_error`, shown on the page). `runReportAllProperties(key)` only touches active properties with the module on. Settings page: report catalog with **Preview** (today's real figures, Telegram + email tabs), recipients with per-report tick boxes, **Send test**, Pause/Remove; **Telegram recipients connect themselves with a Connect link** (no Chat ID, no @userinfobot): adding a Telegram recipient without a Chat ID creates a 7-day link `t.me/<bot>?start=<token>` (or `?startgroup=` to add the bot to a group), shown on the page as a QR code + Send on WhatsApp / Copy / Open in Telegram; when they press Start the bot gets `/start <token>` and `services/telegramLink.js` stores the chat ID + name, clears the token and replies "Connected"; the page refreshes every 4 s while a link is pending. `POST /recipients/:id/link` makes a new link (expired, or Reconnect to move to another phone/group). Receiving is **long-polling `getUpdates` only while an unexpired link exists** (started on server boot and whenever a link is made) — no webhook, so no public URL/nginx setup; caveat: one bot token can be polled by only one server at a time, and not while a webhook is set. Typing a Chat ID stays available as a fallback. The bot's @username comes from `telegramService.getBotUsername` (getMe). Reports carry **no links / call-to-action buttons into the PMS** (removed 2026-09-25 — many recipients, e.g. a kitchen group or an accountant, have no PMS login); the email footer is just "Sent by Smart Reports." **Built so far: Morning Brief** (daily 07:00 WITA) — arrivals / in-house / departing (rooms + pax), tonight's occupancy (vs rooms not out of order), breakfast (+dinner) count, money to collect from guests leaving, needs-attention (not clean with a guest arriving, overdue, late arrivals, out of order, other dirty rooms — >3 of a kind collapse to one line on Telegram), arrival special requests. Built on `loadGuestLists`/`loadBalanceDue` (now also exported from `routes/bookings.js`), so it always matches those pages. **Owner control alerts** (`alert_owner_control`, instant, Telegram, **paid**, ticked by default for owners; `services/ownerAlerts.js` `sendControlAlert`, fire-and-forget; `sendAlert` skips paid alerts when the add-on is off): 💰 **Edit Price** (any change, old → new, overpaid credit), 🎁/🏷 **Change Room** upgrade given free or below the normal difference (downgrades don't alert), 🎁/🏷 **Amend Dates** when the normal difference is positive and less was charged, ⚠️ **check-in without full payment** (single room; a group check-in sends ONE message listing its pay-later rooms — `checkinOneBooking` now returns `pay_later_unpaid`). Each message: what happened, room · guest · dates, who did it (name + role), and the reason (no link). Verified with a throwaway booking against the real dev bot (price edit, free upgrade, discounted extension, complimentary downgrade = no alert, pay-later check-in), then cleaned up. **Daily Close** (`daily_close`, scheduled 00:30 WITA — after the 00:05 night audit — for the day that just ended, paid, default owner + manager; `services/dailyClose.js`): revenue room / meals / extras + total, occupancy (rooms sold ÷ rooms not out of order), ADR, RevPAR — each vs the same weekday a week earlier — all from `routes/reports.js`'s `getReport()` (now exported), so it always matches the Reports page; money received that day by payment method (`payments` received that WITA day + extras paid directly with no payment row + `agent_payments`; a pay-now extra is counted once via its incidental payment); new bookings made that day (count, nights, **net** value — `room_revenue + fnb_revenue`, after discount, before service/tax, same basis as the revenue figures; Weekly's booking pace uses the same; the email footer says amounts are net — list), cancellations (status cancelled, `updated_at` that day — no cancelled_at column), no-shows (check-in that day); and the next day's arrivals/departures. `sendReport`/`renderPreview` now pass `buildOptions` (e.g. `{ date }`) to a report's builder. It also carries what the night-audit owner email had — guests still checked in past check-out, the new day's arrivals (with notes), and balances to collect from guests leaving (the Balance Due page's whole-stay figure, agent-billed stays left out) — and **`jobs/nightAudit.js` skips its owner email when `dailyClose.dailyCloseReplacesAuditEmail(propertyId)`** is true (add-on on + at least one active, connected recipient with Daily Close ticked); otherwise the night-audit email sends exactly as before. The housekeeping-tasks-created count was deliberately not carried over (it's on the Operations board). **Tomorrow Preview** (`tomorrow_preview`, scheduled 19:00 WITA for the next day, paid, default manager + front desk + kitchen; `services/tomorrowPreview.js`): arrivals (rooms/pax, groups 👥, type, nights, rate plan, bed setup, special requests), departures, staying over, next-night occupancy, breakfast that morning + dinner that night (Guest Lists' meal rule), balances to collect from guests leaving (Balance Due figure, agent-billed left out), and **Rooms to get ready** — each arrival whose room is out of order, a same-day turnover (current guest leaves that morning) or still dirty. Built on `loadGuestLists`/`loadBalanceDue` for that date. **Weekly Owner Report** (`weekly_owner`, Monday 08:00 WITA for last Mon–Sun, paid, default owner; `services/weeklyOwner.js`): last week vs the week before from `getReport()` (revenue split, occupancy = room-nights ÷ (rooms not out of order × 7), ADR, RevPAR, expenses + net income only when expenses exist), **already booked for the next 14 / 30 days** (occupancy + revenue on the books, from non-cancelled bookings per night) with **weak nights** (next 14 under 30%, capped to a count + 7 when many), booking pace (bookings made / value / cancellations vs the week before), stays by source with share of stay revenue, and what agents owe + overdue from `agentStatementService.aging()` (same as Agent Billing). **Monthly Report** (`monthly_report`, the 1st 08:00 WITA for last month, paid, default owner; `services/monthlyReport.js`): last month vs the month before and the same month last year (only when last year has data) — revenue split, expenses + net income when used, occupancy, room-nights, ADR, RevPAR — from `getReport()`, plus money received by method (`dailyClose.collected(propertyId, from, to)`, now range-capable), stays by source with share, best day, and agents owed (aging). **The email attaches** a one-page PDF summary (pdfkit, shared compact header), the revenue CSV (`routes/reports.js` `revenueCsv(data)`, now shared with Reports → Export CSV) and, when there are any, the expenses CSV (`routes/expenses.js` `expensesCsv(propertyId, from, to)`, shared SELECT/row builder with the Expenses export) — ready for the accountant (add them as an email recipient, role Other, and tick Monthly Report). A report's `email()` may return `attachments` (nodemailer format); `deliver()` passes them on; `renderPreview` returns only their filenames. Telegram gets a short summary pointing to the email. **Email summary cards** are shared (`services/emailCards.js`: `card()`, `CARD_TABLE_OPEN/CLOSE`, `CARD_HEIGHT` none/one/two) by all five report emails and the night-audit owner email — each card is a `<td>` with a fixed height per email in a border-spacing table, **two cards per row** (three was too narrow on phones — values spilled out), so cards are equal-sized in every mail client. Report emails are sent as a full HTML document (`emailDocument()`) whose `<head>` carries `MOBILE_STYLE` (smaller numbers/padding under 520px, via the `hk-card`/`hk-val`/`hk-wrap` classes); the night-audit email includes the same style in its own `<head>`. To preview at phone width, render inside a 390px iframe — desktop Chrome can't draw a window narrower than ~500px. **All planned reports are now built** (see the table in `ROADMAP.md`). Verified via HTTP against the dev DB (non-owner 403, validation, duplicate 409, default reports by role + channel + add-on, paid report refused with the add-on off and allowed with it on, alert refused on email, pause after the add-on is switched off, channel switch, alert recipient selection, migration copy of an old `telegram_chats` row, real test email sent via property SMTP, preview, cleanup); Connect-link handling tested with simulated Start updates (person, group incl. `/start@bot`, expired link refused, same chat twice → refused); Telegram delivery and the real bot round-trip not tested (no bot token on the dev machine); page not clicked through in a browser.

**Night Audit's owner-facing summary email — fixed 2026-09-21 (real multi-tenancy bug).** `sendAuditEmail()` in `jobs/nightAudit.js` (separate from the guest-facing pre-arrival/post-checkout emails above) used to send from a single hardcoded personal Gmail account (`GMAIL_USER`/`GMAIL_APP_PASSWORD`) to one hardcoded `OWNER_EMAIL`, with a literally hardcoded `[Zahill]` subject — so with Birdnest active alongside Zahill, Birdnest's nightly audit would land in the same inbox mislabeled as Zahill's. Now per-property: uses the same `resolveSmtp()` fail-over as guest emails above, sends to every `role='owner'` user of that property (not a single global address), and the subject/header use that property's own name. Skips gracefully (logged, not thrown) if a property has no owner account with an email on file, or if neither its own SMTP nor `PLATFORM_SMTP_*` is configured.

**Telegram Notifications — implemented 2026-09-13, migration 057. ⚠️ Superseded 2026-09-25 by Reports & Alerts (migration 068, below): alerts now go to `notification_recipients` rows with the matching alert ticked (`sendAlert(propertyId, alertKey, message)`), chats connect via a Connect link instead of a Chat ID, and the Settings card + `/api/settings/telegram-chats` endpoints are gone. The history below is kept for context.** Internal staff/owner alerts (not guest-facing) — until this, no channel reached staff *outside* the app at all; email is guest-only, WhatsApp is still an unbuilt Open Decision, and SSE only pushes to a browser tab someone happens to have open. One shared **platform-wide bot** (`TELEGRAM_BOT_TOKEN` env var — same philosophy as `PLATFORM_SMTP_*`, one platform credential rather than per-property bot setup), `server/services/telegramService.js`'s `sendAlert(propertyId, message)` looks up every active `telegram_chats` row for that property and POSTs to Telegram's `sendMessage` endpoint via native `fetch` (same house pattern as `googlePlaces.js`'s outbound calls — no new dependency) for each, swallowing per-chat errors internally so one bad/blocked chat never stops the others, and no-ops entirely if `TELEGRAM_BOT_TOKEN` isn't set. **Chat linking is deliberately the simplest possible v1**: a staff member gets their own numeric Chat ID from `@userinfobot` on Telegram and pastes it into a new "Telegram Notifications" card on `SettingsCommunications.jsx` (`GET/POST/DELETE /api/settings/telegram-chats`, owner-only) — no bot webhook, no `/start` command parsing. Upgrading later to a polished "Connect Telegram" self-linking flow only ever means adding a second way to populate the same `telegram_chats` table; the send-side logic never changes. Multiple chat IDs per property from day one (e.g. the owner's phone *and* a shared front-desk group both get every alert). No new module — gracefully absent for any property that hasn't linked a chat, same as SMTP. **Three trigger points wired**, each a one-line fire-and-forget call (`.catch(() => {})`) right next to existing logic at that exact spot: `routes/bookings.js`'s `POST /` (new booking — the existing unit/guest validation queries there gained `name` to their `SELECT` for a readable message, since those rows were already being fetched), `routes/display.js`'s `POST /room/:roomId/housekeeping` (both the `type==='clean'` and `type==='dnd'` branches), and `routes/calls.js`'s room-to-staff call creation (Call Front Desk). Verified twice over: first with a fake/invalid token (confirmed per-chat failure doesn't throw, and `is_active` correctly excludes an inactive chat) and all three wired endpoints returning 201 normally with the new call in place, then **live with a real bot and the owner's own Telegram** — created a real bot via @BotFather, confirmed the alert arrives, and confirmed the `POST /room/:roomId/housekeeping` "Clean Room" trigger delivers a real alert end-to-end. **Caught a real bug during this live test**: the same chat had been linked twice (once via the Settings UI, once via a test API call) with nothing stopping it, so the alert arrived twice — fixed with migration 058's `UNIQUE (property_id, chat_id)` constraint, and `POST /telegram-chats` now returns a clean 409 on a duplicate instead of a raw 500 (same `err.code === '23505'` convention `payment-methods` already uses in this file).

**Per-property branding** — logo, brand color, and contact/invoice fields, set from the superadmin `PropertyDetail.jsx` "Branding" card (see Superadmin section above for the full trace). Rendered via `SettingsContext.jsx`'s `branding` value in the client nav, and via `/login/:slug` + an optional slug field on `Login.jsx` pre-auth. Backed by migration `032`.

**Group Bookings** (Phase D #9) — multiple room bookings under one guest + shared date range (villa/event groups), with a group-level discount/deposit prorated across child bookings, a master folio rollup, and best-effort group check-in. Backed by migration `033` (`reservation_groups` table, nullable `bookings.reservation_group_id`). `NewBooking.jsx`'s "+ Add Another Room" is the only new UI surface for *creating* one — a single-room submission still goes through the original `POST /api/bookings` untouched, and only 2+ rooms route to the new `POST /api/bookings/group`. New page `GroupDetail.jsx` (`/reservations/group/:groupId`) shows the room list, payment rollup, a "Check In Whole Group" action (`POST /api/checkin/group/:groupId/start`, partial-success — inspect the response body, not just the HTTP status), "Cancel Group", and a Master Folio tab (`GET /api/folio/group/:groupId`, aggregates each room's own folio — `folio_charges` still always posts per-room). `BookingDetail.jsx` shows a "Part of a group booking" banner when relevant; `CheckIn.jsx`/`QuickCheckIn.jsx` cluster same-group arrivals under one header. No new module — all new endpoints live inside the existing `bookings`/`checkin`/`folio` route files, so they inherit `reservations`/`front_desk`/`financial` gating for free. Multi-room groups get one combined confirmation email (new `group_booking_confirmed` template trigger, editable in Settings → Communications) instead of one per room.

**Concierge / Activities** (Phase D #11) — a schedulable, capacity-limited, staff-confirmed booking concept for tours/transport/wellness, deliberately separate from two pre-existing, unrelated concepts: `products.category='tour'` (a flat instant-purchase POS item, unchanged) and `guest_board_cards.category='activity'` (static "things to do" info cards on the Guest Board CMS — see the linking note below, this is no longer fully unrelated). New module `activities` (default on), backed by migration 037 (`activities`, `activity_bookings`). New page `client/src/pages/Activities.jsx` (`/activities`) with Catalog (owner-only CRUD, any-staff read — same convention as `products.js`) and Bookings tabs (date/status filters, confirm/complete/cancel/no-show actions, a revenue summary card for owners). Requests always start at `status='requested'`, never auto-confirmed, since capacity/guide/vehicle availability can't be verified from a tablet the way a drink order can; staff desk bookings (from `Activities.jsx`) skip straight to `confirmed`. Revenue tracking: confirming a `room_charge` booking posts a real `folio_charges` row (`type='activity'`, already a valid value from migration 028 — no folio migration needed) via `server/services/activityBookingService.js`, and cancelling/no-showing a confirmed booking voids it. (Historical note: at the time this was built, `salesService.createSale` had the same gap — a room-charge sale never posted to the folio either — and fixing that was out of scope here. Migration 049 later closed that gap for sales too; see the Resto Ordering section.) `BookingDetail.jsx` shows a read-only "Activities" tab for that booking's activity bookings. **Guests never browse the raw `activities` catalog directly on Room Display** — see the linking note below for how they actually reach a booking.

**Link a staff desk activity booking to an existing reservation — implemented 2026-09-22.** `Activities.jsx`'s "New Activity Booking" modal always had a free-text Guest Name field with a "Walk-up guest" placeholder — real gap: the backend (`activity_bookings.booking_id`, `activityBookingService.createBooking`) already fully supported linking to a real stay and posting a `room_charge` to its folio, but the client never sent `booking_id`, so staff had no way to book an activity for an in-house/upcoming guest without re-typing their name by hand and losing the folio link entirely. Fixed client-only, no migration — reused two already-existing endpoints: `GET /api/bookings?q=` (the same guest-name search `Reservations.jsx` already uses, same 300ms-debounce pattern) now also powers an optional reservation picker in the modal, filtered client-side to `pending`/`deposit_paid`/`confirmed`/`checked_in` (excludes `cancelled`/`no_show`/`checked_out`) so staff can book ahead of arrival, not just for guests already checked in. Picking a result sets `booking_id` and clears the free-text `guest_name`/`guest_phone` (matching the DB convention that those are walk-up-only fields); the Payment Method dropdown only reveals "Room Charge" once a reservation is linked, since that option is meaningless — and previously would have silently posted nothing — without a `booking_id` behind it. Leaving the picker empty still books a walk-up guest exactly as before. Verified against the live dev DB: search returns real bookings, a linked `room_charge` booking posts a real `folio_charges` row visible on that guest's Folio tab, and the bookings list's existing `room_guest_name || guest_name` display picks up the real linked name. Test rows cleaned up afterward.

**`activity_bookings.payment_method` joins the dynamic payment-methods system — implemented 2026-09-22, migration 064.** Raised while discussing the reservation-linking change above: activity bookings could only ever be paid `cash`/`qris`/`room_charge` (a hardcoded DB `CHECK` from migration 037) — a property that renamed or added its own payment methods in Settings (e.g. Bank Transfer, Wise) had no way to select them for an activity, even though `sales.payment_method` got the exact same fix back in migration 048. Same treatment applied here: migration 064 drops the `CHECK` and widens the column to `VARCHAR(50)` (identical `DO $$ ... pg_get_constraintdef ...` dynamic-constraint-lookup pattern as 048); `activityBookingService.createBooking` gained the same validation choke point `salesService.createSale` already has — `room_charge` requires a `booking_id` (previously silently accepted with no folio posting if missing; now a real `400 ROOM_CHARGE_REQUIRES_BOOKING` error), anything else is checked against the property's real `payment_methods` table (`400 INVALID_PAYMENT_METHOD` if not found/inactive). Both `routes/activities.js` and `routes/display.js`'s guest self-booking endpoint updated to surface any coded service error as a 400 rather than the previous blanket 404. `Activities.jsx`'s Payment Method dropdown now renders the property's real active `payment_methods` (via `useSettings()`, same context `Settings.jsx`/`BackOffice.jsx` already use) instead of two hardcoded options, defaulting to the property's first active method rather than a hardcoded `'cash'`. Verified against the live dev DB: a previously-unselectable real method (`bank_transfer`) now creates a booking successfully; an invalid method and a booking-less `room_charge` both now 400 with the right error code. Test rows cleaned up afterward.

**Activity payment receipt — implemented 2026-09-22.** Raised directly: an activity paid for directly (cash/QRIS/etc, not Room Charge) had no printable proof of payment at all — the guest Invoice/Pro Forma (`routes/folio.js`) only ever carries activity charges billed as Room Charge, so a directly-paid activity was invisible to every existing PDF. New `GET /api/activities/bookings/:id/receipt` (plain `auth`, no new module) + `server/services/activityReceiptPdf.js` — reuses the same `drawDocumentHeader` *and* the same Guest/Stay-block-then-line-item-table structure the room Invoice/Pro Forma already use (Guest/Activity info blocks, a Description/Qty/Unit Price/Amount table with identical column positions, a bold Total Paid line, a Payments-Received-style row, Notes) — first pass was a flat label:value list and got reworked to actually match the Invoice's structure once seen printed. No fabricated tax/service-charge lines, since activity pricing has no tax model in this codebase. Caught and fixed a real pdfkit bug live: after the table's explicitly-positioned cells, `doc.x` was left at the last column's x instead of resetting to the left margin, so the Notes section rendered shifted into the right margin and got clipped — fixed with an explicit `doc.x = 50` reset before continuing. Deliberately titled **"Receipt"**, not "Invoice" — a receipt confirms money already received for one transaction, distinct from the room stay's own Invoice (what's been charged, which can still be unpaid). `Activities.jsx`'s Bookings table gained a "🖨 Receipt" button per row, shown only when `payment_method` is set, isn't `room_charge`, and the booking isn't `cancelled`/`no_show` — a room-charged activity already has its own paper trail on the room's Invoice/Pro Forma, so this deliberately doesn't offer a second document for it. Verified against the live dev DB: a real walk-up QRIS booking (guest name, phone, pickup location, notes) produced a correctly branded PDF with the real payment-method label resolved from the property's `payment_methods` table. Test rows cleaned up afterward.

**Guest Board ↔ Activities linking** — closes the gap above: Guest Board and Activities shipped as two disconnected systems, meaning staff had to enter the same tour twice with no link between the promotional card and the bookable catalog row, *and* guests saw two separate, confusingly-both-labeled "Activities" menus on Room Display (the Explore promotional carousel and a standalone bookable-catalog nav tab). Fixed both problems in one pass: **Guest Board is now the sole guest-facing entry point for activities** — the standalone catalog-browsing nav tab is gone; an activity with no linked Guest Board card simply isn't reachable by guests (staff must link/create a card to expose it).

- Migration 038 adds a nullable `guest_board_cards.activity_id` FK. `activities` stays the sole owner of commerce fields (price/duration/capacity) — `server/services/activitiesService.js` holds the one `createActivity()`/`setAvailability()` write path, shared by `routes/activities.js`'s `POST /` and the Guest Board paid-card flow below.
- `SettingsBoardCards.jsx` gets a Free/Paid toggle (gated on `hasModule('activities')`). Going Paid on an unlinked card offers **two** paths: **create new** (one field, price — auto-creates the activity from the card's title/body) or **link an existing, not-yet-linked activity** (a dropdown of `GET /api/activities` rows not already claimed by another card — closes the gap where an activity made directly in the Catalog tab, e.g. legacy pre-linking data, had no way to become guest-visible without being duplicated). `routes/board.js`'s `linkOrCreateActivity()` helper handles both, rejecting a `link_activity_id` already claimed by another card (409). Once linked, price is **read-only** with a "Manage full details →" link into `/activities` — no further commerce-field editing from Guest Board, ever.
- Going back to Free (or deleting a linked card) never deletes the `activities` row — it sets `is_available = false` and clears `activity_id`, since `activity_bookings` has an `ON DELETE RESTRICT` FK to it once there's booking history.
- `routes/board.js`'s `GET /` and `routes/display.js`'s `GET /room/:roomId/state` both gained a `LEFT JOIN activities`, with the display endpoint only surfacing `activity_id`/`activity_price` while the linked activity is still available — a deactivated link silently stops showing a booking CTA without touching the card.
- Room Display: a linked+available Guest Board card shows price + a "Book Now" CTA (`ExploreTab.jsx`) that opens `BookActivityTab.jsx` as a focused single-activity booking panel (`GuestScreen.jsx`'s `preselectedActivityId` state — set on tap, cleared on "Back"), overlaid on top of whatever tab was showing. `BookActivityTab` no longer has its own sidebar nav entry and no longer shows a catalog grid — it only ever renders the one preselected activity's booking form.

---

## In-Room Display Hardware

Each unit can have **two displays** with distinct, complementary roles, plus two property-wide kiosk/guest apps (not per-unit): Kitchen Display and Resto Ordering.

**Native-app track — `ROOM_DISPLAY_KIOSK_PLAN.md` at the repo root. Phase 1 built 2026-09-11** (migration 052); Phases 2–4 not started.
- **`room-display-kiosk/`** — new Android project, greenfield, toolchain mirrors `tv-screensaver/` exactly (AGP 8.3.2 / Kotlin 1.9.25 / Gradle 8.7 / compileSdk 35 / minSdk 24 / JDK 17; package `com.zahill.roomdisplay`). `MainActivity` = fullscreen landscape WebView of the Room Display PWA + `window.AndroidKiosk` JS bridge (`isKiosk`/`getConfig`/`getDeviceStats`/`openSettings`) + `mediaPlaybackRequiresUserGesture=false` + `onReceivedError` "Reconnecting…" overlay + a 5-tap bottom-right corner → `SettingsActivity` (Room ID / display token / URL, same SharedPreferences pattern as tv-screensaver's `MainActivity`). `TelemetryScheduler` = plain `ScheduledExecutorService` + `HttpURLConnection` (zero new Gradle deps), POSTs every 2 min + on battery/connectivity change while the activity is started (Dashboard marks a tablet offline after ~6 min = 3 missed). **No Device Owner / Lock Task / boot receiver yet — Phase 2.** APK builds with JDK 21 + auto-downloaded android-35 (`bash build.sh` or `./gradlew :app:assembleDebug`); verified running on a Samsung Galaxy Tab A9 / Android 16 (telemetry reaches the Dashboard, PWA loads fullscreen, kiosk chip shows).
- **Voice calls in the kiosk app — fixed 2026-09-25 (APK 0.2.0, versionCode 2).** Phase 1 shipped without microphone support: no `RECORD_AUDIO` in the manifest and no `WebChromeClient.onPermissionRequest`, so the WebView refused every `getUserMedia` → "Permission denied" on every room↔desk call (FO rang but could never connect; answering on the tablet failed). Now: `RECORD_AUDIO` + `MODIFY_AUDIO_SETTINGS`, the Android mic permission is asked once at startup, and `KioskChromeClient` grants `RESOURCE_AUDIO_CAPTURE` only to the Room Display's own host (everything else denied). Build: `./gradlew :app:assembleDebug` with Android Studio's JBR on the PATH (`build.sh` strips PATH and fails; a corrupted `~/.gradle/caches/transforms-4` was cleared once). Room Display side (`room-display/src/callClient.js` `acquireMic()`, `App.jsx`): the microphone is taken BEFORE placing or answering a call — a blocked mic now shows a readable message (6 s) and never rings the desk; a failed place/answer also ends the call on the server (`end-from-room`) so FO isn't left ringing / on "Connecting…".
- **Calls through the TURN relay fixed — 2026-09-25.** Phone on mobile data ↔ tablet on hotel WiFi never connected (same-WiFi calls did). coturn was fine (port **34780**, relay 49152–49452 on the public IP, auth OK); its `peer usage` log showed the caller's relay sending checks (`sp>0, rp=0`) and the answerer's relay receiving them but never replying. Cause: both `callClient.js` files' `createAnswer()` reset `pendingCandidates = []`, discarding the caller's ICE candidates queued while ringing — a TURN relay drops packets from peers it has no permission for, so only the direct (same-LAN) path ever worked. Fixed (queue kept; `close()` resets it). Same pass: room-display `acquireMic()` was calling itself instead of `getUserMedia` (every tablet call would fail as "mic blocked"); either side failing now also ends the call on the server (the other side no longer hangs on "Connecting…"); the connecting timeout starts as soon as a call is answered; the staff app checks the microphone before answering/placing a call and shows "Microphone blocked" instead of freezing.
- **`POST /api/display/room/:roomId/telemetry`** (`routes/display.js`, `authDisplay`, **no `moduleGuard`** — device health is always-on like `GET /state`). Whitelisted+clamped body fields (`TELEMETRY_FIELDS` map at the top of the file), dynamic-column upsert into `room_display_devices` `ON CONFLICT (property_id, controller_id) DO UPDATE` so a partial payload never nulls unsent columns. Empty body still bumps `last_seen_at` (the heartbeat).
- **`room_display_devices`** (migration 052) — `property_id`-scoped, keyed `(property_id, controller_id)`, ~21 nullable telemetry columns (battery/power/network/wifi/storage/uptime/versions) + `last_seen_at`. Not a gated module.
- **PMS Dashboard** — `routes/dashboard.js` occupancy query `LEFT JOIN room_display_devices` → `tablet_*` columns; `client/src/pages/Dashboard.jsx` `tabletHealth()`/`wifiBars()`/`relTime()` helpers, `UnitTile` 📵/⚠️/🔋 badge (`.unit-tile-badge.warn`), `UnitCard` "Room tablet" section. "Offline" = `tablet_last_seen_at` > ~6 min. Its "Remove tablet" button (`DELETE /api/units/:id/tablet`) is **dismiss-only, not a device sign-out** — it just deletes that unit's `room_display_devices` row; it never contacts the physical tablet. If the tablet is still powered on, connected, and configured for that Room ID, its next telemetry heartbeat (~2 min) silently re-creates the row via the `POST /telemetry` upsert above, with no indication to staff that it came back on its own. The button's own label ("Remove tablet (moved / retired)") signals the intended usage — click it only after the tablet has already been physically moved or reset. A real remote sign-out is feasible (push an `unlinked` event down the existing per-room SSE channel to clear the PWA's `localStorage.roomId`/`displayToken`) for a browser-based Room Display, but would need a native APK bridge method too (to clear its SharedPreferences) to survive a kiosk tablet's next reboot — not built; deferred by the owner as of 2026-09-14.
- **`room-display/` PWA** — `src/kiosk.js` (bridge hooks + `bootstrapKiosk()` in `main.jsx` before React: seeds `roomId`/`displayToken` from `?room=&token=`, strips the URL, then — when `isKiosk()` — **force-overrides `localStorage.roomId` from `window.AndroidKiosk.getConfig().roomId`** so the PWA can't drift from what the APK posts telemetry for). `KioskChip.jsx` (battery/wifi chip in the Idle/Guest sidebars, self-hides in a plain browser). `DebugMenu.jsx` — Device section + in kiosk mode the "Change Room ID" field is replaced with a read-only value + "Kiosk Settings" button (`window.AndroidKiosk.openSettings()`). **The room ID lives in two places on a kiosk tablet: the APK's SharedPreferences (authoritative — drives telemetry + launch URL, change via 5-tap bottom-right → Settings) and the PWA's `localStorage` (display only, now always forced to match the APK).**
- Phase 4 (TV launcher) is the **only** thing that will ever touch `tv-screensaver/` — deferred until the tablet APK is stable. See `ROADMAP.md` for the summary.

### 1. Room Display
**Primary purpose:** Device control — relay toggles, RGB LED, AC via IR blaster
**Secondary:** Shows guest name and stay dates at a glance; guest self-ordering (see below); housekeeping marks the room clean after checkout from the idle screen (see below)

- Calls `GET /api/display/room/:roomId/state` (via `authDisplay` middleware, per-property `display_token` — not a staff JWT)
- Three screens: `SetupScreen` (first-time config), `IdleScreen` (vacant), `GuestScreen` (occupied)
- Room ID and display token stored in localStorage on the device
- Debug menu triggered by 5 rapid taps
- Stack: React/Vite PWA (`room-display/`)
- **Guest self-ordering** (extends the `sales` module, see `ROADMAP.md`): when occupied and the property's `sales` module is on (`state.orderingEnabled`), `GuestScreen` shows an "Order Food" tab (`OrderFoodTab.jsx`) listing the full product catalog. `GET /api/display/room/:roomId/menu` + `POST /api/display/room/:roomId/order` (both `authDisplay` + `moduleGuard('sales')`) create a `room_charge` / `room_service` sale against the room's current `checked_in` booking — prices are always looked up server-side, never trusted from the tablet. **Unlike QR/dine-in and staff-entered orders, a room-service order does not fire straight to the kitchen** — it lands in a pre-kitchen "pending" bucket (`sales.confirmation_status='pending'`) that a Resto Ordering staff member must confirm first (see the Resto Ordering section below for why: nobody at the property has looked at a room-service order the way a waiter would a table order). `YourOrdersTab.jsx` shows "Awaiting confirmation" / "Declined" accordingly. Sale creation itself lives in `server/services/salesService.js`, shared with the staff POS (`routes/sales.js`) and the Resto Ordering app.
- **Guest activity booking** (extends the `activities` module — see the Guest Board ↔ Activities linking write-up above for the full picture): reached only via "Book Now" on a linked Guest Board card, never a standalone catalog tab — `BookActivityTab.jsx` renders as a focused single-activity panel. Unlike food ordering, a guest request always starts as `status='requested'` — staff must confirm before it's guaranteed or billed.
- **Your Orders** — closes the gap where a placed food order or activity request had nowhere to go after its 4–5 second confirmation toast. `GET /api/display/room/:roomId/orders` (`authDisplay`, not module-gated — it's just the guest's own current-stay data) returns the room's active booking's `sales`/`sale_items` (`order_type='room_service'`, with `kitchen_status`) and `activity_bookings` (with `status`) in one call. `GuestScreen.jsx` polls it every 15s and also refetches immediately after a successful order/booking (`onOrderPlaced`/`onBooked` callbacks from `OrderFoodTab.jsx`/`BookActivityTab.jsx`). A new "Your Orders" nav entry (`YourOrdersTab.jsx`) appears only once there's at least one order/booking for the stay — same conditional-visibility convention as the Explore tabs — with a small pulsing dot while anything is still in flight (food not yet `served`, activity not yet past `requested`/`confirmed`). Positioned in its own divided-off section directly above the call-front-desk button, separate from the Controls/Order Food group and the Explore tabs group above it.
- **Room-to-desk calling (two-way)** — real voice calls (WebRTC, audio-only; STUN + **self-hosted TURN relay** as of 2026-09-06, see the TURN write-up below — cross-network calls now work where coturn is deployed) between a room's tablet and staff, gated by its own `calling` module (default **on**, see below — not `room_controller`). One `calls` table, `direction` column (`room_to_staff` default / `staff_to_room`, migration 039) distinguishing who placed it, signaled over Server-Sent Events per room (`room:{controllerId}` channel) and per property for staff (`staff:broadcast:{propertyId}` channel) — `routes/calls.js`'s `/signal` and `/signal-from-room` are direction-agnostic relays (just forward whatever offer/answer/ICE payload to the other side), so adding the reverse direction only needed two new endpoints: `POST /to-room` (staff places a call, auth-gated, picks any unit with a `controller_id` assigned) and `POST /:id/answer-from-room` (guest answers). A 45s ring timeout auto-marks a call `missed` either direction. Both `room-display/src/callClient.js` and `client/src/services/callClient.js` are now symmetric — each can play offerer (`createOffer`) or answerer (`createAnswer`) role, since which side initiates now varies. Staff trigger an outgoing call from a "Call a Room" entry in `Sidebar.jsx` (visible whenever `calling` is on, not owner-gated — any staff can already answer an incoming call, so any staff can place one) → `CallRoomModal.jsx` lists units with a controller assigned (deliberately doesn't show online/offline — `GET /api/iot/units`'s `connected` field is the ESP32 controller's MQTT status, not the Room Display tablet's, and there's no tracked presence signal for the tablet app itself; an unanswered call just rings out and gets marked missed, same as the reverse direction already did). On the room side, `CallOverlay.jsx` gained an `'incoming'` status (red ringing UI, Answer/Decline) reached via a new `incoming_call_from_staff` SSE event in `App.jsx`. Room Display's own `CallButton` (both `GuestScreen.jsx` and `IdleScreen.jsx`) is gated by a new `callingEnabled` flag on `GET /room/:roomId/state` — hidden entirely when the module's off, rather than showing a button that 403s.
- **`controller_id` (Room ID) assignment is deliberately NOT gated behind `room_controller`**, even though it lives on the same `units` row the module's relay/RGB/IR features use. Room Display/TV Display/Kitchen Display/calling all key off `controller_id` regardless of whether a property has ESP32 hardware — a hardware-less client still needs to assign a Room ID to get any guest-facing display working at all. `PUT /api/units/:id` (always-on route) accepts `controller_id` directly, and `/units` → `UnitSettings.jsx` has a "Room ID" field, so it's settable with zero dependency on the module. `PUT /api/iot/units/:unitId/controller` (gated, `SettingsRoomControllers.jsx`) still exists too and writes the same column — harmless duplication, kept for hardware-owning clients configuring relay labels at the same time. **Migration 045 went further**: `controller_id` now *defaults to the room name* on `POST /api/units` (so a display-only property never hand-types Room IDs — they're the room number by default, still overridable), and its uniqueness is finally scoped `(controller_id, property_id)` instead of global (two properties can now both have a room "101"; column widened to `varchar(32)`). Separately, `GET /room/:roomId/state` gained a `roomControllerEnabled` flag (mirroring `orderingEnabled`/`activitiesEnabled`) so Room Display's relay/RGB/IR "Controls" tab (`GuestScreen.jsx`, `IdleScreen.jsx`) actually hides when the module is off — previously it showed unconditionally regardless of the toggle, a real bug caught in production testing.
- **Calling split into its own module (migration 040), same day as the above.** Calling was originally gated by `room_controller` purely because it also keys off `controller_id` — it has zero actual ESP32/MQTT dependency. That meant a hardware-less property couldn't get calling either, hit for real in production (`moduleGuard` 403 "Module not enabled for this property" on "Call Front Desk"). New standalone `calling` module (default **on**, unlike `room_controller`) — `server/modules.js`'s `room_controller` entry now only gates `iot`; `routes/calls.js`'s `const gate = moduleGuard(...)` points at `'calling'`. Rewired every place that checked `hasModule('room_controller')` for calling specifically: `Sidebar.jsx`'s "Call a Room" entry, `BookingDetail.jsx`'s "Call Room" action, Room Display's `callingEnabled` flag (above). `PropertyDetail.jsx`'s `MODULE_LABELS` and `seedPropertyDefaults.js` (which defaults every module to enabled except `room_controller` by name, so `calling` needed no special-casing) picked it up for free. The Room Controllers settings page/link (`SettingsRoomControllers.jsx`, `/settings/room-controllers`) is unaffected — still correctly gated by `room_controller`, since relay/RGB/IR config is genuinely hardware-specific.
- **Calling reliability fix (2026-08-20, commit 28d0c30).** Calls used to hang on "Connecting…" forever whenever WebRTC ICE negotiation failed or stalled — `onConnectionStateChange` only reacted to `'connected'`, and an incoming call's offer arriving late was silently ignored. Both `room-display/src/App.jsx` and `client/src/context/CallContext.jsx` now: treat `failed`/`disconnected`/`closed` as a hangup, enforce a 25s connecting timeout, and poll up to 5s for a late-arriving offer instead of no-opping — surfacing a clear "Could not connect" message either side (`CallOverlay.jsx` / `CallBanner.jsx`) instead of hanging indefinitely. Also added `room-display/src/ringtone.js` (ported from the staff app's `services/ringtone.js`, same synthesized Web Audio beep) — Room Display previously had no ringtone at all on an incoming staff call, only the staff side did.
- **Outgoing ringback / dial tone (2026-09-09).** Both `ringtone.js` files (`client/src/services/` + `room-display/src/`, kept byte-identical) gained `startDial()`/`stopDial()` — a single 440Hz tone, 1s on / 2s off, with its **own** AudioContext + interval separate from the incoming-ring state so an incoming ring and an outgoing ringback can never silence each other (same reasoning as `alarm.js` being independent). Wired to the `'calling'` state on each side: `room-display/src/App.jsx`'s call effect starts it while `callState.status === 'calling'` (guest calling front desk, before pickup); `client/src/context/CallContext.jsx` starts it while `activeCall?.status === 'calling'` (staff calling a room via `callRoom`, before the room answers). Stops on `'connecting'`/`'connected'`/`'failed'`/`'idle'`. Incoming ring behaviour unchanged. Net: both apps now play a dial tone when placing a call and a ring tone when receiving one.
- **Self-hosted TURN relay (2026-09-06, commit 764e92a + follow-ups) — the "STUN-only, calls fail 4 in 5 attempts" gap is now addressed in code.** Was deferred ("not now", 2026-08-20), then built when it kept biting. `server/services/turnCredentials.js` generates HMAC time-limited TURN credentials (not a static password baked into the JS bundle); `routes/calls.js` serves them; both `callClient.js` files (`client/src/services/` + `room-display/src/`) add the TURN entry to `ICE_SERVERS`. New TURN env vars in `server/.env.example` — assumes `coturn` running on the same cloud VM (port 3478 UDP/TCP + a relay port range in the firewall). Follow-up fixes the same week: `5022513` lengthens the connecting timeout so relay candidates get tried, `c6ca705` fixes ICE candidates silently dropped when they arrived before the `RTCPeerConnection` existed, `bec2967` stops `useResilientEventSource` forcing an SSE reconnect on every tab/screen wake. **Deploy check:** the code alone doesn't stand up coturn — verify it's installed + running on the VM and the TURN env vars are set in `server/.env`. Whether it fully fixed the on-property WiFi flakiness is not yet confirmed live.
  - **Relay-only ICE reverted (2026-09-09).** `bfbb8f4` had forced `iceTransportPolicy: 'relay'` in both `callClient.js` files whenever `/turn-credentials` returned a non-empty list — the intent was to skip the flaky host/srflx path, but the server only knows the credential *fetch* succeeded, not that coturn is actually *reachable*. Result: with TURN configured in `server/.env`, **every** call (same-LAN included) was forced through coturn, and any coturn misconfig (firewall relay port range, `external-ip`, service down) broke calling entirely with no fallback path. Caught after the property's MikroTik was removed and same-router room→desk calls still wouldn't connect. Both files back to `iceTransportPolicy: 'all'` (TURN servers still in the `iceServers` list as a real cross-NAT fallback) — direct path tried first, relay only when needed. The original "relay tried last, browser gives up first" symptom was really the candidate-drop bug that `c6ca705` fixed separately, plus the 45s timeout.
- **`CallRoomModal.jsx` was fetching from the wrong endpoint (2026-09-05) — real bug, calling was completely broken from the Sidebar.** It listed rooms via `GET /api/iot/units`, which is gated behind the `room_controller` module — off by default, and with zero actual relation to calling (that's exactly why `calling` was split into its own module in the first place, see above). On every hardware-less property the fetch just 403'd, silently caught, leaving "No rooms have a controller assigned yet." Fixed to use the always-on `GET /api/units` instead (same `controller_id` field). While in there, replaced the single scrolling list-row-with-a-Call-button (unusable past a handful of rooms — 35 rows to hunt through on Zahill) with a grouped tile grid, same visual language as the Dashboard's Live Unit Status board, plus a search box that filters live — tapping a tile calls directly.
- **Dashboard: Call Room from Live Unit Status (2026-09-05).** `UnitCard` (the room detail popover) gained a "📞 Call Room" button — same eligibility as the Sidebar modal (`calling` module on + `unit.controller_id` assigned), reusing `useCall()`'s `callRoom()`. Needed `u.controller_id` added to `routes/dashboard.js`'s occupancy query (wasn't selected before).
- **Fixed a real "busy" bug: a second incoming call could hijack a busy staff member's screen and silently drop their first call.** `CallContext.jsx`'s SSE handler `setIncomingCall(...)` unconditionally on every `incoming_call` broadcast — even for a staff member already mid-call. `CallBanner.jsx` renders `incomingCall` with priority over `activeCall`, so their screen got hijacked by the new prompt, and tapping Answer reused the single `callClient` WebRTC connection, killing the first call with no clean hangup (its DB row never got `ended_at`). Fixed: the SSE handler now ignores a new `incoming_call` entirely when `activeCallRef.current` is set (mirrors a guard `room-display/src/App.jsx` already had — `if (callIdRef.current) return`), and ignores it if there's already an unanswered incoming call pending (first one wins). `answerCall()` got the same guard as defense-in-depth. Verified live: staff answers Room 202, Room 201 calls in immediately after, staff's screen stays on the Room 202 "Connecting…" banner with no interruption.
  - **Deliberately not built (discussed, deferred):** an API-level "busy" rejection (reject a new call outright when no staff is free) needs real staff-presence tracking that doesn't exist yet — a naive version ("busy" = any call currently `answered`) would be *wrong* for any property with 2+ staff, since it'd reject calls even when another staff member is free. The existing broadcast-to-every-staff-session + first-to-answer-wins (DB-level `WHERE status='ringing'` guard) already routes correctly when anyone's free; only revisit this if a single/short-staffed shift missing calls turns out to matter in practice.
  - **Also discussed, deferred:** room-to-room calling (a guest calling another room directly). Feasible — `callClient.js` is already symmetric, each room has its own SSE channel — but `calls` only models one room + a `direction` (no second room slot), and open any-room-to-any-room dialing is a real guest-privacy concern most hotels avoid. If built, scope it to rooms sharing a `reservation_group_id` (existing Group Bookings feature) rather than open dialing. User is thinking it over — don't build unprompted.
- **Guest Messages — front desk sends a message to a room's tablet (migration 046, 2026-09-05).** Manual send only, deliberately — discussed automatic triggers (checkout reminder, order-ready) first and scoped this pass down to manual, since it's the smaller/provable-first step. New `guest_messages` table (`unit_id`, `booking_id` nullable, `body`, `sent_by` nullable so an automated trigger can reuse the same row shape later, `read_at`) — kept **separate** from the email/SMTP Guest Communication system (that's for formal comms — confirmations, invoices — and would be heavy machinery for a quick on-screen nudge). Single write path in `server/services/guestMessageService.js` (`sendMessage()`, inserts + `sse.notify()`s the room's existing state-stream channel so it arrives instantly, same channel relay/order/call updates already ride) — written this way specifically so a future scheduled trigger has one obvious function to call, not a second insert path to keep in sync.
  - **Staff side:** `POST /api/bookings/:id/message` (new, in `routes/bookings.js`) → "Send Message" in `BookingDetail.jsx`'s action menu (next to Call Room) and a matching shortcut in the Dashboard's Live Unit Status room popover (`UnitCard` in `Dashboard.jsx`, gated on `unit.booking_id` — no point messaging an empty room's tablet). Both open the same modal shape with two quick-fill template buttons (🔑 Check-in Welcome / 🧳 Check-out Reminder) sourced from `client/src/lib/messageTemplates.js` — one shared file so the two entry points can't drift in wording, and the natural place to pull from once an automated trigger exists (that would still need its own server-side copy of the wording — the sharing here is just client-side, entry-point-to-entry-point).
  - **Room Display side:** `GET /room/:roomId/state` gained a `message` field — the single oldest **unread** message for that unit (`ORDER BY created_at ASC LIMIT 1`, so a burst of sends is served in order, never skipped). Shown via new `MessageOverlay.jsx`, full-screen and must-dismiss (matches the incoming-call/alarm treatment, not a passive banner — deliberate choice after discussing the tradeoff: an interruption every time vs. easy-to-miss). `POST /api/display/room/:roomId/message/:messageId/dismiss` (`authDisplay`) marks it read; next poll/SSE picks up the next unread one if any. Also plays a one-shot notification chime (`room-display/src/notification.js`) the instant a *new* message id shows up — a guest not looking at the tablet would otherwise have no way to notice a silent full-screen overlay appeared. Deliberately not a loop like `ringtone.js`/`alarm.js` (those need an active response and keep going until handled; a message doesn't). `App.jsx` tracks the last-seen message id in a ref and only plays on a genuine change, so repeated 10s polls of the same still-unread message don't replay it — verified live (spied on `AudioContext` construction: fires exactly once when a new message arrives, silent across 20+s of subsequent polling of the same message).
  - **Deliberately not built yet:** any automated trigger. Checkout reminder (scheduled job, same pattern as night-audit/guest-comm cron) and order-ready (hook into Kitchen Display's `PATCH /api/kitchen/:id/status` → `served`) were both discussed as natural first candidates — revisit once manual sending has been used for a while and the wording/timing preferences are clearer.
- **Connectivity resilience pass for calls (2026-09-05) — raised directly: "this is going to my first client, I want it to work perfectly."** Every SSE stream in the app (`onerror = () => {}`, on all three: Room Display's state stream, its calls stream, and the staff calls stream) had zero defense against a connection dying silently — a mobile network handoff or a router dropping an idle NAT mapping doesn't always fire a clean `onerror`, so the app could just stop receiving pushes with nothing telling it anything was wrong. Messages already had a safety net (the state poll independently re-fetches `message` every 10s regardless of SSE health) — calls didn't, since the calls SSE stream was the *only* way either side learned about one. Fixed with two layers, deliberately both rather than picking one:
  - **Hardened the SSE connections themselves** — new `useResilientEventSource` hook (one copy in each app, `room-display/src/useResilientEventSource.js` / `client/src/hooks/useResilientEventSource.js`, no shared package between the two apps so it's duplicated on purpose). Server heartbeats changed from an SSE *comment* (`: heartbeat\n\n`, invisible to `onmessage` per spec) to a real `data: {"type":"heartbeat"}` event every 25s on all three streams, so the client can actually tell they're arriving. The hook tracks time-since-last-event and force-reconnects (capped exponential backoff) if nothing — message or heartbeat — arrives for 40s, on any `onerror`, on the tab becoming visible again, and on the browser's `online` event. All three SSE call sites now go through this hook instead of a raw `new EventSource(...)`.
  - **Added a polling fallback for calls, mirroring the guarantee messages already had** — `GET /room/:roomId/state` gained an `incomingCall` field (oldest `ringing` `staff_to_room` call for that unit); new `GET /api/calls/pending` (staff, property-wide) gives the same for `room_to_staff`. `App.jsx`'s existing 10s state poll and a new 10s poll in `CallContext.jsx` adopt a call the poll found that SSE hasn't already delivered (guarded by `callIdRef`/`incomingCallRef` so it can't double-fire once SSE does catch up). This means an incoming call is now guaranteed to surface within ~10s even if the SSE layer fails completely, not just "probably fine."
  - **Verified, not just written:** for both directions, inserted a `ringing` call row directly into the database with **no** SSE push sent at all, then confirmed the polling fallback alone surfaced the incoming-call UI within the poll window — proving the guarantee actually holds, not just that the code compiles. Also confirmed normal SSE push still delivers in ~30-40ms after the hook refactor (a message sent for real arrived near-instantly), so the hardening didn't regress the common-case speed.
  - **Known residual risk:** this bounds the failure to "noticed within ~10-40s," not zero-latency-always. WebRTC signaling *during* an already-answered call (offer/answer/ICE exchange) is still SSE-only with no polling equivalent — a connection that dies mid-call still relies on the existing 25s `CONNECTING_TIMEOUT_MS` cutoff to surface as "Could not connect" rather than hanging forever, not on this pass's fallback. Good enough for "the guest/staff always finds out a call is happening," not a guarantee the call itself never drops.
- **Guest Screen landing tab fixed (2026-08-20, same commit as above).** `GuestScreen.jsx` used to default to whichever of Controls/Order Food/first Explore tab existed (`roomControllerEnabled ? 'controls' : orderingEnabled ? 'order' : ...`) — meaning a property with `room_controller` off (the default) landed every guest straight on "Order Food" first, with no stay info anywhere prominent. Added an always-visible "Home" tab (first in the nav, not gated by any module) that's now the actual default landing tab — shows the existing `StayPanel` (guest name, room, dates) plus a new `HomeWelcome` component (clock, date, weather). The old Controls tab no longer duplicates `StayPanel` next to the relay/RGB/IR panel, since Home now owns that.
- **Theme token system + per-property accent + auto day/night (2026-09-04, "Option 1").** Every color in `room-display/` was a hardcoded literal (gold `#c9a227`, `bg-bg-dark`, `text-slate-*`, "Kintamani · Bali", …) — one shared deployment couldn't flex per property. Replaced ~127 literals across 17 files with semantic CSS variables (`--accent`, `--bg`, `--surface`, `--text`, `--text-muted`, …) resolved from one of two token sets in `src/index.css` — **dark** and **warm-paper light** — selected by `data-theme` on `<html>`. `src/theme.js`: `startThemeWatcher()` (called from `main.jsx`) auto-picks light 06:00–18:00 / dark otherwise, re-checked each minute; DebugMenu has an Auto/Light/Dark override (device-local `localStorage`, same exception as the stored room id/token); `applyAccent(brandColor)` (called from `App.jsx` when `state.property.brand_color` changes) sets `--accent`/`--accent-rgb`/`--accent-contrast`, the contrast colour luminance-derived so text-on-accent stays readable for any brand hue. `GET /display/room/:roomId/state`'s `property` object gained `location` (from `property_settings.property_address`) — the old "Kintamani · Bali" literal now comes from there, hidden when unset. Semantic Tailwind-v4 utilities (`.bg-app`, `.text-ink`, `.text-accent`, `.accent-tint`, …) live in an `@layer utilities` block; `glass-card` is now theme-aware. **No per-client code edits for branding** — it's all the `brand_color` field + the module flags. The Front Desk call button deliberately stays danger-red (not accent) — it's a call-for-help affordance. PWA splash (`vite.config.js` `theme_color`) stays dark (OS-level, can't be per-theme). A per-property theme override (`auto`/`light`/`dark`) and a real featured-event schedule field are noted for a later "Option 2" `display_config` pass.
- **Sidebar + layout rework (2026-09-04, same pass).** Sidebar nav is vertically centred in the rail (was top-aligned), active item is a raised accent-tinted card (inset ring + soft glow) rather than a hairline bar. Labels: Home→**Stay**, Order Food→**Dining**; explore tabs → **Activities / Venues / Resort**; Controls stays a nav item. New `TopBar` on `GuestScreen` (date + live wifi/wifi_off connection dot, fed by a new `online` prop threaded from `App.jsx`'s `error` state). New `EveningHighlight` floating card promotes the soonest bookable activity-linked Guest Board card (`cards.find(category==='activity' && activity_id)`), "Reserve" reuses the existing `handleBookActivity` flow — soft "this evening" label, no schedule-time data yet. `HomeWelcome` greets with property name + location.
- **`TopBar` weather merge + connectivity icon fix (2026-09-05).** `TopBar`'s date/wifi row and `ExploreTab`'s separate Today/Tomorrow weather strip read as two disconnected rows stacked under each other — merged the weather into `TopBar` itself (`ExploreTab`'s now-redundant `WeatherStrip` removed). The connectivity icon changed `wifi`/`wifi_off` → `cloud_done`/`cloud_off`: it actually reflects whether the last `GET /room/:roomId/state` poll succeeded (`App.jsx`'s `error` state), not the tablet's real WiFi radio — the wifi icon implied the wrong thing (a tablet could have WiFi but a dead backend and still show disconnected, or vice versa near a poll boundary). Sidebar footer's live clock (`Clock.jsx`, now deleted — it had no other callers) replaced with the build commit hash (`__APP_COMMIT__`, same stamp `DebugMenu.jsx` already showed) so the footer states which build is actually running, matching what `git status` on the dev machine can't tell you from across the room.
- **`StayPanel` unit card redesign + Quick Actions (2026-09-05).** The room/property "unit card" was a flat `bg-surface` rectangle with a dead-center ghost `home` icon at 10% opacity — reworked with an accent-tinted diagonal gradient, a soft corner glow + rotated `villa` watermark (positioned top-right, *not* bottom — the bottom is where the scrim gradient needed for text legibility is nearly opaque, so a first pass placing it there rendered invisible), a glowing pill badge, and centered/enlarged room-number + property-name (all per direct visual iteration).
  Below the stay-duration card there was a large dead empty space in the panel — filled it with a new `QuickActions.jsx`, and evened out the whole panel's spacing to one consistent gap (`gap-3`→`gap-5` after a "still feels tight" pass) with `overflow-y-auto` added as a safety net (the panel can now scroll rather than clip if a shorter physical screen can't fit every card).
  - **Call Front Desk** moved here from the sidebar (was a small icon-only button, now full-width and labeled) — sidebar footer, once vacated, is where the commit-hash change above landed.
  - **Do Not Disturb** / **Clean Room** — guest self-service housekeeping, both posting to a new `POST /api/display/room/:roomId/housekeeping` (`authDisplay` + a new `operationsEnabled` module gate mirroring `orderingEnabled`/`activitiesEnabled`/`roomControllerEnabled`/`callingEnabled` on `GET /room/:roomId/state`), which inserts a real `tasks` row (`type='guest_request'`) — the same table/kanban `routes/tasks.js`/`Operations.jsx` already use, no new table. Clean Room is one-shot (priority `medium`, staff mark it done whenever). DND is a toggle (priority `high`): the open task's id round-trips through `localStorage` scoped **per room** (`dndTaskId:{roomId}` — flat, unscoped key was the first pass and a real bug, see below) so the guest can cancel it later. **Turning DND off is always effective immediately in the UI regardless of the server response** — the first version awaited the cancel call and only cleared local state on success, so a stale/mismatched task id (e.g. from a debug-menu room change) 404'd and left the toggle permanently stuck "on" with no guest-facing recovery path. Root-caused and fixed live this session.
  - **DND is modeled as a room *state*, not a completable task** — deliberate, raised by the property owner mid-build. It still rides the `tasks` table for storage convenience (no schema change), but the Dashboard's guest-request banner (below) explicitly excludes it (`isDndTask()` title-prefix check in `Dashboard.jsx`) since there's nothing for staff to "mark done"; it only ever shows as passive status (Live Unit Status tile badge + popover pill, below) and clears itself the moment the guest toggles it off.
  - **Alarm** — fully client-side, zero backend involvement, deliberately labeled "Alarm" not "Wake-up Call" (the latter implies front desk is placing a call, which isn't what this is). Guest sets a time via `AlarmTimePicker.jsx` — large circular tap-up/tap-down steppers (56px targets) for hour/minute + AM/PM buttons in a modal, replacing the native `<input type="time">` dropdown whose scroll-wheel picker was too fiddly for a touchscreen kiosk. Rings once via a dedicated synthesized chime (`alarm.js` — deliberately independent module-level state from `ringtone.js`'s call-ring so an alarm and an incoming call can never share state and silence each other) through a full-screen `AlarmOverlay.jsx`, then auto-disables — one-shot like a real hotel wake-up-call request, not a recurring daily alarm. State (`alarmTime`/`alarmEnabled`/`alarmRinging`) lives in `App.jsx` alongside `callState`, same pattern, so it fires regardless of which Stay-panel tab is active and survives a reload via `localStorage` (`alarmTime`/`alarmEnabled` — device-local, same exception as `roomId`/`displayToken`).
- **Dashboard: live guest-request visibility (2026-09-05).** Two additions to `client/src/pages/Dashboard.jsx`, prompted by realizing `Operations.jsx` has no polling/SSE at all (loads tasks once on mount) — a guest's DND/Clean Room tap could otherwise sit invisible until someone happened to reload that page:
  - A dedicated amber banner ("N guest requests need attention") above the existing holiday/arrivals banners, listing each open **Clean Room** task (DND excluded, see above) with an inline "Mark Done" (`PUT /api/tasks/:id`) — rides the Dashboard's poll (`api.get('/api/tasks')`, already fetched for the "Today's Activity" card), no new endpoint. Poll interval is **10s** (dropped from 30s 2026-09-09 so Room Display housekeeping/unit-status changes surface fast) and is **paused while the tab is hidden**, with an immediate refetch on `visibilitychange` — deliberately kept a poll rather than adding a Dashboard SSE stream (single-tenant hotel scale, self-healing, no held connections; SSE was judged not worth the extra surface area for a status that changes a few times an hour).
  - **Live Unit Status** tiles gained a per-state icon (🛏/🔑/✓/🔧/🔒 — color was previously the *only* signal, an accessibility/legibility gap) plus a small corner badge (🔕 DND / 🧹 Clean) on any room with an open request; the click-through popover (`UnitCard`) shows the same as labeled pills. Both the banner and the tiles read the same `guestRequests` fetch, matched to units by `unit_id`.
- **Housekeeping room status — checkout-cleaning flow (2026-09-09, migration 051).** Front desk previously could not tell a just-vacated uncleaned room from a ready one — checkout set `units.status='available'` immediately, and housekeeping lived only as a `tasks` kanban card with no effect on room state. Added `units.housekeeping_status` (`clean`/`dirty`; `inspected` reserved in the CHECK, unused) as a **second dimension kept fully separate from `units.status`** — a room can be `available` AND `dirty`. Folding it into `units.status` would have broken every `WHERE status='occupied'`/`'available'` query. Scope (per owner): 2 states only, no attribution/login, checkout-cleaning only (no stayover/daily service), dirty check-in warns not blocks.
  - **Backend:** checkout (`routes/checkin.js`) sets `housekeeping_status='dirty'` alongside `status='available'` (the auto housekeeping task still fires — it's the assignable work order, the status is the quick flag). `POST /api/display/room/:roomId/housekeeping` gained `type:'mark_clean'`/`'mark_dirty'` (still `opsGate`) → sets the column, closes any open `type='housekeeping'` task for the unit, `sse.notify(controllerId,{type:'housekeeping'})`. New `PATCH /api/units/:id/housekeeping` (`{status}`) — plain `auth`, **deliberately not owner-gated like `PUT /api/units/:id`** since clearing a room is routine front-desk work; same task-close + SSE side effects. `GET /display/room/:roomId/state` returns `unit.housekeeping_status`; `dashboard/summary` occupancy query + `bookings/today/arrivals` both carry it now.
  - **Room Display:** `IdleScreen`'s `IdleView` shows a `HousekeepingCard` (amber, replaces the "Ready for Guests" pill) when `operationsEnabled && housekeeping_status==='dirty'` — **two-step: "Mark Room Clean" → confirm "…fully cleaned and ready for the next guest?" → Yes/Cancel** (a stray touch can't flip the room), then a brief "Room marked clean" ack before the parent state refetch flips it back to the normal idle screen. `App.jsx` threads `operationsEnabled`/`housekeepingStatus`/`onMarkClean` (POSTs `mark_clean`, then `fetchState()`). `GuestScreen` untouched (no stayover tracking).
  - **PMS client:** Dashboard Live Unit Status board recoloured to the standard housekeeping-board scheme and reordered: **Available 🟢 green / Occupied 🔴 red / To clean 🟡 yellow (dark text — `TILE_FG` override, white is illegible on yellow) / Arriving 🔵 blue / Departing 🟣 purple / Maint · blocked ⚪ grey** (`Guest request` alert dot stays pink `#DB2777`). Occupied took red so `maintenance` dropped to grey (same as `blocked`). New **`'departing'` tile state** — an occupied room whose `nights_left <= 0` (checkout day); `dashboard.js` occupancy query already returns `nights_left`. New `'dirty'` state (vacant + `housekeeping_status='dirty'`) wins over `available`/`arriving`. "To clean" strip count split out of "Available". `UnitCard` card tint/border + status-line colours follow the same palette; "🧳 Departing today" replaces "● Occupied · last night" on the last day. `UnitCard` shows "🧹 Vacant — needs cleaning" + an inline **Mark clean** (→ confirm → `PATCH /api/units/:id/housekeeping`), a small "Flag for cleaning" link on ready rooms for the reverse, and calls `onChanged` (threaded `UnitStatusBoard`→`UnitPopover`→`UnitCard`, wired to the Dashboard's `load()`). `CheckIn.jsx` arrivals list shows an amber "🧹 Room not yet cleaned" line (non-blocking).
  - **Live Unit Status board rework (2026-09-09)** for per-type visibility + many-room legibility (`Dashboard.jsx` `UnitStatusBoard`): a whole-property proportional `.unit-overview-bar` on top; the legend row is now **click-to-filter** chips (`STATUS_META` drives colour/label/order; clicking a status narrows every tile grid to that status, "Show all ✕" clears); each room-type section got a `.unit-type-head` (name + total) plus a `.unit-type-counts` row of per-status count chips (so "how many Villa are occupied" is visible without expanding). All counts (`countByState` / `bucketOf`) now come from `tileState` so per-type sums equal the overall — `arrivals`/`departures` props dropped from `UnitStatusBoard` (the Arriving/Departing *banners* elsewhere still use those arrays). Tile render extracted to `<UnitTile>`.
- **Room "Out of Order" status (2026-09-14, migration 059).** `units.status`'s `maintenance`/`blocked` pair had no documented difference between the two, no reason or date was ever captured, and `maintenance` already meant two other things in this codebase (`tasks.type='maintenance'`, `expenses.category='maintenance'`) — the room *status* being also called "maintenance" was an accidental overload, not a deliberate distinction. Checked against real hotel PMS convention (Opera/Mews/Cloudbeds/RMS): **"Out of Order" is the standard term** for a room pulled from sellable inventory for a fault, so `status` is now `available`/`occupied`/`out_of_order` — one status, with the specifics captured in a free-text reason rather than pre-encoding every possible cause as its own status value. No known property had any unit set to `maintenance`/`blocked` at migration time; the UPDATE backfill is a safety net, not a real data migration.
  - **Backend:** new `units.status_reason`/`status_expected_back`/`status_updated_at`/`status_updated_by` columns. New `PATCH /api/units/:id/status` (`{status:'out_of_order'|'available', reason?, expected_back?}`) — plain `auth`, **deliberately not owner-gated** (same reasoning as `/housekeeping`: a broken AC is usually noticed and needs acting on by front desk/housekeeping, not the owner). `out_of_order` requires a non-empty `reason` and opens a `type='maintenance'` task (the existing Operations task type, not a new one) so it's tracked as real work; returning to `available` closes that task, mirroring the housekeeping clean/dirty flow. **Guarded both directions** since `status` is a single column shared with occupancy tracking: marking a currently-`occupied` room Out of Order is rejected (409) rather than silently erasing that a guest is checked in there — a maintenance issue in an occupied room goes through a regular Operations task instead, untouched by this endpoint; likewise `available` is rejected unless the room is currently `out_of_order` (can't be used as a generic status reset). `dashboard.js`'s occupancy query carries the new columns plus `status_updated_by_name` (`LEFT JOIN users`).
  - **Dashboard:** `UnitCard`'s Out of Order state went from a single static sentence to a real info+action panel — reason, expected-back date, "since {time} · {name}", and a two-step "Return to Service" confirm (mirrors "Mark clean"). Available rooms gained a "Mark Out of Order" link (next to "Flag for cleaning") opening a small modal (reason textarea + optional date, same shape as the existing Send Message modal) — `PATCH .../status` requires the reason non-empty client-side too. `TILE_BG`/`TILE_ICON`/`STATUS_META`/`tileState`/`bucketOf` all collapsed their old `maintenance`+`blocked` pair into one `out_of_order` key (`bucketOf` is now just `tileState` directly — no more fold needed since there's only one state to fold).
  - **`UnitSettings.jsx`/`Allotment.jsx`** updated to the new 3-value enum (`available`/`occupied`/`out_of_order`) for consistency, though the primary way to set Out of Order (with a reason) is the Dashboard popup, not the Unit Settings status dropdown.

### 2. TV Welcome Display
**Only purpose:** Welcome guests and show their stay details when TV is idle. No device controls — purely guest-facing ambient display.

- Android TV screensaver (DreamService APK, `tv-screensaver/`, package `com.zahill.tvscreensaver`) wraps a fullscreen WebView loading `tv-display/`
- Screensaver launches after TV idle; any remote button press exits back to normal TV
- APK sideloaded per TV (no Play Store); Room ID configured once at install (SharedPreferences)
- Landscape only, large text, no touch interaction
- Calls the same `GET /api/display/room/:roomId/state` endpoint as Room Display
  - Returns: `unit.name`, `booking.guest_name`, `booking.check_in_date`, `booking.check_out_date`, `booking.num_guests`, `property.{name,logo_url,brand_color}`
  - Returns `booking: null` when vacant → branded idle screen

Both displays render the property's own logo/name from that `property` field (falling back to the bundled default asset/text when a property hasn't uploaded one) — see the Superadmin Property Branding trace above.

### 3. Kitchen Display
**Only purpose:** Kitchen ticket board — New/Preparing/Ready columns, tap to advance a ticket. One kiosk per kitchen station, not per-unit.

- Originally shipped as a page inside the PMS client (`/kitchen`, staff-JWT-gated); extracted into its own standalone kiosk app (`kitchen-display/`) to match the Room Display / TV Display pattern — same `display_token` device-auth model instead of staff login, so a kitchen tablet doesn't need a staff account.
- `GET /api/kitchen/active` (ticket board data), `PATCH /api/kitchen/:id/status` (advance a ticket), `GET /api/kitchen/stream` (SSE push on `kitchen:{propertyId}`) — all `authDisplay`, all gated `moduleGuard('sales')` per-route. `authDisplay` reads the token from either the `Authorization` header or `?token=`, so the same middleware covers the SSE endpoint (EventSource can't set custom headers) without a separate query-token middleware. `GET /active` excludes any sale still `confirmation_status='pending'` (a not-yet-confirmed Room Display room-service order — see Resto Ordering below); everything else (QR/dine-in, staff-entered, and confirmed room-service orders) behaves exactly as before.
- Setup screen accepts the same per-property `display_token` used by Room Display and TV Display (surfaced in `SettingsProperty.jsx`'s "Device Setup" card, owner-only, `GET /api/settings/display-token`).
- Stack: React/Vite PWA (`kitchen-display/`), dev port 5177, CORS origin via `KITCHEN_URL` env var. Deployed at `kitchen.zahill.kdai.cloud` per `nginx/kitchen.conf` — kept on the existing `zahill.kdai.cloud` domain for now rather than the planned `half.kdai.cloud` (see "What this is" above); the rename stays deferred until every app is confirmed working on the current domain.
- Removed from the PMS client as part of this extraction: `pages/KitchenDisplay.jsx`, `context/KitchenTicketsContext.jsx`, the `/kitchen` route, the `kitchen_display` menu key (`SettingsRoles.jsx`) and its nav links (`Sidebar.jsx`/`BottomNav`). Staff no longer need a menu permission for it — it's a separate device, not a PMS page.

### 4. Resto Ordering
**Purpose:** Restaurant ordering — a guest QR self-order surface per table, plus a floor-staff app for table-side order-taking, a room-service confirm queue, and table/session management. New paid add-on module `resto_ordering` (default **off**, migration 048), own standalone app `resto-display/` (matches the Kitchen Display extraction precedent — a separate deployable so it can be sold per-property independent of the base `sales` module).

- **Why a confirmation step exists at all, and why it's narrow:** industry research (Toast/Lightspeed) confirmed real POS/KDS systems don't gate orders behind human approval before firing to the kitchen for either self-order or staff-entered orders — their "Expo" role checks completeness *after* prep, not before. The one place a gate earns its keep here is **Room Display's room-service orders specifically** — unlike a table order a waiter is standing next to, nobody at the property has looked at a room-service order before it's submitted. So only `order_source='room_display'` orders land as `sales.confirmation_status='pending'`; QR self-order and staff-entered orders fire straight to `kitchen_status='new'` like before.
- **Two auth models, deliberately different from the shared `display_token`:**
  - Guest QR self-order (`resto-display/`'s `/t/:qrToken` screen) uses a new **per-table** credential — `restaurant_tables.qr_token`, validated by `server/middleware/authTableQR.js`, resolving only to `{propertyId, tableId}`. This is *not* the property-wide `display_token` Room/TV/Kitchen Display share — printing that shared secret on a table (a public surface) would let anyone who scans it reach unrelated endpoints for arbitrary rooms. A table's QR is regenerable independently (`POST /api/resto/tables/:id/qr/reset`, owner-only) without touching any other table or device.
  - Floor staff (`resto-display/`'s `/staff/*` screens) reuse the **existing PMS `users`/`roles`/JWT login as-is** — no new auth system. A restricted role (e.g. `resto_staff`, `allowed_menus: ['resto_take_order']`) limits a shared tablet to only the Take Order screen, via a local reimplementation of the PMS's `allowed_menus`/`RequireMenu` pattern (`resto-display/src/auth.js` — can't import `client/`'s `AuthContext` across separate apps). Shared/generic logins are fine if a client doesn't want per-waiter accounts; the only cost is losing per-order attribution on `sales.served_by`. **Menu-key gating is client-side only for navigation** (same as the PMS: the JWT + `resto_ordering` module gate is all any `/api/resto/*` route requires) — **except for two routes that got real server-side enforcement, added after the client explicitly said the resto domain should own its own menu/tables rather than needing a PMS owner login**: `POST/PUT/PATCH /api/products` and `POST/PUT/DELETE /api/tables` now accept either the `owner` role **or** a non-owner whose `allowed_menus` includes `resto_menu`/`resto_tables` respectively (`server/middleware/requireOwnerOrMenu.js`). This required adding `allowed_menus` to the JWT payload itself (`routes/auth.js`, previously only in the login response body, not the token — same re-login-to-take-effect staleness the client already accepted). Because `products`/`tables` are shared with the PMS Sales page, granting a role `resto_menu`/`resto_tables` also grants product/table mutation rights there, not just in resto-display — worth knowing when assigning these permissions. `qr/reset` stays strictly owner-only (unchanged) since it invalidates a printed sticker immediately.
- **Table sessions** (`table_sessions`, migration 048) group every order from one dining visit so a table's QR always resolves to whatever session is currently active — an old scan can't leak into the next party's visit. A session opens implicitly on a table's first order (guest QR or staff) or explicitly via staff "Seat Party," and closes only via staff "Close Table" / "Settle" (`server/services/tableSessionService.js` + `restoSettleService.js`) — **manual only, no auto-close job**, a deliberate trade-off accepted knowingly: a forgotten table stays "occupied" until a staff member clears it. `restaurant_tables.status` (available/occupied) is kept as a derived flag the session service writes, not a second source of truth.
- **Payment: open tabs, settled at the table (migration 050).** The QR self-order screen has **no payment step** — a walk-in shouldn't have to pay before eating. Guest QR orders *and* staff-entered dine-in orders both land with `sales.payment_method='unpaid'` (a sentinel like `'room_charge'`, not a real `payment_methods` row) and still fire straight to the kitchen. When the party's done, staff hit **Settle** on the table in the resto app (`TablesScreen.jsx`'s bill modal): pick **one** method for the whole session — cash/QRIS/etc, or **Charge to room** (picks from `GET /api/resto/rooms`, the in-house list) — and `restoSettleService.settleAndClose` flips every unpaid non-rejected sale to that method + stamps `settled_at`/`settled_by` + posts folio charges if `room_charge`, then closes the session. One payment per table, no split (scope choice). `POST /api/resto/orders`'s `payment_method` is now optional (omit = open tab); a real method still works for immediate-pay takeaway. `/tables/:id/close` 409s (`code:'UNPAID'`) if anything's still unpaid — settle first. `salesService.createSale` dropped `sales.payment_method`'s old hardcoded `cash`/`qris`/`room_charge` CHECK back in migration 048 and is still the one validation choke point (staff POS, Room Display, resto app): `room_charge` needs a `booking_id`, `unpaid` skips validation + folio, anything else is checked against the property's real `payment_methods` table. Room Display room-service orders are unaffected — they still go `room_charge` + confirm queue.
- **Backend:** `server/routes/resto.js` (staff surface) + `server/routes/restoGuest.js` (guest QR surface), both gated `moduleGuard('resto_ordering')` per-route rather than at the `app.use` mount (same reason as `/api/kitchen`/`/api/calls`). `server/services/tableSessionService.js` (session open/close/summary) and the confirm/reject logic in `resto.js` (rejecting restores stock via a compensating `stock_movements` row and nulls `kitchen_status` so a rejected order can never resurface on the kitchen board). Revenue aggregates (`reports.js`, `dashboard.js`, `nightAudit.js`) exclude `confirmation_status='rejected'` rows; the PMS sales list (`routes/sales.js`) deliberately does not, so staff can still see declined orders there.
- **Menu and table management live in resto-display too, not just the PMS**: `MenuManagementScreen.jsx` (`/staff/menu`, `resto_menu` permission) adds/edits `products` directly — the exact same `/api/products` endpoints the PMS Sales page's Products tab uses, so a menu item created either place shows up everywhere immediately. `TablesScreen.jsx`'s "+ Add Table" posts to `/api/tables` the same way. Both reuse existing shared routes rather than duplicating product/table CRUD inside `routes/resto.js`.
- **Stack:** React/Vite PWA (`resto-display/`), dev port 5178, CORS origin via `RESTO_URL` env var, printable per-table QR sheet via `qrcode` (`TableQrSheet.jsx`). Deployed at `resto.zahill.kdai.cloud` per `nginx/resto.conf`, same domain-deferral note as Kitchen Display above. `RESTO_PUBLIC_URL` builds the absolute QR link (falls back to `RESTO_URL`'s first origin).
- **Existing `/sales` PMS page and its dine-in flow are untouched** — this is additive. A property without the module keeps exactly what it has today.
- **Front desk / restaurant catalog split (interim) — implemented 2026-09-22.** Raised directly: `products`/`sales` is one shared table used by both `Sales.jsx` (front desk's ancillary-charges tool — extra bed, merchandise, tours, misc fees) and the whole F&B stack (Kitchen Display, Room Display's "Order Food", resto-display's staff/guest ordering, `MenuManagementScreen.jsx`) — with no separation, a food item could show up in front desk's POS grid and, worse, a front-desk-only item (e.g. "Extra Bed") could show up in a guest's room-service or table QR menu as something orderable. **This pass is a UI/read-path-level fix, not a schema change** — a real separate table for restaurant vs. front-desk items is scoped as its own, larger, separately-planned migration (touches `sale_items`/`stock_movements`/`purchase_order_items`'s FKs, `salesService.createSale`'s one choke point, and 10 route/service files — see "Open Decisions" below). For now:
  - `routes/display.js`'s `GET /room/:roomId/menu` (guest "Order Food"), `routes/resto.js`'s `GET /menu` + its category list, and `routes/restoGuest.js`'s guest QR `GET /menu` all now filter `WHERE category IN ('drinks','food')` unconditionally — these three endpoints exist only to sell food/drinks, so the scope is correct regardless of module state, not conditional on anything.
  - `Sales.jsx` hides `drinks`/`food` (Sell tab grid, Products tab list, and the add/edit-product category dropdown) whenever `hasModule('resto_ordering')` — a property without Resto Ordering enabled keeps the full, unfiltered single-POS experience exactly as before. A subtitle explains the split when it's active ("Drinks & food are sold through the restaurant app...").
  - `resto-display/src/screens/MenuManagementScreen.jsx` (which calls the shared, unfiltered `/api/products` CRUD list directly) now scopes its own `CATEGORIES` constant to `['drinks','food']` only — so it never lists, edits, or lets staff create a front-desk item either, mirroring `Sales.jsx`'s restriction in the other direction.
  - Back Office (Purchasing/Inventory Value) is deliberately **not** filtered — it's a cross-category owner financial view and correctly needs to see every item regardless of catalog origin.
  - Verified against the live dev DB with real HTTP round-trips: created one `food` and one `other` test product, confirmed all three F&B ordering endpoints return only the food item, confirmed the `Sales.jsx` filtering logic (simulated against the real data) shows only the non-food item when `resto_ordering` is on and both when it's off. Test data and temporarily-toggled modules fully reverted afterward.
- **Deliberately not built this pass:** auto-close of idle table sessions (revisit if forgotten tables become a real problem — natural follow-up is a `closeStaleSessions()` job on the existing nightly job runner, same pattern as night audit's auto-no-show), a separate credential store for floor staff (rejected in favor of reusing PMS users/roles), and COGS/recipe costing (belongs to the separate, not-yet-started `back_office` module — see Open Decisions below).

---

## Market Insights (Dashboard)

Three cards on the Owner Dashboard, backed by `server/routes/insights.js`, `server/jobs/marketInsights.js`, `server/jobs/index.js`. All external API calls degrade gracefully (skip + log) when their key isn't set.

**Per-property setup (migration 069, 2026-09-25)** — nothing here is Zahill-hardcoded any more. Settings → Property Details → **Market Insights** card (owner, shown when `insights` is on): Area (`market_area`, appended to every Google Places search — `findPlace(name, area)`), up to 5 Google searches to follow (`market_keywords`), a short description for the AI briefing (`market_description`), and **Your Google listing** (`PUT /api/insights/self` — the competitors row with `is_self`; a new one retires the previous self row, history kept). `GET/PUT /api/insights/settings`; saving new search terms fetches their trend data straight away in the background. The Dashboard cards point owners to that card when nothing is set.
- **Competitor Ratings** — manually curated. Owner types a competitor name; backend resolves via Google Places Text Search (`services/googlePlaces.js`, searched as "name + the property's area"), tracks rating/review count/price level. `POST /api/insights/competitors`, `DELETE /api/insights/competitors/:id` (soft delete). Daily refresh at 06:00 for every active property.
- **Search Interest** — Google Trends (unofficial library). Each property's own `market_keywords`; `GET /api/insights/trends` only returns terms the property still follows.
- **AI Weekly Briefing** — Claude API (`services/claude.js`), structured JSON output, Monday refresh. The prompt describes the property from its own settings (`describeProperty`: name, description, area, room count from `units`) — it used to hardcode "Zahill Glamping Kintamani, a 5-unit glamping property". Model **`claude-opus-5`**, effort low, `max_tokens` 16000 (thinking is on by default on Opus 5), server-side refusal fallbacks (`betas: ['server-side-fallback-2026-07-01']`, `fallbacks: 'default'`); a declined request returns null and last week's briefing is kept.
- **Holidays** — `holidays` table (global, shared across every property). National holidays + cuti bersama/joint-leave days are synced nightly (01:30 WITA, before Yield Pricing's 02:00 run) from the live **api.co.id Indonesian Holidays API** (`server/services/holidayApi.js`, base URL `https://use.api.co.id`, header `x-api-co-id`, `HOLIDAY_API_KEY`/`HOLIDAY_API_URL` env vars — no-ops gracefully when unset). `jobs/marketInsights.js`'s `refreshHolidays()` fetches the current year + 2 years ahead, keeps only rows flagged `is_holiday`/`is_joint_holiday` (drops the API's "Observance"/awareness-day rows — Hari Kartini etc. — which don't move guest demand), and deletes+reinserts by the **exact dates returned**, `source='api_co_id'`, so a year the API hasn't published yet (observed: nothing for 2027 as of 2026-09-21) is left alone rather than wiped — migration 061's hardcoded `source='skb'` 2026–2027 seed stays as a per-date fallback until the live API actually covers that date, then this same sync supersedes it automatically. Balinese Hindu observances (Galungan/Kuningan/Nyepi, pawukon calendar — not in any generic holiday API) stay `source='manual'`, seeded in migration 014, untouched by the sync — same for per-property `yield_holiday_uplifts` overrides (keyed by date, migration 061). Manual trigger: `POST /api/insights/holidays/refresh` (owner-only). Region-specific beyond the national feed — a non-Bali property still needs its own Balinese-equivalent local observances seeded manually.

Manual refresh triggers (owner only): `POST /api/insights/competitors/refresh`, `/api/insights/trends/refresh`, `/api/insights/summary/refresh`.

---

## Environment Variables

See `server/.env.example` for the full current list (kept up to date — check there first, not here, since env vars change often). Categories: server/DB core, JWT, CORS origins (`CLIENT_URL`/`DISPLAY_URL`/`TV_URL`, comma-separated for multi-origin dev), Room Display/TV `DISPLAY_TOKEN`, MQTT, Market Insights (`GOOGLE_PLACES_API_KEY`, `ANTHROPIC_API_KEY`), weather, platform-default SMTP (`PLATFORM_SMTP_*`, used when a property hasn't set its own), and `SERVER_PUBLIC_URL` (this server's own publicly reachable base URL, used to build absolute property-logo URLs for guest emails — unset in local dev just skips the logo image in emails).

---

## Important Conventions

- **Every new table needs `property_id`** (unless it's genuinely platform-global, like `properties` itself or superadmin-only tables) — and every route touching it needs `WHERE property_id = $n` on every SELECT/INSERT/UPDATE/DELETE. This is the single easiest thing to forget when adding a feature; it's the difference between multi-tenant and a data leak between clients.
- **Relays are generic** — `relay_1`...`relay_N` in firmware/DB. Human-readable labels live in `unit_relays`. Never hardcode relay purposes in firmware.
- **Room IDs** — ESP32 uses simple string IDs, mapped via `units.controller_id`.
- **No localStorage in PWA artifacts** — use React state or backend for persistence (exception: Room Display's own device-local room ID/token, which is intentionally local to that physical tablet).
- **CommonJS in server** — `require()`, not `import`. Do not convert to ESM.
- **Migrations** — numbered SQL files in `server/db/migrations/`. Check the migrations table above (or the actual highest-numbered file on disk) for the next number rather than trusting a restated number here — update the "Next migration number" line in both this file and `ROADMAP.md` when you add one.
- **New features that touch routes must consider**: does this need a new module in `server/modules.js`? Does it need `property_id` scoping? Should the migration number and this file's migration table both be updated in the same commit?
- **New module-gated client pages need `RequireModule` on the route, not just a `hasModule()` check in the nav** — `client/src/App.jsx`'s route list and `client/src/components/Sidebar.jsx`/`BottomNav`'s nav-visibility checks are two separate places that must agree; it's easy to add one and forget the other (this was a real, multi-route bug fixed in one pass — see `git log` around the sidebar redesign).

---

## Open Decisions (not yet final — context for whoever picks this up next)

- ~~**Product name:** currently "Zahill PMS" / "ZHP PMS" are placeholders. Shortlisted a real brand name: **Nestly** is the front-runner...~~ **Decided (2026-08-14): the product name is HALF.** See the domain/tenancy note under "What this is" above for the working domain (`half.kdai.cloud`) and architecture. Actual rename across code/docs/infra is still pending — see that note for what's not done yet.
- **Separate `products` table for restaurant vs. front-desk catalogs:** raised directly 2026-09-22, while scoping the front-desk/restaurant split above. Agreed the reasoning is sound — same precedent as Back Office's `raw_materials` getting its own table instead of reusing `products`, for the identical "wrong item leaks into the wrong surface" risk — but this is a real schema migration (3 FK-dependent tables: `sale_items`, `stock_movements`, `purchase_order_items`; touches ~10 route/service files including `salesService.createSale`, the one choke point every sale goes through) and deliberately **not** done in the same pass as a live property. 2026-09-22's fix was the interim, UI/read-path-only version (see the Resto Ordering section above) — safe to ship now, doesn't block or complicate this real split later. Revisit once the app has run settled on production for a while; plan it properly (likely the same "exactly one of product_id/raw_material_id" `CHECK` pattern `purchase_order_items` already uses) rather than rushing it.
- **WhatsApp messaging (Phase B item 5):** decided against Fonnte (unofficial WhatsApp Web gateway) due to real ban risk for a product resold to paying clients. Leaning toward **api.co.id** — an Indonesian Meta Tech Provider offering the official WhatsApp Cloud API at low cost with no per-message markup. Architecture direction: **one WhatsApp number/WABA per property** (not one shared platform-wide number), managed under the platform's own Meta Business Portfolio via the Tech Provider / Embedded Signup model — this keeps messages branded as the property (guests see "Zahill," not the platform name) while sparing each client the full manual Meta Business Verification (they can start immediately at the unverified 250-conversations/day tier). Not yet implemented — no `whatsapp` module, no migration, no route file exist yet. See chat history for the full reasoning if picking this up cold.

---

## Roadmap Status (summary — full detail in ROADMAP.md)

- ✅ Foundation complete (migrations 001–026): core PMS, operations, sales, loyalty, market insights, in-room media, Room Controller/IoT, multi-tenancy, module system, superadmin.
- ✅ Phase A (Guest Folio, Invoice/Receipt PDF, Tax & Service Charge config) — implemented, migrations 028–029.
- ✅ Phase B item 4 (Automated Email) — implemented, migrations 030–031.
- ⏳ Phase B item 5 (WhatsApp messaging) — not started; leaning toward api.co.id (official WhatsApp Cloud API), see Open Decisions below.
- ✅ Superadmin Property Branding (logo, brand color, contact info) — implemented, migration 032.
- ✅ Phase D item 9 (Group Bookings) — implemented, migration 033.
- ✅ Phase D item 10 (F&B / Full POS) — implemented across four slices: Kitchen Display System + guest self-ordering (migration 034), then table management + stock tracking (migrations 035–036). Kitchen Display later extracted from the PMS client into its own standalone kiosk app (`kitchen-display/`), same `display_token` model as Room/TV Display.
- ✅ Resto Ordering — implemented, migration 048. New `resto_ordering` module (default off) + new standalone app `resto-display/`: guest QR table self-order, staff order-taking, a room-service confirm queue, and table/session management. Not yet manually verified in a browser.
- ✅ Phase D item 11 (Concierge / Activities) — implemented, migration 037. New `activities` module (default on).
- ✅ Guest Board ↔ Activities linking — implemented, migration 038. Not yet manually verified in a browser.
- ✅ Room Display "Your Orders" status tab — implemented, no migration (reads existing tables). Not yet manually verified in a browser.
- ✅ Two-way room-to-desk calling — implemented, migration 039. Staff can now call a room, not just answer one. **Verified live in production 2026-08-20** on a real tablet — caught and fixed a real bug along the way (occupied-room `CallOverlay` was missing its `onAnswer` wiring, see `ROADMAP.md`).
- ✅ Room ID decoupled from `room_controller`; calling split into its own `calling` module — migration 040. Fixes real production friction: hardware-less clients previously couldn't set a Room ID or use calling at all. Core flow confirmed working live 2026-08-20.
- ✅ Phase D item 13 (Agent Accounts / Direct Billing — company, travel agent, wholesaler) — **Slices A + B + C shipped** (migrations 041–043). Slice A: `booking_sources` doubles as the per-property agent registry. Slice B: `bookings.folio_status` + `agent_commissions`; checkout takes `bill_to_agent`, `server/services/agentBillingService.js` `settleCheckout()` marks the folio `pending_agent_invoice` and posts commissions; non-blocking credit-limit warning. Slice C: `agent_invoices`/`agent_payments`/`agent_payment_allocations`, `server/services/agentStatementService.js` + `server/routes/agents.js` + `client/src/pages/Agents.jsx` (`/agents` AR-aging list → `/agents/:sourceId` statement with Record Payment auto-allocated oldest-first + editable, and Generate Invoice → consolidated pdfkit PDF via `server/services/agentInvoicePdf.js`). Client decisions locked: credit limit = warn only, Faktur Pajak = deferred, allocation = auto oldest-first + manual adjust. **Booking page badge (2026-09-25):** `BookingDetail.jsx`'s Stay card shows an "Agent billing" row whenever `folio_status` is set — Not invoiced yet / Invoiced — awaiting payment (+ invoice number, amount received so far) / Paid by agent — plus an "Agent statement →" link for owners; `GET /api/bookings/:id` now returns `agent_invoice_number` and `agent_paid_amount`. Full spec in `ROADMAP.md`. Not yet manually verified in a browser (service + HTTP layers tested).
- 🟡 Phase D item 14 (Back Office — Purchasing, Inventory Cost Control, Accounts Payable, Cash & Bank, Recipe Costing) — scoped 2026-08-17 against both guestpro.id and the client's own prior system VHP (sindata.net), which independently converge on the same component set. **Implemented so far, all 2026-09-13:** Slice A (migration 053: Suppliers + Purchasing + PO receiving), Slice B (migration 055: Expenses, a lightweight categorized cost log — not a step toward full accounting), and Inventory Value (migration 056: `products`/`raw_materials` cost-at-hand, deliberately no COGS yet). New `back_office` module (default **off**, paid-tier add-on) — see `ROADMAP.md`'s write-up for the full design, including the raw-materials-get-their-own-table decision and the accounting reasoning behind sequencing Inventory Value before COGS. Cash & Bank, AP Bills, and Recipes/COGS remain 🔵 not started. Full spec in `ROADMAP.md`; explicitly excludes full General Ledger/journal/budgeting/asset-depreciation from v1.
- ✅ Yield / dynamic pricing v1 (migration 061) and Room types (migration 062) — built and deployed to production 2026-09-21. Yield module is off by default (paid add-on); room types are core. Follow-ups (holiday sync, AI events, per-room-type Channex mapping) are listed under "Pricing page tabs + Unit Settings" above and in `ROADMAP.md`.
- 🟡 Phase C item 7 (Channel Manager + Dynamic Pricing) — vendor research done, Channex staging spike validated end-to-end (migration 060), production automation and the yield-pricing engine both not started. Full step-by-step in `ROADMAP.md`. Direct Booking Engine (item 6) still not started.
- ⚪ Phase D remaining (Reviews & Feedback, Stripe subscription billing) — build when a client actually requests it.

Check `ROADMAP.md` directly before starting new work — this summary will drift faster than that file does.

---

## Working Across Multiple Machines (laptop + PC)

Since development happens on two machines, **this file and `ROADMAP.md` are the handoff mechanism** between them — not memory, not chat history. Treat doc updates as part of the feature, not cleanup afterward.

**Every time you (or Claude) finish a feature, migration, or architectural change:**
1. Update `ROADMAP.md` — flip the status, bump "Next migration number" if a migration was added.
2. Update this file (`CLAUDE.md`) if the change affects: routes, migrations table, module registry, module list, env vars, naming conventions, or known gaps.
3. Commit docs **in the same commit** as the code change, not a separate "docs" commit later — it's too easy to skip that second commit.
4. `git push` before walking away from either machine.

**Releasing to `main`:** `git checkout main && git merge --ff-only dev && git push`. Don't cherry-pick — `main` and `dev` were rejoined on 2026-09-26 (`bb29936`) after cherry-picked releases had left them with the same code under different commit ids; `main` should always be a fast-forward of `dev`. If `--ff-only` refuses, something was committed straight to `main` — merge `main` into `dev` first.

**Starting a session on either machine:**
1. `git pull` first, always — don't trust memory of "where things were."
2. Re-read this file's Multi-Tenancy and Roadmap Status sections if it's been more than a few days.
3. Run `npm run migrate` in `server/` if migrations were added on the other machine — check the migrations table above against what's actually applied in your local DB if unsure.
4. `npm install` in `client/` and `server/` if `package.json`/lockfiles changed.

If a feature was left half-done on one machine, add a one-line note under "Known gaps" in the relevant section above (or a `<!-- TODO -->` comment) rather than relying on remembering it — the whole point of this file is that neither machine has to hold context in your head.
